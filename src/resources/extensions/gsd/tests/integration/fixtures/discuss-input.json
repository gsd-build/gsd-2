{
  "description": "A CLI tool that converts markdown files to HTML. It should read .md files from a source directory, convert them to styled HTML using a default theme, and write the output to a build directory. It should support a --watch flag for live rebuilding, a --theme flag to select different CSS themes, and a --port flag to serve the output locally with live-reload. The tool is called 'mdsite' and targets Node.js 20+.",
  "answers": {
    "questions": {},
    "secrets": {},
    "defaults": {
      "strategy": "first_option"
    }
  }
}
