---
phase: 03-panel-shell-design-system
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/mission-control/tests/panel-shell.test.tsx
  - packages/mission-control/tests/panel-states.test.tsx
  - packages/mission-control/tests/layout-storage.test.ts
  - packages/mission-control/tests/design-tokens.test.ts
autonomous: false
requirements:
  - PNLS-01
  - PNLS-02
  - PNLS-03
  - PNLS-04
  - PNLS-05
  - PNLS-06
  - PNLS-07

must_haves:
  truths:
    - "Automated tests verify panel default sizes sum to 100 and match spec"
    - "Automated tests verify design token values match the spec exactly"
    - "Automated tests verify PanelWrapper renders correct state for loading/empty/error conditions"
    - "Visual verification confirms the full design system renders correctly in browser"
  artifacts:
    - path: "packages/mission-control/tests/panel-shell.test.tsx"
      provides: "Tests for PanelShell default sizes and panel count"
    - path: "packages/mission-control/tests/panel-states.test.tsx"
      provides: "Tests for PanelWrapper loading/empty/error routing"
    - path: "packages/mission-control/tests/layout-storage.test.ts"
      provides: "Tests for createSessionStorage adapter"
    - path: "packages/mission-control/tests/design-tokens.test.ts"
      provides: "Tests for design token values"
  key_links:
    - from: "tests/"
      to: "src/components/"
      via: "import and render assertions"
      pattern: "import.*PanelShell|PanelWrapper|PanelSkeleton"
---

<objective>
Write automated tests for the panel layout, state components, storage adapter, and design tokens. Then run a human visual verification checkpoint.

Purpose: Validate the Phase 3 implementation meets all PNLS requirements before moving to Phase 4.
Output: Test files covering all automated requirements, visual sign-off on the complete panel shell.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-panel-shell-design-system/03-RESEARCH.md
@.planning/phases/03-panel-shell-design-system/03-01-SUMMARY.md
@.planning/phases/03-panel-shell-design-system/03-02-SUMMARY.md

@packages/mission-control/src/styles/design-tokens.ts
@packages/mission-control/src/components/layout/PanelShell.tsx
@packages/mission-control/src/components/layout/PanelWrapper.tsx
@packages/mission-control/src/components/states/PanelSkeleton.tsx
@packages/mission-control/src/components/states/PanelEmpty.tsx
@packages/mission-control/src/components/states/PanelError.tsx
@packages/mission-control/src/lib/layout-storage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for panel layout, states, storage, and tokens</name>
  <files>packages/mission-control/tests/panel-shell.test.tsx, packages/mission-control/tests/panel-states.test.tsx, packages/mission-control/tests/layout-storage.test.ts, packages/mission-control/tests/design-tokens.test.ts</files>
  <behavior>
    - design-tokens.test.ts: PANEL_DEFAULTS values sum to 100; COLORS.navy.base equals "#0F1419"; COLORS.cyan.accent equals "#5BC8F0"; TYPOGRAPHY.sizes has exactly 4 entries (xs/sm/base/lg); SPACING values are all multiples of 4px
    - layout-storage.test.ts: createSessionStorage().setItem writes to localStorage; createSessionStorage().getItem reads from localStorage; getItem returns null for missing key
    - panel-states.test.tsx: PanelSkeleton renders without error for each variant; PanelEmpty renders title and description; PanelError renders error message; PanelError renders retry button when onRetry provided; PanelError hides retry button when onRetry omitted
    - panel-shell.test.tsx: PANEL_DEFAULTS match spec (sidebar=14, milestone=22, sliceDetail=19, activeTask=21, chat=24)
  </behavior>
  <action>
1. Check if `happy-dom` or `@happy-dom/global-registrations` is installed. If not, install it: `bun add -d happy-dom`. This provides DOM APIs for component testing in Bun.

2. **design-tokens.test.ts:**
   - Import COLORS, TYPOGRAPHY, SPACING, PANEL_DEFAULTS from design-tokens
   - Test PANEL_DEFAULTS values sum to 100
   - Test PANEL_DEFAULTS.sidebar === 14, milestone === 22, sliceDetail === 19, activeTask === 21, chat === 24
   - Test COLORS.navy.base === "#0F1419", COLORS.cyan.accent === "#5BC8F0"
   - Test TYPOGRAPHY has fontDisplay and fontMono
   - Test TYPOGRAPHY.sizes has xs, sm, base, lg keys
   - Test all SPACING values parse to numbers divisible by 4

3. **layout-storage.test.ts:**
   - Mock localStorage (or use happy-dom's)
   - Test setItem stores value, getItem retrieves it
   - Test getItem returns null for unknown key

4. **panel-states.test.tsx** (if happy-dom available for JSX rendering):
   - Test PanelSkeleton renders without throwing for each variant string
   - Test PanelEmpty renders provided title text
   - Test PanelError renders error.message text
   - Test PanelError renders retry button only when onRetry is provided

   If DOM testing proves too complex with current Bun setup, write these as smoke tests that just import and call the components to verify no import/type errors. Note in test file what was simplified and why.

5. **panel-shell.test.tsx:**
   - Test is primarily a constants/contract test: verify PANEL_DEFAULTS match the spec exactly
   - If DOM rendering available, test that PanelShell can be imported without error

Run `cd packages/mission-control && bun test` to verify all tests pass.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test</automated>
  </verify>
  <done>All 4 test files exist and pass. Design token values verified against spec. Panel defaults sum verified. State components verified. Storage adapter verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of panel shell and design system</name>
  <files>none</files>
  <action>
    Human visually verifies the complete five-panel resizable dashboard layout with design system in the browser.
    What was built:
    - Five panels (Sidebar, Milestone, Slice Detail, Active Task, Chat) with drag-resizable handles
    - Dark navy theme with cyan accent highlights on handles
    - Share Tech Mono font on panel headers, JetBrains Mono on body text
    - Empty states in each panel with contextual messages
    - Panel sizes persist across page refresh
  </action>
  <verify>
    1. Run `cd packages/mission-control && bun run dev` and open http://localhost:4000
    2. Verify five panels render side-by-side filling the viewport
    3. Verify dark navy (#0F1419) background across all panels
    4. Verify panel headers show uppercase text in Share Tech Mono font
    5. Verify body text renders in JetBrains Mono font
    6. Drag each panel handle to resize -- verify all 4 handles work
    7. Refresh the page -- verify resized layout persists
    8. Verify each panel shows an empty state with icon and message (not spinners, not raw text)
    9. Verify cyan accent (#5BC8F0) appears only on handle hover/active states, not as decoration
    10. Check spacing consistency -- padding/margins should feel uniform (8px multiples)
  </verify>
  <done>User types "approved" confirming all 10 verification points pass</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/mission-control && bun test` passes with all new test files
- Visual verification confirms five-panel layout with design system
- Resize handles work and persist sizes
- All panels show proper empty states
</verification>

<success_criteria>
All automated tests green. Human visual verification confirms: five resizable panels, dark theme, correct fonts, proper empty states, cyan accent only on interactive elements, consistent 8-point spacing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-panel-shell-design-system/03-03-SUMMARY.md`
</output>
