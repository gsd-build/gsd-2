---
phase: 07-session-flow-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/styles/animations.css
  - packages/mission-control/src/styles/globals.css
  - packages/mission-control/src/server/session-status-api.ts
  - packages/mission-control/src/server.ts
  - packages/mission-control/src/components/session/LogoAnimation.tsx
  - packages/mission-control/src/components/session/LoadingLogo.tsx
  - packages/mission-control/tests/session-status-api.test.ts
  - packages/mission-control/tests/animations.test.tsx
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
must_haves:
  truths:
    - "CSS @keyframes for logo build animation exist with 600ms total duration"
    - "Loading logo scan line animation loops at 200ms"
    - "Session status API returns continue-here data when file exists"
    - "Session status API returns null continueHere when no continue-here file exists"
  artifacts:
    - path: "packages/mission-control/src/styles/animations.css"
      provides: "CSS @keyframes for logo-build, logo-scan, amber-pulse, all animation utility classes"
      contains: "@keyframes"
    - path: "packages/mission-control/src/server/session-status-api.ts"
      provides: "GET /api/session/status endpoint returning continueHere data"
      exports: ["handleSessionStatusRequest"]
    - path: "packages/mission-control/src/components/session/LogoAnimation.tsx"
      provides: "Animated GsdLogo with sequential rect reveals over 600ms"
      exports: ["LogoAnimation"]
    - path: "packages/mission-control/src/components/session/LoadingLogo.tsx"
      provides: "GsdLogo with scan line animation for initializing state"
      exports: ["LoadingLogo"]
  key_links:
    - from: "packages/mission-control/src/styles/globals.css"
      to: "packages/mission-control/src/styles/animations.css"
      via: "@import"
      pattern: "@import.*animations"
    - from: "packages/mission-control/src/server.ts"
      to: "packages/mission-control/src/server/session-status-api.ts"
      via: "route handler"
      pattern: "handleSessionStatusRequest"
---

<objective>
Create the CSS animation foundation and server-side session status API for Phase 7.

Purpose: Establish the animation @keyframes (logo build 600ms, scan line 200ms, amber pulse 150ms) and the /api/session/status endpoint that detects continue-here files. These are consumed by Plan 02 (session flow) and Plan 03 (micro-interactions).

Output: animations.css with all @keyframes, LogoAnimation and LoadingLogo components, session-status-api.ts with continue-here detection, tests for both.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-session-flow-animation/07-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/components/sidebar/GsdLogo.tsx:
```tsx
interface GsdLogoProps {
  className?: string;
}
export function GsdLogo({ className }: GsdLogoProps): JSX.Element
// SVG with viewBox="0 0 32 32", uses currentColor
// Rect groups: frame (x=2,y=4), titlebar (x=2,y=4,h=4), dots (x=4,y=5 + x=8,y=5), chevron (x=6,y=12 three rects), cursor (x=18,y=14 two rects)
```

From src/server.ts (route pattern):
```typescript
// Each API handler follows: if (pathname.startsWith("/api/X")) { const response = await handleXRequest(req, url, ...); if (response) return addCorsHeaders(response); }
// Session status needs planningDir from pipeline.getPlanningDir()
```

From src/styles/globals.css:
```css
@import "tailwindcss";
@import "tw-animate-css";
/* Add @import "./animations.css"; after tw-animate-css */
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSS animation keyframes + LogoAnimation + LoadingLogo components</name>
  <files>
    packages/mission-control/src/styles/animations.css,
    packages/mission-control/src/styles/globals.css,
    packages/mission-control/src/components/session/LogoAnimation.tsx,
    packages/mission-control/src/components/session/LoadingLogo.tsx,
    packages/mission-control/tests/animations.test.tsx
  </files>
  <behavior>
    - LogoAnimation renders SVG with animation classes on rect groups, total duration 600ms
    - LogoAnimation accepts onComplete callback, fires after 600ms
    - LoadingLogo renders SVG with scan line element that has infinite 200ms animation
    - CSS file contains @keyframes gsd-build-in, logo-scan, amber-pulse
    - Each logo rect group (.logo-anim-frame, .logo-anim-titlebar, .logo-anim-dots, .logo-anim-chevron, .logo-anim-cursor) has sequential animation-delay summing to 600ms
  </behavior>
  <action>
    1. Create `src/styles/animations.css` with @keyframes:
       - `gsd-build-in`: opacity 0->1, transform scale(0.8)->1
       - `logo-scan`: translateX(-100%) -> translateX(100%), linear, 200ms infinite
       - `amber-pulse`: box-shadow amber 0.6 -> transparent 0px -> 8px, 150ms ease-out (for Plan 03)
       Add utility classes: `.logo-anim-frame` (delay 0ms, 120ms dur), `.logo-anim-titlebar` (delay 120ms, 100ms), `.logo-anim-dots` (delay 220ms, 80ms), `.logo-anim-chevron` (delay 300ms, 150ms), `.logo-anim-cursor` (delay 450ms, 150ms). All use `animation-fill-mode: forwards; opacity: 0`.

    2. Update `src/styles/globals.css`: Add `@import "./animations.css";` after the tw-animate-css import.

    3. Create `src/components/session/LogoAnimation.tsx`:
       - Renders the same SVG as GsdLogo but with animation classes on each rect group
       - Props: `{ className?: string; onComplete?: () => void; size?: "sm" | "lg" }`
       - Size "lg" = h-16 w-16 (default for onboarding), "sm" = h-8 w-8
       - Uses useEffect with 600ms setTimeout to fire onComplete
       - Each SVG group element gets the corresponding `.logo-anim-*` class

    4. Create `src/components/session/LoadingLogo.tsx`:
       - Renders GsdLogo SVG at 50% opacity with an overlay rect using `.logo-scan-line` class
       - The scan line is a thin rect (width=4, full height) that translates across the logo
       - Props: `{ className?: string }`

    5. Create `tests/animations.test.tsx`:
       - Test LogoAnimation renders SVG with animation class names on rect groups
       - Test LogoAnimation fires onComplete callback (use fake timers, advance 600ms)
       - Test LoadingLogo renders scan line element with animation class
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/animations.test.tsx</automated>
  </verify>
  <done>LogoAnimation component renders animated SVG with sequential 600ms build. LoadingLogo shows scanning animation. CSS keyframes defined for all Phase 7 animations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session status API endpoint with continue-here detection</name>
  <files>
    packages/mission-control/src/server/session-status-api.ts,
    packages/mission-control/src/server.ts,
    packages/mission-control/tests/session-status-api.test.ts
  </files>
  <behavior>
    - GET /api/session/status returns { continueHere: null } when no continue-here file exists
    - GET /api/session/status returns { continueHere: { phase, task, totalTasks, status, currentState, nextAction } } when continue-here file exists
    - Non-GET methods return null (not handled)
    - Non-matching paths return null
    - Missing phases directory returns { continueHere: null } gracefully
  </behavior>
  <action>
    1. Create `src/server/session-status-api.ts`:
       - Export `handleSessionStatusRequest(req: Request, url: URL, planningDir: string): Promise<Response | null>`
       - Only handles GET /api/session/status, returns null for other paths/methods
       - Scans `{planningDir}/phases/` for directories containing files with "continue-here" in name
       - Parses found file with gray-matter (already a dep) for frontmatter fields: phase, task, total_tasks, status
       - Extracts `<current_state>` and `<next_action>` XML-tagged sections from body content
       - Returns Response.json({ continueHere: { phase, task, totalTasks, status, currentState, nextAction } }) or { continueHere: null }

    2. Wire into `src/server.ts`:
       - Import handleSessionStatusRequest from "./server/session-status-api"
       - Add route block: `if (pathname.startsWith("/api/session/")) { const response = await handleSessionStatusRequest(req, url, pipeline.getPlanningDir()); if (response) return addCorsHeaders(response); }`
       - Place before the CORS preflight handler, after the existing /api/assets/ block

    3. Create `tests/session-status-api.test.ts`:
       - Create temp directory with phases/ subdirectory
       - Test: no continue-here file -> { continueHere: null }
       - Test: continue-here file with frontmatter + XML sections -> parsed correctly
       - Test: non-GET returns null
       - Test: wrong path returns null
       - Test: missing phases dir -> { continueHere: null } (no crash)
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/session-status-api.test.ts</automated>
  </verify>
  <done>GET /api/session/status returns continue-here data when file exists, null otherwise. Endpoint wired into server.ts route chain.</done>
</task>

</tasks>

<verification>
```bash
cd packages/mission-control && bun test tests/animations.test.tsx tests/session-status-api.test.ts
```
All tests pass. CSS file contains @keyframes. Server.ts has session status route.
</verification>

<success_criteria>
- animations.css has @keyframes gsd-build-in, logo-scan, amber-pulse
- LogoAnimation renders animated SVG with 600ms sequential build
- LoadingLogo renders scanning animation at 200ms loop
- GET /api/session/status returns continue-here data or null
- globals.css imports animations.css
- server.ts routes /api/session/* to handler
</success_criteria>

<output>
After completion, create `.planning/phases/07-session-flow-animation/07-01-SUMMARY.md`
</output>
