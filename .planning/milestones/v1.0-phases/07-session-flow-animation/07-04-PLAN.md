---
phase: 07-session-flow-animation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/components/active-task/TaskExecuting.tsx
  - packages/mission-control/tests/animations.test.tsx
autonomous: true
gap_closure: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04
  - ANIM-05

must_haves:
  truths:
    - "Task advance triggers a visible amber pulse (150ms) on the task status element when taskId changes"
  artifacts:
    - path: "packages/mission-control/src/components/active-task/TaskExecuting.tsx"
      provides: "Amber pulse on task ID change"
      contains: "task-advance-pulse"
    - path: "packages/mission-control/tests/animations.test.tsx"
      provides: "Test verifying pulse class application on taskId change"
      contains: "task-advance-pulse"
  key_links:
    - from: "src/components/active-task/TaskExecuting.tsx"
      to: "src/styles/animations.css"
      via: "task-advance-pulse CSS class"
      pattern: "task-advance-pulse"
---

<objective>
Wire the existing `.task-advance-pulse` CSS class to the TaskExecuting component so it triggers a 150ms amber pulse when the taskId prop changes.

Purpose: Close the single ANIM-04 verification gap -- the CSS keyframe exists but is never applied to a component.
Output: TaskExecuting pulses amber on task advance, test confirms the wiring.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-session-flow-animation/07-VERIFICATION.md

<interfaces>
<!-- From animations.css (lines 76-78): -->
.amber-pulse,
.task-advance-pulse {
  animation: amber-pulse 150ms ease-out;
}

<!-- From TaskExecuting.tsx (line 20-29): -->
interface TaskExecutingProps {
  taskId: string;
  wave: number;
  planNumber: number;
  filesCount: number;
  taskCount: number;
  mustHaves?: MustHaves;
  filesModified: string[];
  checkpoint?: string;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire task-advance-pulse to TaskExecuting on taskId change</name>
  <files>packages/mission-control/src/components/active-task/TaskExecuting.tsx, packages/mission-control/tests/animations.test.tsx</files>
  <behavior>
    - Test: When TaskExecuting renders, no pulse class is present initially
    - Test: When taskId prop changes (re-render with new taskId), the root container gets "task-advance-pulse" class
    - Test: After 150ms timeout, the pulse class is removed
  </behavior>
  <action>
1. In tests/animations.test.tsx, add a new describe block "TaskExecuting amber pulse on task advance" with tests:
   - Render TaskExecutingView (pure) with taskId="07-01", confirm no task-advance-pulse class.
   - Create a test that calls the pure isPulsing logic: given previousTaskId="07-01" and currentTaskId="07-02", return true. Given same taskId, return false.

2. In TaskExecuting.tsx, add pulse detection:
   - Import useRef and useEffect from React.
   - Track the previous taskId using a useRef. On each render, compare current taskId to previous. If different (and previous was not empty/undefined), set a `isPulsing` state to true.
   - Use useEffect with a 150ms setTimeout to clear isPulsing back to false.
   - Apply "task-advance-pulse" class conditionally to the root div when isPulsing is true.
   - Extract the comparison logic as a pure exported function `shouldPulseOnTaskChange(prev: string | undefined, current: string): boolean` for testability.

3. Run tests: `cd packages/mission-control && bun test tests/animations.test.tsx`
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/animations.test.tsx</automated>
  </verify>
  <done>TaskExecuting component applies .task-advance-pulse class for 150ms when taskId changes. Tests confirm pulse logic works. ANIM-04 gap closed.</done>
</task>

</tasks>

<verification>
- `cd packages/mission-control && bun test` -- full test suite passes
- grep confirms `task-advance-pulse` appears in TaskExecuting.tsx (not just animations.css)
</verification>

<success_criteria>
- ANIM-04 gap from 07-VERIFICATION.md is closed: `.task-advance-pulse` is applied to a component on task ID change
- All existing tests continue to pass
- The amber pulse triggers for exactly 150ms and auto-removes
</success_criteria>

<output>
After completion, create `.planning/phases/07-session-flow-animation/07-04-SUMMARY.md`
</output>
