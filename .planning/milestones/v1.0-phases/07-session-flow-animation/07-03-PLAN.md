---
phase: 07-session-flow-animation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - packages/mission-control/src/components/layout/SingleColumnView.tsx
  - packages/mission-control/src/components/views/ChatView.tsx
  - packages/mission-control/src/components/chat/ChatMessage.tsx
  - packages/mission-control/tests/animations.test.tsx
autonomous: false
requirements:
  - ANIM-03
  - ANIM-04
  - ANIM-05
must_haves:
  truths:
    - "Panels fade in with staggered 40ms delay and 200ms duration on view switch"
    - "Task advance triggers amber pulse animation on affected element"
    - "Chat messages slide up 80ms with opacity fade on arrival"
  artifacts:
    - path: "packages/mission-control/src/components/layout/SingleColumnView.tsx"
      provides: "Staggered panel fade-in with animate-in classes"
    - path: "packages/mission-control/src/components/chat/ChatMessage.tsx"
      provides: "Chat message with slide-up fade-in animation"
  key_links:
    - from: "packages/mission-control/src/components/layout/SingleColumnView.tsx"
      to: "packages/mission-control/src/styles/animations.css"
      via: "tw-animate-css classes + animation-delay inline style"
      pattern: "animate-in.*fade-in"
    - from: "packages/mission-control/src/components/chat/ChatMessage.tsx"
      to: "packages/mission-control/src/styles/animations.css"
      via: "tw-animate-css slide-in classes"
      pattern: "slide-in-from-bottom"
---

<objective>
Add micro-interaction animations to existing views: staggered panel fade-in, task advance amber pulse, and chat message slide-up. Then human verify all animations visually.

Purpose: Give Mission Control its branded motion feel. Panels fade in sequentially on view switches, task changes pulse amber, and chat messages slide up smoothly.

Output: Modified SingleColumnView with stagger animation, ChatMessage with slide-up, amber pulse utility ready for task views, visual verification.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-session-flow-animation/07-RESEARCH.md
@.planning/phases/07-session-flow-animation/07-01-SUMMARY.md
@.planning/phases/07-session-flow-animation/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 CSS animations -->
From src/styles/animations.css:
```css
/* .task-advance-pulse class uses @keyframes amber-pulse, 150ms ease-out */
/* tw-animate-css provides: animate-in, fade-in, slide-in-from-bottom, duration-200, duration-75 */
```

From src/components/layout/SingleColumnView.tsx:
```tsx
// Renders views based on activeView.kind: chat, milestone, history, settings, assets
// Each view is conditionally rendered with {activeView.kind === "X" && <XView ... />}
```

From src/components/chat/ChatMessage.tsx (need to find exact path):
```tsx
// Renders individual chat messages with distinct styling for agent vs system
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Staggered panel fade-in + chat message slide-up + amber pulse integration</name>
  <files>
    packages/mission-control/src/components/layout/SingleColumnView.tsx,
    packages/mission-control/src/components/views/ChatView.tsx,
    packages/mission-control/src/components/chat/ChatMessage.tsx,
    packages/mission-control/tests/animations.test.tsx
  </files>
  <action>
    1. Modify `src/components/layout/SingleColumnView.tsx` for staggered panel fade-in (ANIM-03):
       - Wrap the main content in a keyed animation wrapper. Use the `activeView.kind` as a React key on an inner div so React re-mounts on view switch, triggering CSS animation replay.
       - Add `className="animate-in fade-in duration-200"` to the inner wrapper div.
       - The key change is: wrap all view content inside main with `div key={activeView.kind} className="animate-in fade-in duration-200 h-full"` so each view switch triggers a 200ms fade-in.
       - For multi-panel stagger within a view, the 40ms stagger between "panels" maps to the view transition since we now have a single-column layout (not 5 panels). If the ChatView or MilestoneView has internal sections, add stagger there.
       - For MilestoneView specifically: if it renders multiple card sections, wrap each in a div with `animate-in fade-in duration-200` and `style={{ animationDelay: "${index * 40}ms" }}`.

    2. Modify chat message component for slide-up animation (ANIM-05):
       - First, find the chat message component. Check `src/components/chat/ChatMessage.tsx` or similar.
       - Add to each chat message wrapper: `className="animate-in fade-in slide-in-from-bottom duration-75"` (75ms is closest Tailwind duration to 80ms).
       - Only apply animation to newly added messages, not all messages on re-render. Approach: add the animation class and let CSS handle it -- since animate-in has `animation-fill-mode: forwards`, it only plays once on mount. React's reconciliation means only new messages get mounted, so existing messages won't re-animate.

    3. Add amber pulse for task advance (ANIM-04):
       - The `.task-advance-pulse` class is already defined in animations.css from Plan 01.
       - In the ChatView or wherever task status updates are displayed, add the class conditionally when a task advances. Since task advance is detected via state changes, the approach is:
         - Find the component that renders active task status (likely in ChatView or a task-related component within it).
         - When task ID changes (detected via useEffect comparing prev vs current), add `.task-advance-pulse` class temporarily. Use a state variable `isPulsing` that's set true on task change and reset after 150ms via setTimeout.
         - If no active task component is easily accessible in current views, add the pulse class to the status indicator in the Sidebar or wherever the current task is displayed. The simplest integration: add it to wherever `TASK-01` (executing state pulsing indicator) is rendered.
       - If the active task display is in a separate component not easily modified here, just ensure the `.task-advance-pulse` CSS class is available and document where to apply it. The CSS is already created in Plan 01.

    4. Update `tests/animations.test.tsx` (extend from Plan 01):
       - Add test: SingleColumnView wraps content with animation classes and uses activeView.kind as key
       - Add test: ChatMessage component has slide-in-from-bottom animation class
       - Add test: task-advance-pulse class exists in CSS (static assertion on file content)
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/animations.test.tsx && bun test</automated>
  </verify>
  <done>View switches trigger 200ms fade-in animation. Chat messages slide up with 75ms fade-in on arrival. Amber pulse CSS class available for task advance. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of session flow and all animations</name>
  <files>packages/mission-control/src/components/layout/AppShell.tsx</files>
  <action>
    Human verifies all session flow screens and animations visually.

    What was built across Plans 01-03:
    - Session state machine: initializing (loading logo) then onboarding (animated logo + guidance) or resume (overlay card) or dashboard (normal)
    - Logo build animation: 600ms sequential pixel-art reveal
    - Loading scan line: 200ms infinite loop across logo
    - Panel fade-in: 200ms on view switch
    - Chat message slide-up: 75ms on arrival
    - Amber pulse utility: 150ms flash (applied via .task-advance-pulse class)
    - Session status API: /api/session/status detects continue-here files

    Steps:
    1. Start dev server: `cd packages/mission-control && bun run dev`
    2. Open http://localhost:4000

    Test 1 - Normal Dashboard Load (existing project):
    - Should see brief loading logo, then dashboard appears with fade-in
    - No flash of onboarding screen
    - Switch between sidebar views (Chat, Milestones, History) -- each should fade in smoothly

    Test 2 - Chat Message Animation:
    - Type a message in chat and send
    - New messages should slide up from bottom with fade-in

    Test 3 - Onboarding (if testable):
    - Switch to a folder without .planning/ directory
    - Should see full-screen onboarding with animated logo build (600ms pixel-art reveal)
    - "Open Folder" and "Start Chat" buttons should appear after animation

    Test 4 - View Switch Stagger:
    - Click between sidebar nav items
    - Each view should fade in (200ms, not instant swap)
  </action>
  <verify>Human confirms animations are smooth, no jank, no flash of wrong content</verify>
  <done>All session flow screens render correctly. Animations feel smooth and natural. Human approves visual quality.</done>
</task>

</tasks>

<verification>
```bash
cd packages/mission-control && bun test
```
Full test suite green. Visual verification of all animations by human.
</verification>

<success_criteria>
- View switches animate with 200ms fade-in
- Chat messages slide up with ~80ms fade-in on arrival
- Logo animation plays 600ms sequential build on onboarding
- Loading state shows scanning logo animation
- No animation jank or layout thrashing
- Human approves visual quality of all animations
</success_criteria>

<output>
After completion, create `.planning/phases/07-session-flow-animation/07-03-SUMMARY.md`
</output>
