---
phase: 10-keyboard-shortcuts-accessibility
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/tests/keyboard-accessibility.test.ts
  - packages/mission-control/src/hooks/useCommandPalette.ts
  - packages/mission-control/src/hooks/usePanelFocus.ts
autonomous: true
requirements:
  - KEYS-01
  - KEYS-02
  - KEYS-06
must_haves:
  truths:
    - "Ctrl+Shift+P triggers command palette open (keyboard predicate correct)"
    - "Ctrl+1 through Ctrl+5 switch views (panel shortcut predicate correct)"
    - "Focus moves to view heading after view switch (panel focus hook wired)"
    - "All pure functions are directly testable without React renderer"
  artifacts:
    - path: "packages/mission-control/tests/keyboard-accessibility.test.ts"
      provides: "Test scaffold for KEYS-01 through KEYS-06"
      contains: "shouldOpenCommandPalette, shouldSwitchPanel"
    - path: "packages/mission-control/src/hooks/useCommandPalette.ts"
      provides: "Keyboard binding hook + exported pure predicate"
      exports: ["shouldOpenCommandPalette", "useCommandPalette"]
    - path: "packages/mission-control/src/hooks/usePanelFocus.ts"
      provides: "View-transition focus management hook + exported predicate"
      exports: ["shouldSwitchPanel", "usePanelFocus", "VIEW_SHORTCUTS"]
  key_links:
    - from: "tests/keyboard-accessibility.test.ts"
      to: "src/hooks/useCommandPalette.ts"
      via: "import shouldOpenCommandPalette"
      pattern: "shouldOpenCommandPalette"
    - from: "tests/keyboard-accessibility.test.ts"
      to: "src/hooks/usePanelFocus.ts"
      via: "import shouldSwitchPanel"
      pattern: "shouldSwitchPanel"
---

<objective>
Create the test scaffold for all Phase 10 keyboard/accessibility requirements, then implement the pure-function keyboard predicates and hooks that make the KEYS-01, KEYS-02, and KEYS-06 tests pass via TDD.

Purpose: Establishes the testable foundation for the command palette keyboard binding and panel view-switching shortcuts. Pure functions are extracted following the project's established pattern (shouldTogglePreview, shouldPulseOnTaskChange).
Output: keyboard-accessibility.test.ts with stubs for all 6 requirements; useCommandPalette.ts and usePanelFocus.ts with exported pure predicates; all KEYS-01/02/06 pure function tests green.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-keyboard-shortcuts-accessibility/10-RESEARCH.md
@.planning/phases/10-keyboard-shortcuts-accessibility/10-VALIDATION.md

@packages/mission-control/src/lib/slash-commands.ts
@packages/mission-control/src/lib/view-types.ts
@packages/mission-control/src/hooks/usePreview.ts

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/lib/view-types.ts:
```typescript
export type ViewType =
  | { kind: "chat" }
  | { kind: "milestone" }
  | { kind: "history" }
  | { kind: "settings" }
  | { kind: "assets" }
  | { kind: "review" };
```

From src/lib/slash-commands.ts:
```typescript
export function getAllCommands(): SlashCommand[];
// Returns array of GSD + Claude Code + custom commands
```

From src/hooks/usePreview.ts (pattern reference):
```typescript
// Pure function extracted for testability — same pattern to follow
export function shouldTogglePreview(e: KeyboardEvent): boolean {
  return (e.ctrlKey || e.metaKey) && e.key === "p";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — keyboard-accessibility.test.ts</name>
  <files>packages/mission-control/tests/keyboard-accessibility.test.ts</files>
  <behavior>
    - shouldOpenCommandPalette returns true for { ctrlKey: true, shiftKey: true, key: "P" }
    - shouldOpenCommandPalette returns true for { metaKey: true, shiftKey: true, key: "P" }
    - shouldOpenCommandPalette returns false for { ctrlKey: true, shiftKey: false, key: "p" } (Ctrl+P without Shift)
    - shouldOpenCommandPalette returns false for { ctrlKey: true, shiftKey: true, key: "p" } (lowercase p — wrong case)
    - shouldSwitchPanel returns "1" for { ctrlKey: true, shiftKey: false, key: "1" }
    - shouldSwitchPanel returns "5" for { ctrlKey: true, shiftKey: false, key: "5" }
    - shouldSwitchPanel returns null for { ctrlKey: true, shiftKey: true, key: "1" } (Ctrl+Shift+1 is not a panel shortcut)
    - shouldSwitchPanel returns null for { ctrlKey: false, key: "1" } (no modifier)
    - getAllCommands() returns non-empty array (command registry is available)
    - CommandPalette module exports a function (smoke check that component file is importable)
    - usePanelFocus module exports shouldSwitchPanel (smoke check)
    - Stub test for KEYS-03 (heading hierarchy — marked todo)
    - Stub test for KEYS-04 (aria-label — marked todo)
    - Stub test for KEYS-05 (touch targets — marked todo)
  </behavior>
  <action>
    Create `packages/mission-control/tests/keyboard-accessibility.test.ts`.

    Follow the pattern from `usePreview.test.ts`: import pure functions directly (no React renderer, no happy-dom mounting). Use plain objects cast to KeyboardEvent for event stubs — exactly as established for shouldTogglePreview tests.

    Structure with describe blocks:
    - describe("KEYS-01: shouldOpenCommandPalette") — 4 tests
    - describe("KEYS-02: shouldSwitchPanel") — 4 tests
    - describe("KEYS-01: command registry") — 1 test (getAllCommands non-empty)
    - describe("KEYS-01: CommandPalette module") — 1 test (typeof export === "function")
    - describe("KEYS-06: usePanelFocus module") — 1 test (shouldSwitchPanel importable)
    - describe("KEYS-03: heading hierarchy") — 1 stub test marked todo
    - describe("KEYS-04: aria-label presence") — 1 stub test marked todo
    - describe("KEYS-05: touch targets") — 1 stub test marked todo

    The stubs for KEYS-03/04/05 should use test.todo("check heading hierarchy") pattern.

    These tests MUST FAIL initially (Wave 0 — the hooks do not exist yet). After Task 2 and 3 implement the hooks, these tests will pass.

    NOTE: Do NOT import CommandPalette.tsx in this test file yet — cmdk may not be installed. Import only the pure function hooks. Add a placeholder comment for the CommandPalette import: "// import { CommandPalette } from '../src/components/command-palette/CommandPalette' — added in plan 03"
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/keyboard-accessibility.test.ts 2>&1 | head -40</automated>
  </verify>
  <done>Test file exists. Most tests fail with "Cannot find module" or similar — that is expected at Wave 0. The test structure is correct and matches the behavior spec above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useCommandPalette hook with shouldOpenCommandPalette pure function</name>
  <files>packages/mission-control/src/hooks/useCommandPalette.ts</files>
  <behavior>
    - shouldOpenCommandPalette({ ctrlKey: true, shiftKey: true, key: "P" }) === true
    - shouldOpenCommandPalette({ metaKey: true, shiftKey: true, key: "P" }) === true
    - shouldOpenCommandPalette({ ctrlKey: true, shiftKey: false, key: "p" }) === false
    - shouldOpenCommandPalette({ ctrlKey: true, shiftKey: true, key: "p" }) === false
  </behavior>
  <action>
    Create `packages/mission-control/src/hooks/useCommandPalette.ts`.

    Export:
    1. `shouldOpenCommandPalette(e: KeyboardEvent): boolean` — pure function, exported for direct testing. Check `(e.ctrlKey || e.metaKey) && e.shiftKey && e.key === "P"`. Key is uppercase "P" because Shift is held — this is the inverse of shouldTogglePreview which uses lowercase "p". The project decision log documents this exact pitfall.

    2. `useCommandPalette(): { open: boolean; setOpen: (v: boolean) => void }` — React hook that attaches a keydown listener to window, calls shouldOpenCommandPalette, toggles open state, and cleans up on unmount.

    Do NOT import cmdk in this file — the hook only manages open state. The CommandPalette component (plan 03) imports cmdk.

    Follow the exact pattern from usePreview.ts:
    - Export pure predicate function first
    - Hook uses useState + useEffect + addEventListener/removeEventListener
    - e.preventDefault() when shortcut fires (prevents browser default for Ctrl+Shift+P)
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/keyboard-accessibility.test.ts --filter "shouldOpenCommandPalette" 2>&1</automated>
  </verify>
  <done>shouldOpenCommandPalette tests pass (4 tests green). useCommandPalette exports confirmed. TypeScript compiles without errors (bun test does type checking).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: usePanelFocus hook with shouldSwitchPanel pure function</name>
  <files>packages/mission-control/src/hooks/usePanelFocus.ts</files>
  <behavior>
    - shouldSwitchPanel({ ctrlKey: true, shiftKey: false, key: "1" }) === "1"
    - shouldSwitchPanel({ ctrlKey: true, shiftKey: false, key: "5" }) === "5"
    - shouldSwitchPanel({ ctrlKey: true, shiftKey: true, key: "1" }) === null (Shift held = not a panel shortcut)
    - shouldSwitchPanel({ ctrlKey: false, key: "1" }) === null (no Ctrl)
  </behavior>
  <action>
    Create `packages/mission-control/src/hooks/usePanelFocus.ts`.

    Export:
    1. `VIEW_SHORTCUTS: Record<string, ViewType["kind"]>` — mapping "1"→"chat", "2"→"milestone", "3"→"history", "4"→"settings", "5"→"assets". Do not include "review" — it activates automatically via useChatMode, not via keyboard shortcut.

    2. `shouldSwitchPanel(e: KeyboardEvent): string | null` — returns the key string ("1"-"5") if Ctrl/Cmd is held WITHOUT Shift and the key is in VIEW_SHORTCUTS, null otherwise. Shift must NOT be held (distinguishes from Ctrl+Shift+P palette shortcut).

    3. `usePanelFocus(onSwitchView: (kind: ViewType["kind"]) => void): { headingRef: React.RefObject<HTMLHeadingElement | null> }` — React hook that:
       - Attaches keydown listener to window for panel switching (calls shouldSwitchPanel, calls onSwitchView with the ViewType kind)
       - Returns headingRef (useRef<HTMLHeadingElement | null>(null)) for the caller to attach to the view's h1
       - In a useEffect watching onSwitchView's identity, calls headingRef.current?.focus() after view changes — NOTE: this effect runs on mount too, which is acceptable

    Import ViewType from "@/lib/view-types".

    Do NOT use @/ alias if it causes issues — use relative paths: "../../lib/view-types" pattern is safer given bun test resolution.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/keyboard-accessibility.test.ts --filter "shouldSwitchPanel" 2>&1</automated>
  </verify>
  <done>shouldSwitchPanel tests pass (4 tests green). VIEW_SHORTCUTS and usePanelFocus exports confirmed. Full keyboard-accessibility test suite: KEYS-01 and KEYS-02 pure function tests pass; KEYS-03/04/05 are todo stubs (expected); CommandPalette and usePanelFocus module smoke tests pass.</done>
</task>

</tasks>

<verification>
cd packages/mission-control && bun test tests/keyboard-accessibility.test.ts
Expected: KEYS-01 pure function tests (4) green, KEYS-02 pure function tests (4) green, module smoke tests green, KEYS-03/04/05 todo stubs (shown as todo, not failure), no TypeScript errors.
</verification>

<success_criteria>
- tests/keyboard-accessibility.test.ts exists with all KEYS-01 through KEYS-06 covered (pure function tests passing, structural stubs as todo)
- useCommandPalette.ts exports shouldOpenCommandPalette (pure) and useCommandPalette (hook)
- usePanelFocus.ts exports shouldSwitchPanel (pure), VIEW_SHORTCUTS, and usePanelFocus (hook)
- bun test tests/keyboard-accessibility.test.ts shows no red failures (only todo items)
- All pure function tests pass: shouldOpenCommandPalette (4 cases) + shouldSwitchPanel (4 cases)
</success_criteria>

<output>
After completion, create `.planning/phases/10-keyboard-shortcuts-accessibility/10-01-SUMMARY.md`
</output>
