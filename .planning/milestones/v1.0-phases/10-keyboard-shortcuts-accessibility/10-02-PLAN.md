---
phase: 10-keyboard-shortcuts-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/components/views/ChatView.tsx
  - packages/mission-control/src/components/views/MilestoneView.tsx
  - packages/mission-control/src/components/views/HistoryView.tsx
  - packages/mission-control/src/components/views/SettingsView.tsx
  - packages/mission-control/src/components/views/AssetsView.tsx
  - packages/mission-control/src/components/views/ReviewView.tsx
  - packages/mission-control/src/components/views/VerifyView.tsx
  - packages/mission-control/src/components/views/ActivityView.tsx
  - packages/mission-control/src/components/views/SliceView.tsx
  - packages/mission-control/src/components/session/OnboardingScreen.tsx
  - packages/mission-control/src/components/layout/Sidebar.tsx
  - packages/mission-control/src/components/layout/SingleColumnView.tsx
  - packages/mission-control/src/components/shared/NavItems.tsx
  - packages/mission-control/src/components/session/ResumeCard.tsx
autonomous: true
requirements:
  - KEYS-03
  - KEYS-04
  - KEYS-05
must_haves:
  truths:
    - "Every view has exactly one h1 element when rendered"
    - "OnboardingScreen has only one h1 in the DOM at any given time"
    - "All icon-only buttons have aria-label attributes"
    - "All compact interactive elements have min-h-[44px] min-w-[44px]"
    - "Heading hierarchy cascades h1 (view) -> h2 (panel section) -> h3 (sub-section)"
  artifacts:
    - path: "packages/mission-control/src/components/views/ChatView.tsx"
      provides: "sr-only h1 for Chat view"
      contains: "sr-only"
    - path: "packages/mission-control/src/components/views/MilestoneView.tsx"
      provides: "h1 for Milestone view (promoted from h2)"
      contains: "<h1"
    - path: "packages/mission-control/src/components/session/OnboardingScreen.tsx"
      provides: "Single h1 per conditional branch"
      contains: "exactly one h1"
    - path: "packages/mission-control/src/components/layout/Sidebar.tsx"
      provides: "aria-label on all icon-only buttons + 44px touch targets"
      contains: "aria-label"
  key_links:
    - from: "src/components/views/MilestoneView.tsx"
      to: "src/components/milestone/MilestoneHeader.tsx"
      via: "heading cascade: MilestoneView h1 -> MilestoneHeader h2"
      pattern: "<h1"
    - from: "src/components/layout/Sidebar.tsx"
      to: "icon buttons"
      via: "aria-label attribute on collapse toggle and gear icon"
      pattern: "aria-label"
---

<objective>
Fix heading hierarchy across all views (KEYS-03), add aria-label to all icon-only buttons (KEYS-04), and enforce 44px minimum touch targets on all compact interactive elements (KEYS-05).

Purpose: This is a pure markup/attribute audit pass — no new components, no new libraries. All changes are surgical attribute additions and tag-level promotions to existing markup.
Output: Every rendered view has exactly one h1; icon-only buttons have aria-label; compact buttons have min-h-[44px] min-w-[44px].
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-keyboard-shortcuts-accessibility/10-RESEARCH.md

@packages/mission-control/src/components/views/ChatView.tsx
@packages/mission-control/src/components/views/MilestoneView.tsx
@packages/mission-control/src/components/views/HistoryView.tsx
@packages/mission-control/src/components/views/SettingsView.tsx
@packages/mission-control/src/components/views/AssetsView.tsx
@packages/mission-control/src/components/views/ReviewView.tsx
@packages/mission-control/src/components/views/VerifyView.tsx
@packages/mission-control/src/components/views/ActivityView.tsx
@packages/mission-control/src/components/session/OnboardingScreen.tsx
@packages/mission-control/src/components/layout/Sidebar.tsx
@packages/mission-control/src/components/layout/SingleColumnView.tsx

<interfaces>
<!-- Heading hierarchy audit results from codebase. -->

Current heading state (from grep audit):
- ChatView: NO heading element at all — needs sr-only h1
- MilestoneView: uses h2 as primary title — promote to h1
- HistoryView: uses h2 as primary title — promote to h1
- SettingsView: h2 "Settings" at line 229 — promote to h1
- AssetsView: TWO h2 elements (line 78 sub-heading, line 224 "Assets" title) — promote line 224 to h1
- ReviewView: h2 "UI Review Results" at line 155 — promote to h1
- VerifyView: h2 as primary title — promote to h1
- ActivityView: check for h2 primary title — promote if present
- SliceView: check for h2 primary title — promote if present
- OnboardingScreen: TWO h1 elements (lines 38 and 80) — second branch must remain h1, verify only one is in DOM at a time via exclusive conditional
- MilestoneHeader (sub-component): h2 "milestone name" — stays as h2 (correct cascade under view h1)
- PanelWrapper: h2 for panel labels — stays as h2 (correct under view h1)
- CommittedHistory: h3 — stays as h3

Cascade rule: view h1 → panel section h2 → sub-section h3. Do NOT change h3 sub-components.

Icon-only button gaps (from research audit):
- Sidebar collapse toggle: has title= but NOT aria-label — add aria-label="Collapse sidebar"
- Session tab close buttons (×): may lack aria-label — add aria-label="Close session"
- Settings gear icon: has title="Settings" — add aria-label="Settings"
- NavItems icon buttons: audit for missing aria-label
- SingleColumnView: any icon-only buttons — audit

Touch target gaps (research audit):
- Sidebar collapse toggle: compact p-2 button — add min-h-[44px] min-w-[44px]
- Session tab close buttons: compact — add min-h-[44px] min-w-[44px]
- NavItems items: ensure h-11 (44px) or min-h-[44px]
- Preview close button: already has aria-label — check touch target
- Viewport switcher buttons in PreviewPanel: check height
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Heading hierarchy — promote primary h2 to h1 in all views, fix OnboardingScreen double-h1</name>
  <files>
    packages/mission-control/src/components/views/ChatView.tsx,
    packages/mission-control/src/components/views/MilestoneView.tsx,
    packages/mission-control/src/components/views/HistoryView.tsx,
    packages/mission-control/src/components/views/SettingsView.tsx,
    packages/mission-control/src/components/views/AssetsView.tsx,
    packages/mission-control/src/components/views/ReviewView.tsx,
    packages/mission-control/src/components/views/VerifyView.tsx,
    packages/mission-control/src/components/views/ActivityView.tsx,
    packages/mission-control/src/components/views/SliceView.tsx,
    packages/mission-control/src/components/session/OnboardingScreen.tsx
  </files>
  <action>
    Make the following targeted changes:

    **ChatView.tsx:** Add a visually-hidden h1 as the first element inside the view's root div:
    ```tsx
    <h1 className="sr-only" tabIndex={-1}>GSD Mission Control — Chat</h1>
    ```
    Place it before any existing children. Do not add tabIndex={-1} if it causes issues — sr-only with no tabIndex is also acceptable.

    **MilestoneView.tsx:** Find the primary view title element (currently an h2 with "Milestone" or similar). Change the tag from `<h2` to `<h1`. Preserve all existing className attributes exactly.

    **HistoryView.tsx:** Same promotion — find primary h2 title, change to h1.

    **SettingsView.tsx:** Line 229 has `<h2 className="text-lg font-display text-slate-200">Settings</h2>` — change to h1.

    **AssetsView.tsx:** Line 224 has `<h2 className="text-lg font-display text-slate-200 flex-1">Assets</h2>` — change to h1. The h2 at line 78 is a sub-section label — leave it as h2 (it is a panel section header, correct under the view h1).

    **ReviewView.tsx:** Line 155 has `<h2 className="text-lg font-display text-slate-200 flex-1">UI Review Results</h2>` — change to h1.

    **VerifyView.tsx:** Find primary h2 title, change to h1.

    **ActivityView.tsx:** Read the file first. If it has an h2 primary view title, promote to h1. If it has no heading, add `<h1 className="sr-only">Activity</h1>`.

    **SliceView.tsx:** Read the file first. Same approach as ActivityView.

    **OnboardingScreen.tsx:** The file has TWO h1 elements on lines 38 and 78 (each in a different conditional branch). Verify the conditional is truly exclusive (only one branch renders at a time). If the structure is `if (condition) { return <div><h1>A</h1>...</div> } return <div><h1>B</h1>...</div>`, this is already correct — document this in a comment. If both h1s can be in the DOM simultaneously due to CSS-based hiding (display:none), convert one to h2. Read the file and make the right call.

    Do NOT change:
    - MilestoneHeader h2 (sub-component, correct cascade)
    - PanelWrapper h2 (panel labels, correct cascade)
    - CommittedHistory h3 (sub-section, correct)
    - ResumeCard h3 (sub-component, correct)
    - Any h3 sub-components

    After changes, run: `cd packages/mission-control && bun build src/components/views/ChatView.tsx --target=browser 2>&1 | head -20` to verify TypeScript compiles.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/ --filter "layout" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Every view file (ChatView, MilestoneView, HistoryView, SettingsView, AssetsView, ReviewView, VerifyView, ActivityView, SliceView) has exactly one h1 element (verified by grep)
    - OnboardingScreen conditional structure ensures only one h1 in DOM at a time
    - All pre-existing tests still pass (no layout regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: ARIA names on icon-only buttons + 44px touch targets across Sidebar and compact controls</name>
  <files>
    packages/mission-control/src/components/layout/Sidebar.tsx,
    packages/mission-control/src/components/layout/SingleColumnView.tsx,
    packages/mission-control/src/components/shared/NavItems.tsx,
    packages/mission-control/src/components/session/ResumeCard.tsx
  </files>
  <action>
    Read each file before editing. Apply these targeted changes:

    **Sidebar.tsx:**
    - Find the collapse/expand toggle button. Add `aria-label="Collapse sidebar"` (or "Expand sidebar" if already collapsed — use a dynamic value based on the collapsed state prop/state). Also add `min-h-[44px] min-w-[44px] flex items-center justify-center` to its className.
    - Find the settings gear icon button. If it has `title="Settings"` but no `aria-label`, add `aria-label="Settings"`.
    - Find any other icon-only buttons. Add appropriate `aria-label` to each.
    - For all compact buttons (p-2 or smaller on an icon), add `min-h-[44px] min-w-[44px]` to ensure 44px touch target. Use `flex items-center justify-center` if needed to center the icon within the larger tap area.

    **SingleColumnView.tsx:**
    - Read the file. Identify any icon-only buttons (close buttons, toggle buttons).
    - Add `aria-label` to any that lack it.
    - Add `min-h-[44px] min-w-[44px]` to any compact buttons.

    **NavItems.tsx (or ProjectTree.tsx wherever nav items live):**
    - Read the file. If nav items are `<button>` or `<div role="button">` with only an icon (no text label), add `aria-label` matching the nav item name.
    - If nav items already contain visible text, aria-label is not needed.
    - Check height: nav item rows should have at least `min-h-[44px]` or `h-11` (h-11 = 44px in Tailwind).

    **Session tab controls (if in SingleColumnView or a dedicated component):**
    - Find close (×) buttons on session tabs. Add `aria-label="Close session"` and `min-h-[44px] min-w-[44px]`.

    **Pattern for all fixes:**
    ```tsx
    // Before (icon-only, missing aria-label, undersized)
    <button className="rounded p-2 text-slate-400 hover:text-slate-200">
      <XIcon className="h-4 w-4" />
    </button>

    // After (accessible, 44px target)
    <button
      aria-label="Close session"
      title="Close session"
      className="flex min-h-[44px] min-w-[44px] items-center justify-center rounded p-2 text-slate-400 hover:text-slate-200"
    >
      <XIcon className="h-4 w-4" />
    </button>
    ```

    Do NOT add aria-label to buttons that already have visible text — that would be redundant. Only icon-only buttons need it.

    After editing, verify with: `grep -rn "aria-label" packages/mission-control/src/components/layout/Sidebar.tsx`
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/sidebar.test.tsx tests/layout.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All icon-only buttons in Sidebar.tsx, SingleColumnView.tsx, and NavItems.tsx have aria-label attributes
    - All compact buttons (icon-only, p-2 or smaller) have min-h-[44px] min-w-[44px] in className
    - Existing sidebar and layout tests still pass (no regressions)
    - grep confirms aria-label present on collapse toggle and settings gear
  </done>
</task>

</tasks>

<verification>
cd packages/mission-control && bun test tests/ 2>&1 | tail -30

Expected: All pre-existing tests pass. No TypeScript errors. Grep checks:
- `grep -rn "<h1" packages/mission-control/src/components/views/` — one h1 per view file
- `grep -rn "aria-label" packages/mission-control/src/components/layout/Sidebar.tsx` — at least 2 results (collapse toggle + gear)
- `grep -rn "min-h-\[44px\]" packages/mission-control/src/components/layout/Sidebar.tsx` — at least 1 result
</verification>

<success_criteria>
- Each view component file (ChatView, MilestoneView, HistoryView, SettingsView, AssetsView, ReviewView, VerifyView, ActivityView, SliceView) contains exactly one h1 element
- OnboardingScreen has two h1 elements but only one is in DOM at a time (exclusive conditional)
- All icon-only buttons in Sidebar, SingleColumnView, NavItems have aria-label attributes
- All compact icon buttons have min-h-[44px] min-w-[44px] in className
- All pre-existing bun tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-keyboard-shortcuts-accessibility/10-02-SUMMARY.md`
</output>
