---
phase: 06.2-live-streaming-native-os
plan: 03
type: execute
wave: 3
depends_on: ["06.2-01", "06.2-02"]
files_modified:
  - packages/mission-control/src/components/views/ActivityView.tsx
  - packages/mission-control/src/components/views/VerifyView.tsx
  - packages/mission-control/src/components/views/HistoryView.tsx
  - packages/mission-control/src/components/modals/PermissionModal.tsx
  - packages/mission-control/src/hooks/useChat.ts
  - packages/mission-control/src/hooks/useActivity.ts
  - packages/mission-control/src/components/layout/SingleColumnView.tsx
  - packages/mission-control/src/components/layout/AppShell.tsx
  - packages/mission-control/tests/activity-view.test.tsx
  - packages/mission-control/tests/history-view.test.ts
autonomous: true
requirements: [LS-05, LS-06, LS-07, LS-08, LS-09]

must_haves:
  truths:
    - "Activity view shows live Claude Code stream events (tool calls, text, thinking indicators)"
    - "History view shows GSD checkpoint commits from git log"
    - "Verify view sends /gsd:verify-work command and shows results"
    - "Permission modal appears as blocking overlay when permission_prompt event received"
    - "Client clears chat messages and activity feed on project_switched event"
  artifacts:
    - path: "packages/mission-control/src/components/views/ActivityView.tsx"
      provides: "Live stream activity feed showing tool calls, file writes, thinking"
      exports: ["ActivityView"]
    - path: "packages/mission-control/src/components/views/HistoryView.tsx"
      provides: "GSD checkpoint commit history"
      exports: ["HistoryView"]
    - path: "packages/mission-control/src/components/views/VerifyView.tsx"
      provides: "Verify work trigger and results display"
      exports: ["VerifyView"]
    - path: "packages/mission-control/src/components/modals/PermissionModal.tsx"
      provides: "Blocking permission prompt modal with Approve/Always Allow/Deny"
      exports: ["PermissionModal"]
    - path: "packages/mission-control/src/hooks/useActivity.ts"
      provides: "Activity state from stream events"
      exports: ["useActivity"]
  key_links:
    - from: "packages/mission-control/src/hooks/useChat.ts"
      to: "packages/mission-control/src/components/modals/PermissionModal.tsx"
      via: "permission_prompt event triggers modal state"
      pattern: "permission_prompt"
    - from: "packages/mission-control/src/hooks/useChat.ts"
      to: "client state clear"
      via: "project_switched event clears messages"
      pattern: "project_switched"
    - from: "packages/mission-control/src/components/views/HistoryView.tsx"
      to: "/api/git/log"
      via: "fetch GET for commit history"
      pattern: "api/git/log"
    - from: "packages/mission-control/src/components/views/VerifyView.tsx"
      to: "useChat.sendMessage"
      via: "sends /gsd:verify-work command"
      pattern: "gsd:verify-work"
---

<objective>
Build functional view components (Activity, History, Verify), permission modal, and enhance useChat for project switch state clearing.

Purpose: Replace the stub placeholders from Plan 02 with real view components. Add the permission prompt modal UI. Handle client-side state reset on project switch. This completes all LS-* requirements.

Output: Fully functional views accessible from sidebar, permission modal ready for use, clean project switch behavior.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.2-live-streaming-native-os/06.2-RESEARCH.md
@.planning/phases/06.2-live-streaming-native-os/06.2-01-SUMMARY.md
@.planning/phases/06.2-live-streaming-native-os/06.2-02-SUMMARY.md

<interfaces>
<!-- Types from Plans 01 and 02 -->

From src/server/chat-types.ts (enhanced by Plan 01):
```typescript
export type ChatEventType = "system" | "assistant" | "result" | "stream_event" | "permission_prompt";
export interface StreamEvent {
  type: ChatEventType;
  event?: { type: string; index?: number; delta?: { type: string; text?: string }; content_block?: { type: string; name?: string; id?: string } };
  parent_tool_use_id?: string | null;
  uuid?: string; session_id?: string; result?: string; subtype?: string;
}
export interface PermissionPromptEvent { type: "permission_prompt"; toolName: string; toolInput: string; promptId: string; }
export interface PermissionResponse { type: "permission_response"; promptId: string; action: "approve" | "always_allow" | "deny"; }
```

From src/lib/view-types.ts (created by Plan 02):
```typescript
export type ViewType =
  | { kind: "chat" }
  | { kind: "activity" }
  | { kind: "milestone"; milestoneId?: string }
  | { kind: "slice"; phaseId: string }
  | { kind: "history" }
  | { kind: "verify" };
```

From src/components/layout/SingleColumnView.tsx (created by Plan 02):
```typescript
// Renders view based on activeView.kind
// Needs updating to replace stubs with real ActivityView, VerifyView, HistoryView
```

From src/hooks/useChat.ts (CURRENT):
```typescript
export interface UseChatResult { messages: ChatMessage[]; sendMessage: (text: string) => void; isProcessing: boolean; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityView + HistoryView + VerifyView components + useActivity hook</name>
  <files>
    packages/mission-control/src/components/views/ActivityView.tsx,
    packages/mission-control/src/components/views/HistoryView.tsx,
    packages/mission-control/src/components/views/VerifyView.tsx,
    packages/mission-control/src/hooks/useActivity.ts,
    packages/mission-control/src/components/layout/SingleColumnView.tsx,
    packages/mission-control/tests/activity-view.test.tsx,
    packages/mission-control/tests/history-view.test.ts
  </files>
  <action>
    1. Create src/hooks/useActivity.ts (NEW):
       - Maintains a list of recent stream activity events (max 100, FIFO)
       - Interface: ActivityItem = { id: string; type: "tool_use" | "text" | "thinking" | "result"; name?: string; content?: string; timestamp: number; isSubAgent: boolean }
       - `export function useActivity()` returning { activities: ActivityItem[]; addEvent: (event: StreamEvent) => void; clear: () => void }
       - addEvent processes a StreamEvent:
         - content_block_start with tool_use type -> ActivityItem with type "tool_use", name from content_block.name
         - content_block_delta with text -> ActivityItem with type "text" (accumulate into last text item if consecutive)
         - isSubAgent = parent_tool_use_id is non-null

    2. Create src/components/views/ActivityView.tsx (NEW):
       - Props: { activities: ActivityItem[] }
       - Renders a scrollable list of activity items
       - Each item shows: icon (Wrench for tool_use, Type for text, Brain for thinking, CheckCircle for result), name/content preview, timestamp (relative: "2s ago"), sub-agent badge if isSubAgent
       - Use design tokens: bg-navy-base, text-slate-300, border-navy-600
       - Empty state: "No activity yet. Send a message to Claude Code to see live activity."
       - Auto-scrolls to bottom as new items arrive (useRef + scrollIntoView)

    3. Create src/components/views/HistoryView.tsx (NEW):
       - Props: none (self-contained, fetches from API)
       - On mount, fetch GET /api/git/log?limit=50
       - Renders list of commits: hash (monospace, cyan), subject, date (relative), author
       - Each commit row: border-b border-navy-600, p-2 spacing
       - Loading state: skeleton shimmer rows
       - Empty state: "No GSD history found for this project."
       - Error state: "Failed to load history: {error}"

    4. Create src/components/views/VerifyView.tsx (NEW):
       - Props: { onSendCommand: (cmd: string) => void }
       - Shows a "Run Verification" button (cyan accent) that calls onSendCommand("/gsd:verify-work")
       - Below button: instructions text "Click to run /gsd:verify-work against the current phase. Results will appear in the Chat view."
       - After clicking, button shows "Verification sent..." disabled state for 3 seconds
       - Future: could show verification results inline, but for v1 just sends the command

    5. Update src/components/layout/SingleColumnView.tsx:
       - Replace the three placeholder divs for "activity", "verify", "history" with actual components
       - Import and render ActivityView, VerifyView, HistoryView
       - Pass activities prop to ActivityView (from useActivity in AppShell)
       - Pass onSendCommand to VerifyView (from onChatSend)

    6. Write tests:
       - tests/activity-view.test.tsx: Test useActivity hook adds events correctly, respects max 100, marks sub-agents, clear works
       - tests/history-view.test.ts: Test HistoryView fetches from /api/git/log, renders commit rows, handles empty state
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --filter "activity" && bun test --filter "history"</automated>
  </verify>
  <done>
    - ActivityView renders live stream events with tool_use/text/thinking indicators
    - HistoryView fetches and displays GSD checkpoint commits from git log API
    - VerifyView sends /gsd:verify-work command on button click
    - useActivity hook processes StreamEvents into ActivityItems
    - SingleColumnView renders real components instead of placeholders
  </done>
</task>

<task type="auto">
  <name>Task 2: Permission modal + useChat enhancement for project_switched and permission events + AppShell wiring</name>
  <files>
    packages/mission-control/src/components/modals/PermissionModal.tsx,
    packages/mission-control/src/hooks/useChat.ts,
    packages/mission-control/src/components/layout/AppShell.tsx
  </files>
  <action>
    1. Create src/components/modals/PermissionModal.tsx (NEW):
       - Props: { prompt: PermissionPromptEvent | null; onRespond: (action: "approve" | "always_allow" | "deny") => void }
       - When prompt is null, render nothing
       - When prompt is set, render a blocking modal overlay:
         - Fixed position, full screen, bg-black/60 backdrop
         - Centered modal card: bg-navy-900, border border-navy-600, rounded-lg, p-6
         - Title: "Permission Required" (font-display, text-lg)
         - Tool name: {prompt.toolName} in cyan monospace
         - Tool input preview: {prompt.toolInput} truncated to 200 chars, in monospace, bg-navy-800 p-2
         - Three buttons in a row:
           - "Approve" (bg-green-600 hover:bg-green-500)
           - "Always Allow" (bg-cyan-accent/20 hover:bg-cyan-accent/30 text-cyan-accent)
           - "Deny" (bg-red-600/20 hover:bg-red-600/30 text-red-400)
         - Each button calls onRespond with its action
       - Per user decision: modal only appears when --dangerously-skip-permissions is DISABLED

    2. Enhance src/hooks/useChat.ts:
       - Add permission prompt state: const [permissionPrompt, setPermissionPrompt] = useState<PermissionPromptEvent | null>(null)
       - In handleMessage, add handler for msgType === "permission_prompt":
         - Set permissionPrompt to the event data
       - Add respondToPermission function: sends { type: "permission_response", promptId, action } over WebSocket, then clears permissionPrompt state
       - In handleMessage, add handler for msgType === "project_switched":
         - Clear messages: setMessages([])
         - Clear isProcessing: setIsProcessing(false)
         - This ensures stale state from previous project is gone
       - Add to UseChatResult: permissionPrompt: PermissionPromptEvent | null, respondToPermission: (action: "approve" | "always_allow" | "deny") => void, clearMessages: () => void
       - Feed stream events to useActivity by accepting an optional onStreamEvent callback prop or by exposing events
       - Actually, simpler approach: have useChat call an optional onActivityEvent callback when it processes stream events. AppShell passes this callback to feed useActivity.

    3. Update src/components/layout/AppShell.tsx:
       - Import PermissionModal and useActivity
       - Extract permissionPrompt and respondToPermission from useChat
       - Create useActivity instance, pass addEvent as onActivityEvent to useChat
       - Pass activities to SingleColumnView (which passes to ActivityView)
       - Render PermissionModal at root level (outside SingleColumnView, so it overlays everything)
       - Pass clearMessages from useChat, wire to project switch handler to also clear activity
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test</automated>
  </verify>
  <done>
    - Permission modal renders with Approve/Always Allow/Deny when permission_prompt event received
    - Modal sends permission_response back over WebSocket
    - Client clears chat messages and activity on project_switched event
    - useChat exposes permissionPrompt state and respondToPermission callback
    - Activity feed receives stream events from useChat pipeline
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
- `cd packages/mission-control && bun test` -- all tests pass
- `grep "ActivityView" packages/mission-control/src/components/views/ActivityView.tsx` -- component exists
- `grep "HistoryView" packages/mission-control/src/components/views/HistoryView.tsx` -- component exists
- `grep "VerifyView" packages/mission-control/src/components/views/VerifyView.tsx` -- component exists
- `grep "PermissionModal" packages/mission-control/src/components/modals/PermissionModal.tsx` -- component exists
- `grep "project_switched" packages/mission-control/src/hooks/useChat.ts` -- handled in client
- `grep "permission_prompt" packages/mission-control/src/hooks/useChat.ts` -- handled in client
</verification>

<success_criteria>
All three sidebar views (Activity, History, Verify) are functional. Permission modal renders correctly. Client handles project_switched event by clearing state. All LS-* requirements are met.
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-live-streaming-native-os/06.2-03-SUMMARY.md`
</output>
