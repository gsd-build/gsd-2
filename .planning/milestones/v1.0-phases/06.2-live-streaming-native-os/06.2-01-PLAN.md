---
phase: 06.2-live-streaming-native-os
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/server/chat-types.ts
  - packages/mission-control/src/server/claude-process.ts
  - packages/mission-control/src/server/dialog-api.ts
  - packages/mission-control/src/server/git-api.ts
  - packages/mission-control/src/server/pipeline.ts
  - packages/mission-control/src/server/ws-server.ts
  - packages/mission-control/src/server/index.ts
  - packages/mission-control/tests/stream-events.test.ts
  - packages/mission-control/tests/dialog-api.test.ts
  - packages/mission-control/tests/permissions-flag.test.ts
autonomous: true
requirements: [LS-01, LS-02, LS-03, LS-04, LS-05, LS-09]

must_haves:
  truths:
    - "Claude Code process spawns with --dangerously-skip-permissions by default"
    - "All stream event types (tool_use, text, thinking, result) reach the WebSocket"
    - "Sub-agent events are tagged with parent_tool_use_id and forwarded"
    - "Native OS file dialog opens via POST /api/dialog/open-folder"
    - "Project switch broadcasts project_switched event to all clients"
    - "Permission prompt events forward to clients when skip-permissions is disabled"
  artifacts:
    - path: "packages/mission-control/src/server/chat-types.ts"
      provides: "Enhanced StreamEvent with parent_tool_use_id, permission_prompt type"
      exports: ["ChatEventType", "StreamEvent", "ChatMessage", "ChatRequest", "ChatResponse", "PermissionPromptEvent", "PermissionResponse"]
    - path: "packages/mission-control/src/server/dialog-api.ts"
      provides: "Native OS folder dialog spawning"
      exports: ["handleDialogRequest"]
    - path: "packages/mission-control/src/server/git-api.ts"
      provides: "Git log API for history view"
      exports: ["handleGitRequest"]
    - path: "packages/mission-control/src/server/claude-process.ts"
      provides: "Enhanced process manager with skip-permissions flag"
      exports: ["ClaudeProcessManager"]
  key_links:
    - from: "packages/mission-control/src/server/claude-process.ts"
      to: "packages/mission-control/src/server/chat-types.ts"
      via: "StreamEvent with parent_tool_use_id"
      pattern: "parent_tool_use_id"
    - from: "packages/mission-control/src/server/pipeline.ts"
      to: "packages/mission-control/src/server/ws-server.ts"
      via: "publishChat for project_switched event"
      pattern: "project_switched"
    - from: "packages/mission-control/src/server/dialog-api.ts"
      to: "powershell|zenity spawn"
      via: "node:child_process.spawn"
      pattern: "nodeSpawn.*powershell|zenity"
---

<objective>
Server-side streaming infrastructure, native dialog API, and git history API for Phase 6.2.

Purpose: Build all server-side capabilities that the UI plans will consume -- enhanced streaming types with sub-agent support, --dangerously-skip-permissions flag, native OS folder dialog endpoint, git log endpoint, and project_switched WebSocket broadcast.

Output: Enhanced server modules ready for UI consumption in Plans 02 and 03.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-live-streaming-native-os/06.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/server/chat-types.ts (CURRENT -- to be enhanced):
```typescript
export type ChatEventType = "system" | "assistant" | "result" | "stream_event";
export interface StreamEvent {
  type: ChatEventType;
  event?: { type: string; index?: number; delta?: { type: string; text?: string; partial_json?: string }; content_block?: { type: string; name?: string }; };
  session_id?: string; result?: string; subtype?: string;
}
export interface ChatResponse {
  type: "chat_event" | "chat_complete" | "chat_error";
  event?: StreamEvent; sessionId?: string; error?: string;
}
```

From src/server/claude-process.ts (CURRENT -- to be enhanced):
```typescript
export interface ClaudeProcessOptions { allowedTools?: string[]; model?: string; }
export class ClaudeProcessManager {
  constructor(cwd: string, options?: ClaudeProcessOptions);
  onEvent(handler: (event: StreamEvent) => void): void;
  async sendMessage(prompt: string): Promise<void>;
  async kill(): Promise<void>;
}
```

From src/server/ws-server.ts (CURRENT):
```typescript
export interface WsServer {
  broadcast(diff: StateDiff): void;
  sendToClient(ws: ServerWebSocket, data: unknown): void;
  publishChat(data: unknown): void;
  stop(): void;
  getSequence(): number;
}
```

From src/server/pipeline.ts (CURRENT):
```typescript
export interface PipelineHandle {
  stop(): void;
  processManager: ClaudeProcessManager;
  switchProject(newPlanningDir: string): Promise<void>;
}
```

From src/server/fs-api.ts (route dispatcher pattern to follow):
```typescript
export async function handleFsRequest(req: Request, url: URL): Promise<Response | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhanced streaming types + ClaudeProcessManager with --dangerously-skip-permissions and sub-agent awareness</name>
  <files>
    packages/mission-control/src/server/chat-types.ts,
    packages/mission-control/src/server/claude-process.ts,
    packages/mission-control/tests/stream-events.test.ts,
    packages/mission-control/tests/permissions-flag.test.ts
  </files>
  <behavior>
    - Test: ChatEventType includes "permission_prompt" type
    - Test: StreamEvent has optional parent_tool_use_id field (string | null | undefined)
    - Test: StreamEvent has optional uuid field
    - Test: PermissionPromptEvent interface has toolName, toolInput, promptId fields
    - Test: PermissionResponse interface has promptId and action (approve|always_allow|deny) fields
    - Test: ChatResponse type union includes "permission_prompt" type
    - Test: ClaudeProcessOptions has optional skipPermissions field (default undefined = skip enabled)
    - Test: sendMessage() includes --dangerously-skip-permissions in args when skipPermissions is not explicitly false
    - Test: sendMessage() omits --dangerously-skip-permissions when skipPermissions is false
    - Test: Events with parent_tool_use_id are forwarded to handlers with the field intact
  </behavior>
  <action>
    1. Enhance chat-types.ts:
       - Add "permission_prompt" to ChatEventType union
       - Add parent_tool_use_id?: string | null to StreamEvent
       - Add uuid?: string to StreamEvent
       - Add content_block id?: string to the event.content_block type
       - Add PermissionPromptEvent interface: { type: "permission_prompt"; toolName: string; toolInput: string; promptId: string }
       - Add PermissionResponse interface: { type: "permission_response"; promptId: string; action: "approve" | "always_allow" | "deny" }
       - Add "permission_prompt" to ChatResponse type union
       - Keep all existing types backward-compatible

    2. Enhance claude-process.ts:
       - Add skipPermissions?: boolean to ClaudeProcessOptions (undefined = skip enabled, per user decision: default ON)
       - In sendMessage(), add --dangerously-skip-permissions to args array UNLESS this.options.skipPermissions === false
       - Preserve parent_tool_use_id field when parsing events (it flows through NDJSON parser naturally since parser emits raw JSON)
       - No other changes needed -- existing NDJSON parser already handles all event types

    3. Write tests:
       - tests/stream-events.test.ts: Verify enhanced type shapes compile and are usable. Test that processStreamEvent (from useChat) handles events with parent_tool_use_id without breaking.
       - tests/permissions-flag.test.ts: Mock nodeSpawn and verify --dangerously-skip-permissions is in args by default, and absent when skipPermissions: false.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --filter "stream-events" && bun test --filter "permissions-flag"</automated>
  </verify>
  <done>
    - ChatEventType includes permission_prompt
    - StreamEvent has parent_tool_use_id and uuid fields
    - PermissionPromptEvent and PermissionResponse types exported
    - ClaudeProcessManager spawns with --dangerously-skip-permissions by default
    - All existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Native dialog API + git log API + pipeline project_switched broadcast + WebSocket permission routing</name>
  <files>
    packages/mission-control/src/server/dialog-api.ts,
    packages/mission-control/src/server/git-api.ts,
    packages/mission-control/src/server/pipeline.ts,
    packages/mission-control/src/server/ws-server.ts,
    packages/mission-control/src/server/index.ts,
    packages/mission-control/tests/dialog-api.test.ts
  </files>
  <behavior>
    - Test: POST /api/dialog/open-folder spawns PowerShell on Windows with FolderBrowserDialog script
    - Test: POST /api/dialog/open-folder spawns zenity on Linux with --file-selection --directory
    - Test: Dialog returns { path: "selected/path" } on success, { cancelled: true } on cancel
    - Test: GET /api/git/log returns array of GSD checkpoint commits (filtered by commit message pattern)
    - Test: GET /api/git/log accepts optional ?limit=N parameter
    - Test: Pipeline switchProject broadcasts project_switched event via publishChat before full state broadcast
    - Test: WebSocket message handler recognizes permission_response messages and forwards to pipeline
  </behavior>
  <action>
    1. Create dialog-api.ts (NEW):
       - Follow route dispatcher pattern from fs-api.ts: `export async function handleDialogRequest(req: Request, url: URL): Promise<Response | null>`
       - POST /api/dialog/open-folder: spawn PowerShell (Windows) or zenity (Linux) to show native folder dialog
       - Use process.platform to detect OS. Windows: `powershell -NoProfile -Command "Add-Type -AssemblyName System.Windows.Forms; $dlg = New-Object System.Windows.Forms.FolderBrowserDialog; $dlg.Description = 'Select Project Folder'; $dlg.ShowNewFolderButton = $true; if ($dlg.ShowDialog() -eq 'OK') { $dlg.SelectedPath } else { '' }"`
       - Linux: `zenity --file-selection --directory --title="Select Project Folder"`
       - Return Response.json({ path: selectedPath.replace(/\\/g, "/") }) on success
       - Return Response.json({ cancelled: true }) when path is empty or process exits non-zero
       - IMPORTANT: The dialog process blocks its own thread but nodeSpawn runs in separate process, so the Bun event loop is NOT blocked. However, the HTTP response should only resolve when dialog closes (await the promise).

    2. Create git-api.ts (NEW):
       - `export async function handleGitRequest(req: Request, url: URL, repoRoot: string): Promise<Response | null>`
       - GET /api/git/log: spawn `git log --oneline --format={"hash":"%h","subject":"%s","date":"%ci","author":"%an"} --grep="^docs\\|^feat\\|^fix\\|^refactor" -n {limit}` in repoRoot
       - Parse NDJSON output into array of commit objects
       - Filter to GSD-relevant commits (commit messages matching gsd-tools commit patterns: docs(), feat(), fix(), refactor())
       - Default limit=50, accept ?limit=N query param
       - Return Response.json({ commits: [...] })

    3. Enhance pipeline.ts switchProject():
       - After state rebuild, BEFORE the full state broadcast, publish a project_switched event:
         `wsServer.publishChat({ type: "project_switched", path: newPlanningDir, timestamp: Date.now() })`
       - This lets clients clear their local state before receiving new full state

    4. Enhance ws-server.ts:
       - In the message handler, add handling for { type: "permission_response" } messages
       - Forward to a new optional callback: `onPermissionResponse?: (response: PermissionResponse, ws: ServerWebSocket) => void`
       - Add onPermissionResponse to WsServerOptions interface

    5. Wire into index.ts (server entry):
       - Import handleDialogRequest and handleGitRequest
       - Add to the fetch handler chain alongside existing handleFsRequest
       - Pass repoRoot to handleGitRequest

    6. Write tests:
       - tests/dialog-api.test.ts: Mock nodeSpawn, test Windows/Linux platform branching, test success/cancel responses
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --filter "dialog-api" && bun test --filter "pipeline-switch"</automated>
  </verify>
  <done>
    - POST /api/dialog/open-folder endpoint exists and spawns platform-appropriate dialog
    - GET /api/git/log endpoint returns filtered commit history
    - Pipeline broadcasts project_switched before full state on switch
    - WebSocket handles permission_response messages
    - All existing tests still pass (cd packages/mission-control && bun test)
  </done>
</task>

</tasks>

<verification>
- `cd packages/mission-control && bun test` -- all tests pass including new ones
- `grep "dangerously-skip-permissions" packages/mission-control/src/server/claude-process.ts` -- flag present
- `grep "parent_tool_use_id" packages/mission-control/src/server/chat-types.ts` -- field present
- `grep "permission_prompt" packages/mission-control/src/server/chat-types.ts` -- type present
- `grep "project_switched" packages/mission-control/src/server/pipeline.ts` -- broadcast present
- `grep "handleDialogRequest" packages/mission-control/src/server/dialog-api.ts` -- endpoint present
- `grep "handleGitRequest" packages/mission-control/src/server/git-api.ts` -- endpoint present
</verification>

<success_criteria>
All server-side streaming, dialog, git, and permission infrastructure is in place. Enhanced types are exported. New API endpoints respond. Pipeline broadcasts project_switched. No regressions in existing test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-live-streaming-native-os/06.2-01-SUMMARY.md`
</output>
