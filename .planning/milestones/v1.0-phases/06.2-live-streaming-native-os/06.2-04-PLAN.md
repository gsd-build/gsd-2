---
phase: 06.2-live-streaming-native-os
plan: 04
type: execute
wave: 4
depends_on: ["06.2-03"]
files_modified: []
autonomous: false
requirements: [LS-01, LS-04, LS-05, LS-06, LS-07, LS-08]

must_haves:
  truths:
    - "Dashboard renders with sidebar-driven single-column layout"
    - "Open Folder triggers native OS dialog"
    - "Sidebar tree items switch the right panel view"
    - "Chat messages stream in real-time from Claude Code"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 6.2 implementation.

Purpose: Verify that the live streaming architecture, native OS dialog, sidebar restructure, and single-column view all work correctly in the running application.

Output: User confirmation that Phase 6.2 meets requirements.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.2-live-streaming-native-os/06.2-01-SUMMARY.md
@.planning/phases/06.2-live-streaming-native-os/06.2-02-SUMMARY.md
@.planning/phases/06.2-live-streaming-native-os/06.2-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of Phase 6.2</name>
  <files></files>
  <action>
    Present the running application to the user for verification of all Phase 6.2 features.
    Start dev server with `cd packages/mission-control && bun run dev` if not already running.
  </action>
  <verify>User confirms all features work</verify>
  <done>User types "approved" after verifying all Phase 6.2 functionality</done>
  <what-built>
    Complete Phase 6.2: Live Streaming Architecture + Native OS Integration
    - Enhanced Claude Code streaming with sub-agent event tagging and --dangerously-skip-permissions flag
    - Native OS file dialog (PowerShell on Windows) replacing in-panel file browser
    - Sidebar restructured into VS Code-style project tree (Chat, Activity, Milestones, Slices, History, Verify)
    - Single-column view replacing the old 3-tab layout
    - Activity view showing live stream events
    - History view showing GSD checkpoint commits
    - Verify view sending /gsd:verify-work command
    - Permission modal for when skip-permissions is disabled
    - Client-side state clearing on project switch
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd packages/mission-control && bun run dev`
    2. Open http://localhost:4000 in browser

    **Sidebar Layout:**
    3. Verify sidebar shows "New Window" and "Open Folder" buttons at top
    4. Verify project tree shows expandable items: Chat, Activity, Milestones, Slices (with phases), History, Verify
    5. Click each tree item and verify the right panel switches to the corresponding single-column view

    **Native Dialog:**
    6. Click "Open Folder" button
    7. Verify a native Windows folder dialog appears (not an in-panel browser)
    8. Select a folder with a .planning/ directory and verify project switches

    **Chat Streaming:**
    9. Type a message in the Chat view and send
    10. Verify streaming response renders token-by-token
    11. Switch to Activity view and verify tool calls and text events appear

    **History:**
    12. Click History in sidebar
    13. Verify GSD checkpoint commits are listed with hash, subject, date

    **Verify:**
    14. Click Verify in sidebar
    15. Verify "Run Verification" button is visible

    **New Window:**
    16. Click "New Window" button
    17. Verify a new browser tab opens with the same dashboard
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of end-to-end functionality.
</verification>

<success_criteria>
User confirms all Phase 6.2 features work correctly in the running application.
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-live-streaming-native-os/06.2-04-SUMMARY.md`
</output>
