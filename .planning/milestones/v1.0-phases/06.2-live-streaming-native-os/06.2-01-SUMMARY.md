---
phase: 06.2-live-streaming-native-os
plan: 01
subsystem: api
tags: [streaming, websocket, ndjson, child_process, powershell, zenity, git-log, permissions]

requires:
  - phase: 06-chat-and-slash
    provides: "Claude process manager, NDJSON parser, WebSocket server, pipeline orchestrator"
  - phase: 06.1-native-fs
    provides: "fs-api route dispatcher pattern, project switch, switch guard"
provides:
  - "Enhanced StreamEvent with parent_tool_use_id and uuid for sub-agent tracking"
  - "PermissionPromptEvent and PermissionResponse types for permission UI flow"
  - "--dangerously-skip-permissions flag on by default in ClaudeProcessManager"
  - "POST /api/dialog/open-folder endpoint for native OS folder dialog"
  - "GET /api/git/log endpoint for filtered commit history"
  - "project_switched WebSocket broadcast before full state on switch"
  - "permission_response WebSocket message handling"
affects: [06.2-02, 06.2-03, 06.2-04]

tech-stack:
  added: []
  patterns: ["injectable spawn for testability (_spawnFn, _setSpawnFn)", "route dispatcher pattern for new API modules"]

key-files:
  created:
    - "packages/mission-control/src/server/dialog-api.ts"
    - "packages/mission-control/src/server/git-api.ts"
    - "packages/mission-control/tests/stream-events.test.ts"
    - "packages/mission-control/tests/permissions-flag.test.ts"
    - "packages/mission-control/tests/dialog-api.test.ts"
  modified:
    - "packages/mission-control/src/server/chat-types.ts"
    - "packages/mission-control/src/server/claude-process.ts"
    - "packages/mission-control/src/server/pipeline.ts"
    - "packages/mission-control/src/server/ws-server.ts"
    - "packages/mission-control/src/server.ts"

key-decisions:
  - "Injectable spawn via _spawnFn property (not constructor DI) -- minimal API surface change"
  - "Skip-permissions defaults ON (undefined = true) per user decision from Phase 6.2 planning"
  - "Git log filters to conventional commit patterns (docs/feat/fix/refactor/test/chore)"
  - "project_switched broadcast uses publishChat (chat topic) not broadcast (state topic)"

patterns-established:
  - "_setSpawnFn test helper for dialog-api spawn injection"
  - "_spawnFn instance property for process manager spawn injection"

requirements-completed: [LS-01, LS-02, LS-03, LS-04, LS-05, LS-09]

duration: 7min
completed: 2026-03-10
---

# Phase 6.2 Plan 01: Server Streaming Infrastructure Summary

**Enhanced streaming types with sub-agent awareness, --dangerously-skip-permissions by default, native OS folder dialog, git log API, project_switched broadcast, and WebSocket permission routing**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-10T20:31:14Z
- **Completed:** 2026-03-10T20:38:19Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- Enhanced ChatEventType and StreamEvent with permission_prompt, parent_tool_use_id, and uuid fields
- ClaudeProcessManager spawns with --dangerously-skip-permissions by default, configurable via skipPermissions option
- Native OS folder dialog API (PowerShell on Windows, zenity on Linux)
- Git log API returning GSD-relevant commits filtered by conventional commit patterns
- Pipeline broadcasts project_switched event before full state on project switch
- WebSocket message handler routes permission_response to new callback

## Task Commits

Each task was committed atomically:

1. **Task 1: Enhanced streaming types + skip-permissions flag** - `cb4c2a3` (feat)
2. **Task 2: Native dialog API, git log API, project_switched broadcast, permission routing** - `a2448e7` (feat)

## Files Created/Modified
- `packages/mission-control/src/server/chat-types.ts` - Added permission_prompt type, parent_tool_use_id, uuid, PermissionPromptEvent, PermissionResponse
- `packages/mission-control/src/server/claude-process.ts` - Added skipPermissions option, --dangerously-skip-permissions flag, _spawnFn injection
- `packages/mission-control/src/server/dialog-api.ts` - NEW: Native OS folder dialog endpoint
- `packages/mission-control/src/server/git-api.ts` - NEW: Git log endpoint with commit pattern filtering
- `packages/mission-control/src/server/pipeline.ts` - project_switched broadcast before full state
- `packages/mission-control/src/server/ws-server.ts` - permission_response message routing
- `packages/mission-control/src/server.ts` - Wired dialog and git routes into fetch handler
- `packages/mission-control/tests/stream-events.test.ts` - NEW: 12 tests for enhanced types
- `packages/mission-control/tests/permissions-flag.test.ts` - NEW: 4 tests for skip-permissions flag
- `packages/mission-control/tests/dialog-api.test.ts` - NEW: 8 tests for dialog and git APIs

## Decisions Made
- Injectable spawn via `_spawnFn` property rather than constructor DI -- minimal public API change while enabling full test isolation
- Skip-permissions defaults ON (undefined = true) per user decision from Phase 6.2 planning
- Git log filters to conventional commit patterns (docs/feat/fix/refactor/test/chore) rather than all commits
- project_switched uses publishChat (chat topic) to reach all clients before state rebuild

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Injectable spawn for test isolation**
- **Found during:** Task 1 (permissions flag tests)
- **Issue:** Bun ESM imports cannot be monkey-patched via require.cache; spawn mock tests failed
- **Fix:** Added `_spawnFn` injectable property to ClaudeProcessManager and `_setSpawnFn` helper to dialog-api
- **Files modified:** claude-process.ts, dialog-api.ts
- **Verification:** All 4 permissions-flag tests pass, all 8 dialog-api tests pass
- **Committed in:** cb4c2a3 and a2448e7

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary for test isolation in Bun's ESM environment. No scope creep.

## Issues Encountered
- 3 pre-existing test failures in chat-router.test.ts (isActive, concurrent spawn, kill no-op) -- out of scope, not caused by this plan's changes

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All server-side streaming, dialog, git, and permission infrastructure is in place
- Enhanced types exported and ready for UI consumption in Plans 02 and 03
- 270 tests passing (same 3 pre-existing failures unrelated to this plan)

---
*Phase: 06.2-live-streaming-native-os*
*Completed: 2026-03-10*
