---
phase: 02-file-to-state-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/server/types.ts
  - packages/mission-control/src/server/watcher.ts
  - packages/mission-control/src/server/state-deriver.ts
  - packages/mission-control/tests/watcher.test.ts
  - packages/mission-control/tests/state-deriver.test.ts
autonomous: true
requirements: [SERV-02, SERV-03, SERV-09]

must_haves:
  truths:
    - "File watcher detects new, modified, and deleted files in .planning/ recursively"
    - "State deriver parses STATE.md frontmatter, ROADMAP.md phases, config.json, PLAN.md files, and REQUIREMENTS.md into typed PlanningState"
    - "buildFullState() reconstructs identical state from .planning/ files after process restart"
    - "Partial writes and editor temp files do not produce parse errors"
  artifacts:
    - path: "packages/mission-control/src/server/types.ts"
      provides: "Shared types for PlanningState, StateDiff, PhaseState, PlanState, etc."
      exports: ["PlanningState", "ProjectState", "RoadmapState", "PhaseState", "PlanState", "ConfigState", "RequirementState", "StateDiff", "WatcherOptions"]
    - path: "packages/mission-control/src/server/watcher.ts"
      provides: "Debounced recursive file watcher for .planning/"
      exports: ["createFileWatcher"]
    - path: "packages/mission-control/src/server/state-deriver.ts"
      provides: "Parses .planning/ files into PlanningState"
      exports: ["buildFullState", "parseRoadmap", "parseRequirements"]
  key_links:
    - from: "packages/mission-control/src/server/watcher.ts"
      to: "fs.watch"
      via: "node:fs watch with recursive: true"
      pattern: "watch\\(.*recursive.*true"
    - from: "packages/mission-control/src/server/state-deriver.ts"
      to: "gray-matter"
      via: "import matter from gray-matter"
      pattern: "matter\\("
---

<objective>
Create the shared type system, debounced file watcher, and state derivation engine for the file-to-state pipeline.

Purpose: This is the data foundation for all dashboard panels. The watcher detects .planning/ file changes, and the state deriver parses those files into typed JSON state objects. Together they satisfy SERV-02 (file watching), SERV-03 (state derivation), and SERV-09 (state reconstruction on restart).

Output: Three source files (types.ts, watcher.ts, state-deriver.ts) and two test files.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-to-state-pipeline/02-RESEARCH.md

Key context from Phase 1:
- Bun v1.3.10 is the runtime (not Node.js)
- Server entry is packages/mission-control/src/server.ts on port 4000
- Tests use bun:test (not node:test) for mission-control package
- Phase 1 tests are in packages/mission-control/tests/ using describe/test/expect pattern
- Use Bun.file().text() and Bun.file().json() for file reads (async, not readFileSync)

Key decisions from research:
- Use gray-matter for YAML frontmatter parsing (install: bun add gray-matter in packages/mission-control)
- Use Bun fs.watch({ recursive: true }) with 50ms debounce -- NOT chokidar
- Ignore dotfiles (except .planning), temp files (~, .swp), and .mission-control-session.json
- Full state rebuild on every change (not incremental) since .planning/ has ~25 files and parse takes <10ms
- ROADMAP.md checkbox list (- [x] / - [ ]) is source of truth for phase completion (not progress table)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared types and debounced file watcher</name>
  <files>packages/mission-control/src/server/types.ts, packages/mission-control/src/server/watcher.ts, packages/mission-control/tests/watcher.test.ts</files>
  <behavior>
    - Test: watcher calls onChange with set of changed filenames after debounce window
    - Test: watcher ignores temp files (.swp, ~), dotfiles (except .planning paths), and .mission-control-session.json
    - Test: watcher coalesces multiple rapid events into single onChange call (debounce)
    - Test: watcher.close() stops watching without errors
  </behavior>
  <action>
1. Create `packages/mission-control/src/server/types.ts` with all shared types:
   - `PlanningState` (top-level: roadmap, state, config, phases, requirements)
   - `ProjectState` (from STATE.md frontmatter: milestone, status, progress with total_phases/completed_phases/percent, last_updated, last_activity)
   - `RoadmapState` with `RoadmapPhase` (completed, number, name, description)
   - `PhaseState` (number, name, status enum, plans array)
   - `PlanState` (phase, plan, wave, requirements, autonomous, type, files_modified, depends_on)
   - `ConfigState` (model_profile, model_overrides -- match .planning/config.json shape)
   - `RequirementState` (id, description, phase, completed)
   - `StateDiff` (type: "full" | "diff", changes: Partial<PlanningState>, timestamp, sequence)
   - `WatcherOptions` (planningDir, debounceMs?, onChange callback)

2. Create `packages/mission-control/src/server/watcher.ts`:
   - Export `createFileWatcher(options: WatcherOptions)` function
   - Use `import { watch } from "node:fs"` with `{ recursive: true }`
   - 50ms default debounce: collect filenames in a Set, fire onChange after quiet period
   - Filter out: filenames ending in `~` or `.swp`, filenames that are exactly `.mission-control-session.json`, filenames starting with `.` that are not under `.planning` path
   - Return object with `close()` method that calls `watcher.close()`
   - Wrap the entire watch callback in try/catch to prevent crashes on weird filesystem events

3. Install gray-matter: run `cd packages/mission-control && bun add gray-matter` (needed by Task 2 but install now to avoid lock file conflicts)
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/watcher.test.ts --timeout 15000</automated>
  </verify>
  <done>File watcher detects file changes in a temp directory with debounce, ignores temp/swap files, and tests pass. gray-matter is installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create state derivation engine with full state rebuild</name>
  <files>packages/mission-control/src/server/state-deriver.ts, packages/mission-control/tests/state-deriver.test.ts</files>
  <behavior>
    - Test: buildFullState parses STATE.md frontmatter into ProjectState with correct milestone, status, progress fields
    - Test: buildFullState parses ROADMAP.md checkbox list into RoadmapPhase array with correct completed/number/name/description
    - Test: buildFullState parses config.json into ConfigState
    - Test: buildFullState parses PLAN.md files with YAML frontmatter into PlanState array grouped by phase
    - Test: buildFullState parses REQUIREMENTS.md checkbox list into RequirementState array with completed status
    - Test: buildFullState handles missing files gracefully (returns empty/default values, does not throw)
    - Test: buildFullState produces identical output when called twice with same files (SERV-09 -- restart reconstruction)
  </behavior>
  <action>
1. Create `packages/mission-control/src/server/state-deriver.ts`:
   - Import `matter` from `gray-matter` and types from `./types`
   - Export `buildFullState(planningDir: string): Promise<PlanningState>`:
     - Use `Promise.all` with `Bun.file().text()` to read STATE.md, ROADMAP.md concurrently
     - Use `Bun.file().json()` for config.json (wrap in try/catch, default to empty object)
     - Parse STATE.md with `matter()` to extract frontmatter data as ProjectState
     - Parse ROADMAP.md with `parseRoadmap()` (regex on `- [(x| )] **Phase (\d+): (.+?)** - (.+)`)
     - Parse REQUIREMENTS.md with `parseRequirements()` (regex on `- [(x| )] **([A-Z]+-\d+)**: (.+)`)
     - Glob for `phases/*/XX-YY-PLAN.md` files using `Bun.Glob` or `fs.readdirSync` + filter
     - Parse each PLAN.md with `matter()` to extract PlanState frontmatter
     - Group plans into PhaseState objects by phase number
     - Return complete PlanningState

   - Export `parseRoadmap(content: string): RoadmapState`
   - Export `parseRequirements(content: string): RequirementState[]`

2. For file discovery of PLAN.md files, use `fs.readdirSync` with recursive option to find files matching pattern `*-PLAN.md` in the phases directory. This avoids importing glob libraries.

3. Handle missing files: wrap each file read in try/catch. If STATE.md is missing, return default ProjectState. If ROADMAP.md is missing, return empty phases array. If config.json is missing, return default ConfigState. This ensures SERV-09 (process restart) works even if .planning/ is partially populated.

4. All file reads MUST use async Bun.file() API (not readFileSync) to avoid blocking the event loop (per research pitfall 4).
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/state-deriver.test.ts --timeout 15000</automated>
  </verify>
  <done>buildFullState correctly parses all .planning/ file types into typed PlanningState. Missing files return defaults without throwing. Calling buildFullState twice on same files produces identical output (SERV-09).</done>
</task>

</tasks>

<verification>
cd packages/mission-control && bun test tests/watcher.test.ts tests/state-deriver.test.ts --timeout 15000
</verification>

<success_criteria>
- types.ts exports all shared types needed by watcher, deriver, differ, and ws-server
- createFileWatcher detects recursive file changes with 50ms debounce
- buildFullState parses STATE.md, ROADMAP.md, config.json, PLAN.md files, REQUIREMENTS.md
- Missing files produce defaults, not crashes
- gray-matter is installed as dependency
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-to-state-pipeline/02-01-SUMMARY.md`
</output>
