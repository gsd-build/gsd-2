---
phase: 06.1-native-file-system-integration
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - packages/mission-control/src/server/pipeline.ts
  - packages/mission-control/src/server.ts
  - packages/mission-control/tests/pipeline-switch.test.ts
  - packages/mission-control/src/hooks/useProjectBrowser.ts
  - packages/mission-control/src/components/sidebar/ProjectBrowser.tsx
  - packages/mission-control/src/components/sidebar/FileTreeNode.tsx
  - packages/mission-control/src/components/sidebar/ProjectList.tsx
  - packages/mission-control/src/components/layout/Sidebar.tsx
autonomous: false
requirements: [FS-04, FS-06, FS-07]

must_haves:
  truths:
    - "User can click 'Open Folder' in sidebar to browse local directories"
    - "Directory tree expands lazily on click, loading children from the FS API"
    - "Directories containing .planning/ show a GSD badge"
    - "Selecting a project directory triggers pipeline hot-switch and UI state refresh"
    - "Recent projects appear in the sidebar for quick re-opening"
    - "File tree shows folder/file icons from lucide-react with proper indentation"
  artifacts:
    - path: "packages/mission-control/src/server/pipeline.ts"
      provides: "switchProject method on PipelineHandle"
      exports: ["PipelineHandle"]
    - path: "packages/mission-control/tests/pipeline-switch.test.ts"
      provides: "Unit tests for pipeline switchProject behavior"
    - path: "packages/mission-control/src/hooks/useProjectBrowser.ts"
      provides: "useProjectBrowser hook for FS API interaction"
      exports: ["useProjectBrowser"]
    - path: "packages/mission-control/src/components/sidebar/ProjectBrowser.tsx"
      provides: "Main file tree browser panel"
      exports: ["ProjectBrowser"]
    - path: "packages/mission-control/src/components/sidebar/FileTreeNode.tsx"
      provides: "Recursive tree node component"
      exports: ["FileTreeNode"]
    - path: "packages/mission-control/src/components/sidebar/ProjectList.tsx"
      provides: "Updated project list with recent projects and Open Folder button"
      exports: ["ProjectList"]
  key_links:
    - from: "packages/mission-control/src/components/sidebar/ProjectBrowser.tsx"
      to: "/api/fs/list"
      via: "useProjectBrowser hook fetch calls"
      pattern: "fetch.*api/fs/list"
    - from: "packages/mission-control/src/components/sidebar/ProjectBrowser.tsx"
      to: "/api/project/switch"
      via: "openProject POST request"
      pattern: "fetch.*api/project/switch"
    - from: "packages/mission-control/src/server.ts"
      to: "packages/mission-control/src/server/pipeline.ts"
      via: "pipeline.switchProject called from /api/project/switch handler"
      pattern: "pipeline\\.switchProject"
---

<objective>
Add pipeline hot-switch capability and build the interactive file tree browser UI in the sidebar. Users can browse directories, see GSD project badges, select a project to switch to, and view recent projects.

Purpose: Completes the native file system integration by connecting the server FS API (Plan 01) to an interactive sidebar UI and enabling project switching without server restart.
Output: Working project browser in sidebar, pipeline hot-switch, recent projects list.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-native-file-system-integration-project-panel-works-like-vs-code-to-navigate-create-and-open-working-directories-to-start-claude-code-gsd-sessions/06.1-01-SUMMARY.md
@packages/mission-control/src/server/pipeline.ts
@packages/mission-control/src/server.ts
@packages/mission-control/src/components/layout/Sidebar.tsx
@packages/mission-control/src/components/sidebar/ProjectList.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From packages/mission-control/src/server/fs-types.ts:
```typescript
export interface FileSystemEntry {
  name: string;
  path: string;       // normalized forward-slash
  isDirectory: boolean;
  isGsdProject: boolean;
}

export interface RecentProject {
  path: string;
  name: string;
  lastOpened: number;
  isGsdProject: boolean;
}
```

From packages/mission-control/src/server/fs-api.ts:
```typescript
export function listDirectory(dirPath: string): Promise<FileSystemEntry[]>;
export function detectProject(dirPath: string): Promise<{ isGsdProject: boolean; path: string }>;
export function handleFsRequest(req: Request, url: URL): Promise<Response | null>;
```

From packages/mission-control/src/server/recent-projects.ts:
```typescript
export function getRecentProjects(): Promise<RecentProject[]>;
export function addRecentProject(project: RecentProject): Promise<void>;
```

From packages/mission-control/src/server/pipeline.ts:
```typescript
export interface PipelineHandle {
  stop(): void;
  processManager: ClaudeProcessManager;
}
```

From packages/mission-control/src/components/layout/Sidebar.tsx:
```typescript
interface SidebarProps {
  collapsed: boolean;
  onToggle: () => void;
  connectionStatus: ConnectionStatusType;
  projectState: ProjectState | null;
  configState: ConfigState | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline hot-switch and project switch API endpoint</name>
  <files>
    packages/mission-control/src/server/pipeline.ts,
    packages/mission-control/src/server.ts,
    packages/mission-control/tests/pipeline-switch.test.ts
  </files>
  <behavior>
    - switchProject(newPlanningDir) stops the existing watcher and process manager
    - switchProject(newPlanningDir) rebuilds state from the new planning directory
    - switchProject rejects with error if called while processManager.isProcessing is true
    - switchProject rejects concurrent switch attempts (switching flag guard)
    - POST /api/project/switch with valid GSD path returns { switched: true, path }
    - POST /api/project/switch with non-existent path returns 400
    - POST /api/project/switch records the project in recent projects
  </behavior>
  <action>
    1. Create `tests/pipeline-switch.test.ts` FIRST (TDD red phase) using bun:test:
       - Test switchProject rejects when isProcessing is true (mock processManager with isProcessing: true)
       - Test switchProject rejects concurrent calls (call twice simultaneously, second should reject)
       - Test switchProject calls stop/kill on existing resources (mock watcher.close, processManager.kill)
       - Test the /api/project/switch route handler logic: valid path returns 200, missing path returns 400
       - Use mock/stub approach: create a minimal mock of PipelineHandle internals to test switchProject logic without starting real processes. If the pipeline internals are too coupled for direct unit testing, extract the switchProject guard logic (isProcessing check, switching flag) into a testable helper function.

    2. Extend PipelineHandle interface in pipeline.ts to add:
       ```typescript
       switchProject(newPlanningDir: string): Promise<void>;
       ```

    3. Implement switchProject in startPipeline's return object:
       - Close the existing file watcher (watcher.close())
       - Kill existing Claude process (await processManager.kill())
       - Resolve new repoRoot from newPlanningDir (resolve(newPlanningDir, ".."))
       - Rebuild full state from new planning dir (currentState = await buildFullState(newPlanningDir))
       - Create new ClaudeProcessManager with new repoRoot
       - Re-wire processManager.onEvent to forward events to activeClient (same pattern as initial setup)
       - Start new Claude process (processManager.start())
       - Re-discover custom commands for new repo root
       - Create new file watcher on newPlanningDir (same onChange callback but referencing new planningDir)
       - Broadcast full state to all clients: wsServer.broadcast({ type: "full", changes: currentState, timestamp: Date.now(), sequence: ++seq })
       - Update the return object's processManager reference
       - Log: console.log("[pipeline] Switched to project:", newPlanningDir)

    4. Add guard: if processManager.isProcessing, throw new Error("Cannot switch projects while Claude is processing")

    5. In server.ts, add a POST /api/project/switch route in the fetch handler:
       - Parse JSON body: { path: string }
       - Validate path exists and has .planning/ subdirectory
       - Call pipeline.switchProject(resolve(path, ".planning"))
       - Call addRecentProject with the project info
       - Return Response.json({ switched: true, path })
       - On error, return Response.json({ error: message }, { status: 400 or 500 })

    6. Export `pipeline` from the module-level scope in server.ts so it can be referenced in the fetch handler (it's already module-level, just ensure it's accessible in the fetch closure).

    IMPORTANT: The pipeline variable is created with `await startPipeline(...)` at module top level. The fetch handler is a closure that can already access it. No structural changes needed beyond adding the /api/project/switch route matching.

    Race condition prevention: The switchProject method should set a `switching` flag at the start and clear it at the end. Reject concurrent switch attempts.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/pipeline-switch.test.ts</automated>
  </verify>
  <done>
    - PipelineHandle has switchProject method
    - POST /api/project/switch triggers full pipeline reconfiguration
    - Guard prevents switching while Claude is processing
    - Recent project is recorded on switch
    - Full state broadcast sent to all WebSocket clients after switch
    - Pipeline switch tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Project browser UI components and sidebar integration</name>
  <files>
    packages/mission-control/src/hooks/useProjectBrowser.ts,
    packages/mission-control/src/components/sidebar/FileTreeNode.tsx,
    packages/mission-control/src/components/sidebar/ProjectBrowser.tsx,
    packages/mission-control/src/components/sidebar/ProjectList.tsx,
    packages/mission-control/src/components/layout/Sidebar.tsx
  </files>
  <action>
    1. Create `useProjectBrowser.ts` hook:
       - State: currentPath (string | null), entries (FileSystemEntry[]), loading (boolean), error (string | null), recentProjects (RecentProject[])
       - `browse(path: string)` — fetches GET /api/fs/list?path=encodeURIComponent(path), sets entries + currentPath
       - `openProject(path: string)` — POSTs to /api/project/switch with { path }, refreshes recent projects on success
       - `loadRecentProjects()` — fetches GET /api/projects/recent
       - `goUp()` — navigates to parent directory (path.split("/").slice(0, -1).join("/") or similar)
       - On mount (useEffect), call loadRecentProjects()
       - Import FileSystemEntry and RecentProject types from "@/server/fs-types"

    2. Create `FileTreeNode.tsx` — recursive tree node component:
       - Props: entry (FileSystemEntry), depth (number), onSelect (path: string) => void
       - State: expanded (boolean), children (FileSystemEntry[] | null), loading (boolean)
       - On click of a directory: toggle expanded. If expanding and children is null, fetch /api/fs/list?path=entry.path, set children
       - On click of non-directory: no-op (files are informational only in the tree)
       - Render: button with paddingLeft={depth * 16 + 8}px for indentation
       - Icons: ChevronDown/ChevronRight for expanded/collapsed dirs (lucide-react), Folder/File for type
       - If entry.isGsdProject: show small cyan "GSD" badge text next to folder name
       - Styling: w-full text-left py-1 px-2 hover:bg-navy-700 text-slate-300 text-xs font-mono, truncate on name
       - Recursively render children when expanded

    3. Create `ProjectBrowser.tsx` — main browser panel:
       - Uses useProjectBrowser hook
       - Header: breadcrumb showing currentPath segments (clickable to navigate up), "Open Folder" button to start browsing from homedir
       - Body: scrollable list of FileTreeNode components for current entries
       - Bottom action bar: when a directory is selected, show "Open Project" button (cyan accent) if it's a GSD project, or "Open Here" for non-GSD directories
       - Loading state: show skeleton/spinner while fetching
       - Error state: show error message with retry button
       - "Back" button (ArrowLeft icon) to navigate to parent directory
       - Start browsing from os.homedir() equivalent (fetched from a simple /api/fs/home endpoint, or hardcode initial browse to empty path which defaults to homedir on server)

    4. Update `ProjectList.tsx`:
       - Keep existing project status display (the current active project)
       - Add "Recent Projects" section below current project showing recent projects from useProjectBrowser
       - Each recent project: clickable row with project name, "GSD" badge if isGsdProject, click to switch
       - Add "Open Folder" button (FolderOpen icon from lucide-react) that toggles the ProjectBrowser overlay/panel
       - When ProjectBrowser is not active, show the current simple project list (existing behavior)

    5. Update `Sidebar.tsx`:
       - Add state: showBrowser (boolean) to toggle between project list and project browser views
       - Pass showBrowser toggle to ProjectList's "Open Folder" button
       - When showBrowser is true, render ProjectBrowser in the sidebar body instead of NavItems + ProjectList
       - Add a close/back button to exit the browser and return to normal sidebar view
       - ProjectBrowser receives an onProjectOpened callback that sets showBrowser=false and triggers the project switch

    Design tokens: Follow 8-point spacing grid (p-2, p-4, gap-2). Use font-mono text-xs for file names. Cyan accent for GSD badges and action buttons. Navy-700 for hover backgrounds. Use cn() from @/lib/utils for conditional classes.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun build src/components/sidebar/ProjectBrowser.tsx --no-bundle --outdir /tmp/check-browser 2>&1 | head -5 && bun test tests/pipeline-switch.test.ts tests/fs-api.test.ts tests/recent-projects.test.ts</automated>
  </verify>
  <done>
    - FileTreeNode renders recursive directory tree with expand/collapse
    - ProjectBrowser shows breadcrumb navigation and file tree
    - GSD projects highlighted with cyan badge
    - Opening a project triggers pipeline switch via API
    - Recent projects shown in sidebar for quick access
    - Sidebar toggles between normal view and browser view
    - All existing tests still pass (no regressions)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify file system integration end-to-end</name>
  <files>none</files>
  <action>Human verifies the complete native file system integration: directory browsing, GSD project detection, project switching, and recent projects.</action>
  <verify>
    1. Start the dev server: `cd packages/mission-control && bun run src/server.ts`
    2. Open http://localhost:4000 in browser
    3. In the sidebar, click "Open Folder" button
    4. Verify the file tree browser appears showing your home directory contents
    5. Click on directories to expand them — children should load lazily
    6. Navigate to a directory containing .planning/ — it should show a cyan "GSD" badge
    7. Click "Open Project" on a GSD project directory
    8. Verify the dashboard state panels update to show that project's data
    9. Close the browser view — verify recent projects section shows the project you just opened
    10. Click a recent project to switch back — verify it works without full page reload
  </verify>
  <done>Human confirms: directory browsing works, GSD badges visible, project switching updates dashboard, recent projects persist</done>
</task>

</tasks>

<verification>
- `bun test tests/fs-api.test.ts tests/recent-projects.test.ts tests/pipeline-switch.test.ts` — all unit tests pass
- Server starts, FS API endpoints respond correctly
- Project browser UI renders in sidebar with working tree navigation
- Project switching updates all dashboard panels via pipeline hot-switch
- Recent projects persist across page refreshes
</verification>

<success_criteria>
Users can browse the local file system from the sidebar, identify GSD projects, switch between projects (triggering full pipeline reconfiguration), and quickly re-open recent projects. The experience is similar to VS Code's "Open Folder" workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-native-file-system-integration-project-panel-works-like-vs-code-to-navigate-create-and-open-working-directories-to-start-claude-code-gsd-sessions/06.1-02-SUMMARY.md`
</output>
