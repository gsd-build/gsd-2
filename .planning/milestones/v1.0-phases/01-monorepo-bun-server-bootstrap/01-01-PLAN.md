---
phase: 01-monorepo-bun-server-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/mission-control/package.json
  - packages/mission-control/bunfig.toml
  - packages/mission-control/tsconfig.json
  - packages/mission-control/components.json
  - packages/mission-control/src/server.ts
  - packages/mission-control/src/frontend.tsx
  - packages/mission-control/src/App.tsx
  - packages/mission-control/src/styles/globals.css
  - packages/mission-control/src/lib/utils.ts
  - packages/mission-control/public/index.html
autonomous: true
requirements:
  - MONO-01
  - MONO-02
  - MONO-03
  - SERV-01

must_haves:
  truths:
    - "packages/mission-control/ exists as a Bun workspace with its own package.json"
    - "Root package.json has workspaces field and dev script delegating to mission-control"
    - "GSD core root package remains publishable (npm pack --dry-run shows no packages/ files)"
    - "bun run dev from repo root starts Bun server serving React app on localhost:4000"
    - "Tailwind CSS classes render correctly in the browser"
    - "shadcn/ui cn() utility and components.json are configured"
  artifacts:
    - path: "package.json"
      provides: "Workspace config and dev script"
      contains: "workspaces"
    - path: "packages/mission-control/package.json"
      provides: "Mission Control workspace package"
      contains: "@gsd/mission-control"
    - path: "packages/mission-control/src/server.ts"
      provides: "Bun HTTP server entry point"
      contains: "Bun.serve"
    - path: "packages/mission-control/src/App.tsx"
      provides: "Root React component"
      contains: "GSD Mission Control"
    - path: "packages/mission-control/src/styles/globals.css"
      provides: "Tailwind v4 CSS with theme tokens"
      contains: "@import"
    - path: "packages/mission-control/src/lib/utils.ts"
      provides: "cn() utility for shadcn/ui"
      contains: "twMerge"
  key_links:
    - from: "package.json"
      to: "packages/mission-control/package.json"
      via: "workspaces field"
      pattern: "workspaces.*packages"
    - from: "packages/mission-control/src/server.ts"
      to: "packages/mission-control/public/index.html"
      via: "HTML import"
      pattern: "import.*index\\.html"
    - from: "packages/mission-control/public/index.html"
      to: "packages/mission-control/src/frontend.tsx"
      via: "script tag"
      pattern: "src.*frontend\\.tsx"
    - from: "packages/mission-control/src/frontend.tsx"
      to: "packages/mission-control/src/App.tsx"
      via: "React root render"
      pattern: "import.*App"
---

<objective>
Create the monorepo workspace structure and Bun fullstack server serving a React 19 application with Tailwind CSS v4 and shadcn/ui on localhost:4000.

Purpose: Establish the foundation for all subsequent Mission Control development -- workspace isolation from GSD core, dev server with HMR, and the full styling stack.
Output: A working `bun run dev` command that serves a styled React page at localhost:4000.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-bun-server-bootstrap/01-RESEARCH.md

<interfaces>
<!-- Root package.json — existing fields that MUST NOT be modified -->
From package.json (root):
```json
{
  "name": "get-shit-done-cc",
  "version": "1.22.4",
  "bin": { "get-shit-done-cc": "bin/install.js" },
  "files": ["bin", "commands", "get-shit-done", "agents", "hooks/dist", "scripts"],
  "scripts": {
    "build:hooks": "node scripts/build-hooks.js",
    "prepublishOnly": "npm run build:hooks",
    "test": "node scripts/run-tests.cjs",
    "test:coverage": "c8 --check-coverage ..."
  }
}
```
Only ADD `"workspaces"` and `"dev"` script. All other fields remain untouched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo workspace structure and configuration files</name>
  <files>
    package.json,
    packages/mission-control/package.json,
    packages/mission-control/bunfig.toml,
    packages/mission-control/tsconfig.json,
    packages/mission-control/components.json,
    packages/mission-control/src/lib/utils.ts
  </files>
  <action>
1. Read root `package.json` and ADD two fields only (preserve all existing fields exactly):
   - `"workspaces": ["packages/*"]`
   - In scripts, ADD `"dev": "bun run --cwd packages/mission-control dev"` (keep all existing scripts)

2. Create directory `packages/mission-control/` and subdirectories: `src/`, `src/styles/`, `src/components/`, `src/components/ui/`, `src/lib/`, `src/hooks/`, `public/`.

3. Create `packages/mission-control/package.json`:
```json
{
  "name": "@gsd/mission-control",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/server.ts"
  }
}
```

4. Create `packages/mission-control/bunfig.toml`:
```toml
[serve.static]
plugins = ["bun-plugin-tailwind"]
```

5. Create `packages/mission-control/tsconfig.json` with target ESNext, module ESNext, moduleResolution bundler, jsx react-jsx, jsxImportSource react, strict true, paths `"@/*": ["./src/*"]`, include ["src"]. Use exact config from RESEARCH.md.

6. Create `packages/mission-control/components.json` for shadcn/ui with rsc false, tsx true, tailwind css pointing to `src/styles/globals.css`, aliases using `@/` prefix. Use exact config from RESEARCH.md.

7. Create `packages/mission-control/src/lib/utils.ts` with the cn() utility using clsx + tailwind-merge. Use exact code from RESEARCH.md.

8. Install dependencies inside packages/mission-control:
```bash
cd packages/mission-control
bun add react@latest react-dom@latest
bun add tailwindcss bun-plugin-tailwind
bun add class-variance-authority clsx tailwind-merge tw-animate-css lucide-react
bun add -d @types/react @types/react-dom
```

9. Verify GSD core publishability: run `npm pack --dry-run` at repo root and confirm NO files from `packages/` appear in the output.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun --version && cd ../.. && npm pack --dry-run 2>&1 | grep -c "packages/" | grep -q "0"</automated>
  </verify>
  <done>
    - packages/mission-control/ exists with its own package.json naming @gsd/mission-control
    - Root package.json has "workspaces": ["packages/*"] and dev script
    - All existing root package.json fields unchanged
    - npm pack --dry-run shows zero files from packages/
    - Dependencies installed in mission-control workspace
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Bun fullstack server, React app, and Tailwind styling</name>
  <files>
    packages/mission-control/src/server.ts,
    packages/mission-control/src/frontend.tsx,
    packages/mission-control/src/App.tsx,
    packages/mission-control/src/styles/globals.css,
    packages/mission-control/public/index.html
  </files>
  <action>
1. Create `packages/mission-control/public/index.html`:
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GSD Mission Control</title>
  <link rel="stylesheet" href="../src/styles/globals.css" />
</head>
<body class="dark">
  <div id="root"></div>
  <script type="module" src="../src/frontend.tsx"></script>
</body>
</html>
```

2. Create `packages/mission-control/src/server.ts` using Bun.serve() with HTML import pattern:
   - Import homepage from "../public/index.html"
   - Serve on port 4000
   - Route "/" to homepage
   - Enable development mode with hmr: true and console: true
   - Add fetch fallback returning 404 for unmatched routes
   - Log startup URL to console
   Use exact pattern from RESEARCH.md.

3. Create `packages/mission-control/src/frontend.tsx`:
   - Import createRoot from react-dom/client
   - Import App component
   - Render App into #root element
   Use exact pattern from RESEARCH.md.

4. Create `packages/mission-control/src/styles/globals.css` with Tailwind v4 CSS-first config:
```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme {
  --color-navy-base: #0F1419;
  --color-cyan-accent: #5BC8F0;
  --font-display: "Share Tech Mono", monospace;
  --font-mono: "JetBrains Mono", monospace;
}
```

5. Create `packages/mission-control/src/App.tsx` — a minimal component proving React + Tailwind work:
   - Full viewport height, dark navy background using the theme token (bg-navy-base)
   - Centered content with flexbox
   - "GSD Mission Control" heading in cyan accent color (text-cyan-accent)
   - Subtitle "Dashboard loading..." in muted gray
   - Use Tailwind utility classes only (no inline styles)
   - This is a placeholder that Phase 3 will replace with the panel layout

6. Start the dev server from repo root with `bun run dev` and verify:
   - Server starts without errors
   - curl http://localhost:4000 returns HTML containing "root" div
   - Kill the server process after verification
  </action>
  <verify>
    <automated>cd packages/mission-control && bun run dev &amp; SERVER_PID=$! &amp;&amp; sleep 3 &amp;&amp; curl -sf http://localhost:4000 | grep -q "root" &amp;&amp; echo "PASS: Server responds with HTML" &amp;&amp; kill $SERVER_PID 2>/dev/null; exit 0</automated>
  </verify>
  <done>
    - bun run dev starts Bun server on port 4000 without errors
    - localhost:4000 returns HTML with React mount point
    - App.tsx renders with Tailwind-styled content (dark background, cyan heading)
    - Tailwind v4 CSS processes via bun-plugin-tailwind
    - HMR enabled for development
  </done>
</task>

</tasks>

<verification>
1. `bun run dev` from repo root starts server on :4000 (SERV-01)
2. `packages/mission-control/` exists as proper workspace (MONO-01)
3. Root package.json has workspace:* configuration (MONO-02)
4. `npm pack --dry-run` at root shows no packages/ files (MONO-03)
5. curl localhost:4000 returns HTML with React mount point
6. Tailwind classes render (verified visually in Plan 02)
</verification>

<success_criteria>
- `bun run dev` from repo root serves a styled React page at localhost:4000
- Monorepo workspace structure is correct with GSD core isolated
- All four phase requirements (MONO-01, MONO-02, MONO-03, SERV-01) have automated verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bun-server-bootstrap/01-01-SUMMARY.md`
</output>
