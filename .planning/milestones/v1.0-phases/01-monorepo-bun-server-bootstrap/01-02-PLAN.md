---
phase: 01-monorepo-bun-server-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/mission-control/tests/setup.test.ts
  - packages/mission-control/tests/server.test.ts
autonomous: false
requirements:
  - MONO-01
  - MONO-02
  - MONO-03
  - SERV-01

must_haves:
  truths:
    - "Automated tests prove workspace structure is correct"
    - "Automated tests prove server starts and responds on :4000"
    - "GSD core publishability is verified by test"
    - "Human confirms styled React app renders in browser at localhost:4000"
  artifacts:
    - path: "packages/mission-control/tests/setup.test.ts"
      provides: "Workspace structure validation tests"
      contains: "describe"
    - path: "packages/mission-control/tests/server.test.ts"
      provides: "Server startup and response tests"
      contains: "describe"
  key_links:
    - from: "packages/mission-control/tests/server.test.ts"
      to: "packages/mission-control/src/server.ts"
      via: "imports or spawns server"
      pattern: "localhost:4000"
---

<objective>
Write automated smoke tests validating the workspace structure and server behavior, then have the user verify the styled React app renders correctly in the browser.

Purpose: Close the Nyquist gap from VALIDATION.md -- every requirement needs an automated test. Then confirm visual correctness (Tailwind rendering, dark theme, fonts) which cannot be automated.
Output: Passing test suite and human-verified visual confirmation.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-bun-server-bootstrap/01-RESEARCH.md
@.planning/phases/01-monorepo-bun-server-bootstrap/01-VALIDATION.md
@.planning/phases/01-monorepo-bun-server-bootstrap/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- files the tests will validate -->
packages/mission-control/package.json -- contains "@gsd/mission-control", private: true
packages/mission-control/src/server.ts -- Bun.serve() on port 4000, routes "/" to HTML
package.json (root) -- "workspaces": ["packages/*"], scripts.dev delegates to mission-control
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write smoke tests for workspace structure and server startup</name>
  <files>
    packages/mission-control/tests/setup.test.ts,
    packages/mission-control/tests/server.test.ts
  </files>
  <action>
1. Create `packages/mission-control/tests/setup.test.ts` with Bun test runner (import { describe, expect, it } from "bun:test"):

   Test "workspace structure" group:
   - MONO-01: `packages/mission-control/package.json` exists and has name "@gsd/mission-control"
   - MONO-02: Root `package.json` has "workspaces" field containing "packages/*"
   - MONO-03: Root `package.json` "files" field does NOT include any "packages" entry (GSD core publishes independently)
   - Verify `bunfig.toml` exists in packages/mission-control/
   - Verify `tsconfig.json` exists in packages/mission-control/

   Read files using `Bun.file()` and `JSON.parse()` for package.json assertions.

2. Create `packages/mission-control/tests/server.test.ts` with Bun test runner:

   Test "server" group:
   - SERV-01: Spawn server with `Bun.spawn(["bun", "run", "src/server.ts"], { cwd: "packages/mission-control" })`, wait 2 seconds, then fetch http://localhost:4000
   - Assert response status is 200
   - Assert response body contains "root" (the React mount div)
   - Assert response content-type includes "text/html"
   - Clean up: kill the spawned process in afterAll/afterEach

   Use `Bun.spawn` not `child_process` (Bun-native). Set a test timeout of 10 seconds for the server test.

3. Run the full test suite: `bun test --cwd packages/mission-control`
   All tests must pass.
  </action>
  <verify>
    <automated>bun test --cwd packages/mission-control</automated>
  </verify>
  <done>
    - setup.test.ts passes: workspace structure validated (MONO-01, MONO-02, MONO-03)
    - server.test.ts passes: server starts and responds with HTML on :4000 (SERV-01)
    - All tests green with `bun test --cwd packages/mission-control`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify styled React app renders correctly in browser</name>
  <files>packages/mission-control/src/App.tsx</files>
  <action>
Start the dev server with `bun run dev` from repo root. Present the verification checklist to the user. The server must be running when the user performs verification.
  </action>
  <verify>User confirms visual rendering matches expected output</verify>
  <done>User types "approved" after confirming dark navy background, cyan heading, HMR working, and npm pack clean</done>
  <what-built>
    Complete monorepo workspace with Bun fullstack server serving a React 19 app styled with Tailwind CSS v4 on localhost:4000. Dark navy background (#0F1419) with cyan accent heading (#5BC8F0). All automated tests passing.
  </what-built>
  <how-to-verify>
    1. Run `bun run dev` from the repo root
    2. Open http://localhost:4000 in your browser
    3. Verify you see:
       - Dark navy background filling the viewport
       - "GSD Mission Control" heading in cyan/blue color
       - Subtitle text in gray
       - No console errors in browser DevTools
    4. Edit `packages/mission-control/src/App.tsx` (change the heading text) and verify HMR updates the page without full reload
    5. Stop the server (Ctrl+C)
    6. Run `npm pack --dry-run` at repo root -- confirm no files from `packages/` directory appear
  </how-to-verify>
  <resume-signal>Type "approved" if the styled page renders correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `bun test --cwd packages/mission-control` passes all tests
2. Human confirms visual rendering at localhost:4000
3. HMR works (edit triggers browser update)
4. npm pack --dry-run shows no packages/ contamination
</verification>

<success_criteria>
- All automated tests green covering MONO-01, MONO-02, MONO-03, SERV-01
- Human confirms styled React page renders at localhost:4000 with correct dark theme
- Phase 1 is complete: monorepo workspace bootstrapped, Bun server running, React + Tailwind + shadcn/ui configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bun-server-bootstrap/01-02-SUMMARY.md`
</output>
