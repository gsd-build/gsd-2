---
phase: 03.1-layout-rewrite-sidebar-tab-navigation
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - packages/mission-control/tests/layout.test.tsx
  - packages/mission-control/tests/panel-shell.test.tsx
autonomous: false
requirements: [LAYOUT-VERIFY]

must_haves:
  truths:
    - "Sidebar component renders with expected structure"
    - "TabLayout component switches tabs on click"
    - "AppShell composes Sidebar and TabLayout together"
    - "Layout matches wireframe from CONTEXT.md visually"
  artifacts:
    - path: "packages/mission-control/tests/layout.test.tsx"
      provides: "Unit tests for Sidebar, TabLayout, AppShell"
      min_lines: 40
  key_links:
    - from: "packages/mission-control/tests/layout.test.tsx"
      to: "packages/mission-control/src/components/layout/Sidebar.tsx"
      via: "import and render in test"
      pattern: "import.*Sidebar"
    - from: "packages/mission-control/tests/layout.test.tsx"
      to: "packages/mission-control/src/components/layout/TabLayout.tsx"
      via: "import and render in test"
      pattern: "import.*TabLayout"
---

<objective>
Validate the new sidebar + tab layout with automated tests and a visual checkpoint.

Purpose: Ensure the layout rewrite is correct before future phases build content into these containers.

Output: Passing test suite and user-verified visual layout.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-layout-rewrite-sidebar-tab-navigation/CONTEXT.md
@.planning/phases/03.1-layout-rewrite-sidebar-tab-navigation/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write layout tests for Sidebar, TabLayout, and AppShell</name>
  <files>
    packages/mission-control/tests/layout.test.tsx
    packages/mission-control/tests/panel-shell.test.tsx
  </files>
  <behavior>
    - Sidebar renders new-project button (Plus icon or accessible name)
    - Sidebar renders connection status indicator with "ACTIVE" text
    - Sidebar collapsed state hides text labels
    - TabLayout renders all 3 tab buttons: "Chat & Task", "Milestone", "Slice"
    - TabLayout defaults to "Chat & Task" tab active
    - Clicking "Milestone" tab switches active content
    - Active tab has cyan accent styling (check for border class or aria-selected)
    - AppShell renders both Sidebar and TabLayout
  </behavior>
  <action>
Create `packages/mission-control/tests/layout.test.tsx` using bun:test and happy-dom (same pattern as existing tests like panel-states.test.tsx and panel-shell.test.tsx).

Use `@testing-library/react` if available, otherwise use happy-dom DOM APIs directly matching the pattern in panel-shell.test.tsx.

Check how panel-shell.test.tsx works and follow the same testing approach (DOM rendering strategy, assertions, etc.).

Also update `panel-shell.test.tsx`: Either mark the old PanelShell tests as skipped with a comment "// PanelShell replaced by AppShell in Phase 03.1", or update them to test AppShell instead. Do not delete the file — update it.

Run the new tests and ensure they pass.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/layout.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All layout tests pass. Sidebar, TabLayout, and AppShell render correctly with expected DOM structure. Old panel-shell tests marked as skipped or updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of sidebar + tab layout</name>
  <files>none (visual verification only)</files>
  <action>
Start the dev server and present the layout to the user for visual verification against the CONTEXT.md wireframe. Run `cd packages/mission-control && bun run dev` and instruct the user to check the browser.
  </action>
  <verify>
    <automated>cd packages/mission-control && bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User confirms layout matches CONTEXT.md wireframe: sidebar on left, three tabs on right, correct styling.</done>
  <what-built>New sidebar + tabbed layout replacing the 5-panel resizable layout. Sidebar on left with project area and connection status. Three tabs (Chat & Task, Milestone, Slice) on right with content switching.</what-built>
  <how-to-verify>
    1. Run `cd packages/mission-control && bun run dev`
    2. Open http://localhost:4000 in browser
    3. Verify layout matches the wireframe from CONTEXT.md:
       - Left sidebar (~220px) with "+" button at top and cyan pulsing dot + "ACTIVE" at bottom
       - Tab bar on right with "Chat & Task", "Milestone", "Slice" tabs
       - "Chat & Task" tab active by default with cyan bottom border
    4. Click each tab — content area should switch, showing empty states
    5. Verify dark navy background, cyan accents, Share Tech Mono for headers
    6. Check that the old 5-panel layout is gone
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Layout tests pass: `cd packages/mission-control && bun test tests/layout.test.tsx`
2. No TypeScript errors: `cd packages/mission-control && bunx tsc --noEmit`
3. Visual verification by user confirms wireframe match
</verification>

<success_criteria>
- Layout test suite passes with all assertions
- User confirms visual match to CONTEXT.md wireframe
- Sidebar renders with project area and connection status
- Tab switching works correctly with proper active styling
- Design system (colors, fonts, spacing) correctly applied
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-layout-rewrite-sidebar-tab-navigation/03.1-02-SUMMARY.md`
</output>
