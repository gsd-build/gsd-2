---
phase: 06-chat-panel-claude-code-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/server/ndjson-parser.ts
  - packages/mission-control/src/server/claude-process.ts
  - packages/mission-control/src/server/chat-router.ts
  - packages/mission-control/src/server/chat-types.ts
  - packages/mission-control/tests/ndjson-parser.test.ts
  - packages/mission-control/tests/chat-router.test.ts
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-07]

must_haves:
  truths:
    - "NDJSON parser correctly splits chunked text into typed StreamEvent objects"
    - "Claude Code spawns as child process with correct CLI flags and produces streaming output"
    - "Chat router resolves /gsd: commands locally and routes prompts to Claude process"
    - "Chat routing completes under 200ms excluding model latency"
  artifacts:
    - path: "packages/mission-control/src/server/chat-types.ts"
      provides: "Shared chat types: StreamEvent, ChatMessage, ChatRequest, ChatResponse"
      exports: ["StreamEvent", "ChatMessage", "ChatRequest", "ChatResponse", "ChatEventType"]
    - path: "packages/mission-control/src/server/ndjson-parser.ts"
      provides: "NDJSON line parser TransformStream"
      exports: ["createNdjsonParser", "parseNdjsonLine"]
    - path: "packages/mission-control/src/server/claude-process.ts"
      provides: "Claude Code process lifecycle manager"
      exports: ["spawnClaude", "ClaudeProcessManager"]
    - path: "packages/mission-control/src/server/chat-router.ts"
      provides: "Chat message routing with /gsd: prefix detection"
      exports: ["routeMessage", "isGsdCommand"]
  key_links:
    - from: "packages/mission-control/src/server/claude-process.ts"
      to: "claude CLI"
      via: "Bun.spawn with -p --output-format stream-json flags"
      pattern: "Bun\\.spawn.*claude"
    - from: "packages/mission-control/src/server/claude-process.ts"
      to: "packages/mission-control/src/server/ndjson-parser.ts"
      via: "stdout piped through NDJSON parser"
      pattern: "createNdjsonParser"
    - from: "packages/mission-control/src/server/chat-router.ts"
      to: "packages/mission-control/src/server/claude-process.ts"
      via: "routeMessage dispatches to spawnClaude for non-command prompts"
      pattern: "spawnClaude"
---

<objective>
Server-side chat infrastructure: NDJSON stream parser, Claude Code process manager, and chat message router.

Purpose: Establish the server-side backbone that spawns Claude Code as a child process, parses its streaming NDJSON output into typed events, and routes user messages with sub-200ms local routing.
Output: Four server modules (chat-types, ndjson-parser, claude-process, chat-router) with unit tests for parser and router.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-panel-claude-code-integration/06-RESEARCH.md

<interfaces>
<!-- Existing server types this plan builds alongside -->

From packages/mission-control/src/server/types.ts:
```typescript
export interface PlanningState { roadmap: RoadmapState; state: ProjectState; config: ConfigState; phases: PhaseState[]; requirements: RequirementState[]; }
export interface StateDiff { type: "full" | "diff"; changes: Partial<PlanningState>; timestamp: number; sequence: number; }
```

From packages/mission-control/src/server/ws-server.ts:
```typescript
export interface WsServer { broadcast(diff: StateDiff): void; stop(): void; getSequence(): number; }
export function createWsServer(options: WsServerOptions): WsServer;
```

From packages/mission-control/src/server/pipeline.ts:
```typescript
export interface PipelineHandle { stop(): void; }
export async function startPipeline(options: PipelineOptions): Promise<PipelineHandle>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chat types and NDJSON stream parser with tests</name>
  <files>
    packages/mission-control/src/server/chat-types.ts,
    packages/mission-control/src/server/ndjson-parser.ts,
    packages/mission-control/tests/ndjson-parser.test.ts
  </files>
  <behavior>
    - parseNdjsonLine: valid JSON line returns parsed object
    - parseNdjsonLine: empty/whitespace lines return null
    - parseNdjsonLine: malformed JSON returns null (no throw)
    - createNdjsonParser: chunked input split across newline boundaries yields complete events
    - createNdjsonParser: multiple events in single chunk all emitted
    - createNdjsonParser: incomplete trailing line buffered until flush
    - StreamEvent type covers: system, assistant, result, content_block_delta (text_delta, input_json_delta)
  </behavior>
  <action>
    1. Create chat-types.ts with shared types:
       - ChatEventType union: "system" | "assistant" | "result" | "stream_event"
       - StreamEvent interface matching Claude CLI stream-json output (type, event with delta/content_block, session_id, result)
       - ChatMessage interface: { id: string, role: "user" | "assistant" | "system", content: string, timestamp: number, streaming: boolean, toolName?: string, toolDone?: boolean }
       - ChatRequest: { type: "chat", prompt: string }
       - ChatResponse: { type: "chat_event" | "chat_complete" | "chat_error", event?: StreamEvent, sessionId?: string, error?: string }

    2. Create ndjson-parser.ts with two exports:
       - parseNdjsonLine(line: string): StreamEvent | null -- pure function, try JSON.parse, return null on failure
       - createNdjsonParser(): TransformStream<string, StreamEvent> -- buffers partial lines, splits on "\n", parses each complete line, flushes remainder

    3. Write tests in ndjson-parser.test.ts covering all behaviors above. Use fixture NDJSON strings matching Claude CLI output format.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/ndjson-parser.test.ts</automated>
  </verify>
  <done>parseNdjsonLine handles valid/empty/malformed lines. createNdjsonParser handles chunked input, multi-event chunks, and incomplete trailing lines. All types exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Claude process manager and chat router with tests</name>
  <files>
    packages/mission-control/src/server/claude-process.ts,
    packages/mission-control/src/server/chat-router.ts,
    packages/mission-control/tests/chat-router.test.ts
  </files>
  <behavior>
    - isGsdCommand: "/gsd:plan-phase" returns true
    - isGsdCommand: "hello world" returns false
    - isGsdCommand: "/gsd:" (bare prefix) returns true
    - routeMessage: /gsd: command returns { type: "command", command: string }
    - routeMessage: plain text returns { type: "prompt", prompt: string }
    - routeMessage: routing completes under 5ms (well under 200ms target)
    - ClaudeProcessManager: tracks active process, rejects concurrent spawn
    - ClaudeProcessManager: kill() terminates active process
  </behavior>
  <action>
    1. Create claude-process.ts:
       - ClaudeProcessManager class with:
         - spawn(prompt, cwd): spawns `claude -p <prompt> --output-format stream-json --verbose --include-partial-messages` via Bun.spawn with stdin: "ignore" (not "inherit" since server has no TTY), stdout: "pipe", stderr: "pipe"
         - Returns AsyncIterable<StreamEvent> by piping stdout through TextDecoderStream then createNdjsonParser
         - Tracks active process ref; if spawn called while active, rejects with error
         - kill(): calls proc.kill() and awaits proc.exited
         - isActive getter
         - Stores sessionId from ResultMessage for future --resume support
       - CRITICAL: stdin set to "ignore" per research (no TTY in server context; "inherit" for TTY, "ignore" for daemon)

    2. Create chat-router.ts:
       - isGsdCommand(input: string): boolean -- checks input.trimStart().startsWith("/gsd:")
       - routeMessage(input: string): { type: "command", command: string, args: string } | { type: "prompt", prompt: string }
         - For /gsd: commands: parse command name and remaining args
         - For everything else: return as prompt
       - Pure functions, no side effects, sub-millisecond execution

    3. Write tests in chat-router.test.ts:
       - Unit tests for isGsdCommand and routeMessage
       - Performance test: routeMessage x1000 under 50ms total
       - ClaudeProcessManager: test spawn rejection when active (mock Bun.spawn)
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/chat-router.test.ts</automated>
  </verify>
  <done>isGsdCommand correctly identifies /gsd: prefix. routeMessage routes commands vs prompts. ClaudeProcessManager spawns claude with correct flags, rejects concurrent spawn, and supports kill. Routing performance under 200ms verified.</done>
</task>

</tasks>

<verification>
- All tests pass: `cd packages/mission-control && bun test tests/ndjson-parser.test.ts tests/chat-router.test.ts`
- chat-types.ts exports all required types without compilation errors
- No new npm dependencies added (Bun.spawn and Web Streams are built-in)
</verification>

<success_criteria>
- NDJSON parser handles chunked streaming input correctly
- Claude process manager spawns with correct CLI flags
- Chat router resolves /gsd: commands locally in under 200ms
- All four server modules export clean typed interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-panel-claude-code-integration/06-01-SUMMARY.md`
</output>
