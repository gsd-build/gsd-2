---
phase: 06-chat-panel-claude-code-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/lib/slash-commands.ts
  - packages/mission-control/src/components/chat/ChatInput.tsx
  - packages/mission-control/src/components/chat/ChatMessage.tsx
  - packages/mission-control/src/components/chat/SlashAutocomplete.tsx
  - packages/mission-control/src/components/chat/ChatPanel.tsx
  - packages/mission-control/tests/chat-input.test.tsx
  - packages/mission-control/tests/chat-message.test.tsx
autonomous: true
requirements: [CHAT-01, CHAT-04, CHAT-06]

must_haves:
  truths:
    - "Chat input shows / prefix hint and filters GSD slash commands as user types"
    - "Agent responses are visually distinct from user and system messages"
    - "Up arrow recalls previous commands from history"
    - "Chat panel renders message list with input at bottom"
  artifacts:
    - path: "packages/mission-control/src/lib/slash-commands.ts"
      provides: "GSD command registry with descriptions"
      exports: ["GSD_COMMANDS", "filterCommands"]
    - path: "packages/mission-control/src/components/chat/ChatInput.tsx"
      provides: "Chat input with autocomplete and command history"
      exports: ["ChatInput"]
    - path: "packages/mission-control/src/components/chat/ChatMessage.tsx"
      provides: "Message renderer with role-based styling"
      exports: ["ChatMessage"]
    - path: "packages/mission-control/src/components/chat/SlashAutocomplete.tsx"
      provides: "Autocomplete dropdown for slash commands"
      exports: ["SlashAutocomplete"]
    - path: "packages/mission-control/src/components/chat/ChatPanel.tsx"
      provides: "Chat panel container with message list and input"
      exports: ["ChatPanel"]
  key_links:
    - from: "packages/mission-control/src/components/chat/ChatInput.tsx"
      to: "packages/mission-control/src/lib/slash-commands.ts"
      via: "filterCommands called on input change"
      pattern: "filterCommands"
    - from: "packages/mission-control/src/components/chat/ChatInput.tsx"
      to: "packages/mission-control/src/components/chat/SlashAutocomplete.tsx"
      via: "renders SlashAutocomplete when filtered results exist"
      pattern: "SlashAutocomplete"
    - from: "packages/mission-control/src/components/chat/ChatPanel.tsx"
      to: "packages/mission-control/src/components/chat/ChatMessage.tsx"
      via: "maps messages array to ChatMessage components"
      pattern: "ChatMessage"
---

<objective>
Client-side chat UI components: slash command registry, chat input with autocomplete and command history, message renderer with role-based styling, and chat panel container.

Purpose: Build the presentational chat components that will be wired to the server-side Claude process in Plan 03. Components are designed to receive messages as props and call onSend callbacks -- no server coupling.
Output: Five client modules (slash-commands, ChatInput, ChatMessage, SlashAutocomplete, ChatPanel) with component tests.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-panel-claude-code-integration/06-RESEARCH.md

<interfaces>
<!-- Design system patterns from existing components -->

From packages/mission-control/src/styles/design-tokens.ts (patterns):
- COLORS.navy, COLORS.cyan, COLORS.slate for dark theme
- 8-point spacing grid: p-2, p-4, p-6, p-8
- font-display for headers (Share Tech Mono), font-mono for data (JetBrains Mono)

From packages/mission-control/src/components/layout/TabLayout.tsx:
```typescript
type TabId = "chat-task" | "milestone" | "slice";
// Chat & Task tab currently renders TaskExecuting or TaskWaiting
```

From packages/mission-control/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string; // clsx + tailwind-merge
```

ChatMessage type (to be created in 06-01 chat-types.ts):
```typescript
interface ChatMessage {
  id: string;
  role: "user" | "assistant" | "system";
  content: string;
  timestamp: number;
  streaming: boolean;
  toolName?: string;
  toolDone?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slash command registry, ChatInput, and SlashAutocomplete with tests</name>
  <files>
    packages/mission-control/src/lib/slash-commands.ts,
    packages/mission-control/src/components/chat/ChatInput.tsx,
    packages/mission-control/src/components/chat/SlashAutocomplete.tsx,
    packages/mission-control/tests/chat-input.test.tsx
  </files>
  <behavior>
    - filterCommands("/gsd:p"): returns commands starting with "/gsd:p" (plan-phase, pause-work, progress)
    - filterCommands("hello"): returns empty array (not a slash prefix)
    - filterCommands("/"): returns all commands
    - ChatInput: typing "/" shows autocomplete dropdown
    - ChatInput: selecting autocomplete item fills input
    - ChatInput: Enter key calls onSend with input value and clears input
    - ChatInput: up arrow recalls previous command
    - ChatInput: down arrow navigates forward in history
    - ChatInput: empty input Enter does not call onSend
    - ChatInput: placeholder shows "Type / for commands..."
  </behavior>
  <action>
    1. Create slash-commands.ts:
       - Export GSD_COMMANDS array with { command, description, args } for all 22 GSD commands (list from research)
       - Export filterCommands(input: string): typeof GSD_COMMANDS -- returns commands where command.startsWith(input), or all if input is just "/"

    2. Create SlashAutocomplete.tsx:
       - Props: { commands: typeof GSD_COMMANDS, onSelect: (command: string) => void }
       - Renders absolute-positioned dropdown above input (bottom-full)
       - Each item shows command in cyan-accent, description in slate-400
       - Keyboard navigation not required for v1 (click to select)
       - Style: bg-navy-800, border-navy-600, rounded-t, text-xs font-mono

    3. Create ChatInput.tsx:
       - Props: { onSend: (message: string) => void, disabled?: boolean }
       - Internal state: value, command history (useRef array with index pointer)
       - On "/" typed: filter commands and show SlashAutocomplete
       - On Enter (not shift+Enter): call onSend, push to history, clear input
       - On ArrowUp: navigate history backward
       - On ArrowDown: navigate history forward
       - When disabled: show "Claude is working..." placeholder, input disabled
       - Style: bg-navy-800, text-slate-200, px-4 py-3, text-sm font-mono, border-t border-navy-600

    4. Write tests in chat-input.test.tsx covering filterCommands and ChatInput behaviors.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/chat-input.test.tsx</automated>
  </verify>
  <done>filterCommands correctly filters GSD commands by prefix. ChatInput renders with placeholder, handles Enter/ArrowUp/ArrowDown, shows/hides autocomplete on "/" input. Disabled state shows working indicator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatMessage, ChatPanel container, and message styling tests</name>
  <files>
    packages/mission-control/src/components/chat/ChatMessage.tsx,
    packages/mission-control/src/components/chat/ChatPanel.tsx,
    packages/mission-control/tests/chat-message.test.tsx
  </files>
  <behavior>
    - ChatMessage(role=assistant): renders with left cyan border and navy-800 background
    - ChatMessage(role=system): renders with italic text, slate-500 color, navy-900 bg
    - ChatMessage(role=user): renders with default navy-base background
    - ChatMessage(streaming=true): shows pulsing cursor indicator
    - ChatMessage(toolName set): shows tool usage indicator in amber
    - ChatPanel: renders message list scrollable area with ChatInput at bottom
    - ChatPanel: auto-scrolls to bottom on new message
  </behavior>
  <action>
    1. Create ChatMessage.tsx:
       - Props: { message: ChatMessage } (import type from chat-types or define locally for now)
       - Role-based styling using cn():
         - assistant: bg-navy-800 border-l-2 border-cyan-accent
         - system: bg-navy-900 text-slate-500 italic text-xs
         - user: bg-navy-base (no special border)
       - If toolName present: show amber indicator "[Using {toolName}...]" with "done" suffix if toolDone
       - If streaming: show pulsing "|" cursor via animate-pulse
       - Content in whitespace-pre-wrap span
       - Style: px-4 py-3 text-sm font-mono

    2. Create ChatPanel.tsx:
       - Props: { messages: ChatMessage[], onSend: (msg: string) => void, isProcessing: boolean }
       - Layout: flex flex-col h-full
       - Top: scrollable message list (flex-1 overflow-y-auto), auto-scroll to bottom via useEffect + ref
       - Bottom: ChatInput with disabled={isProcessing}
       - Empty state when no messages: centered text "Start a conversation with /gsd: commands" in slate-500

    3. Write tests in chat-message.test.tsx:
       - ChatMessage renders different styles per role
       - ChatMessage shows streaming indicator
       - ChatMessage shows tool indicator
       - ChatPanel renders messages and input
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/chat-message.test.tsx</automated>
  </verify>
  <done>ChatMessage renders visually distinct styles for assistant/system/user roles. Streaming cursor and tool indicators work. ChatPanel composes message list with input and handles empty state.</done>
</task>

</tasks>

<verification>
- All tests pass: `cd packages/mission-control && bun test tests/chat-input.test.tsx tests/chat-message.test.tsx`
- Components use design system tokens (cyan-accent, navy-800, font-mono, 8-point spacing)
- No server-side imports in client components (pure presentational)
</verification>

<success_criteria>
- Slash command autocomplete filters all 22 GSD commands
- Chat input handles Enter, ArrowUp, ArrowDown, and disabled state
- Message styling clearly distinguishes assistant (cyan border), system (italic), user (plain) roles
- ChatPanel auto-scrolls and shows empty state
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-panel-claude-code-integration/06-02-SUMMARY.md`
</output>
