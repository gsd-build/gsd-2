---
phase: 06.3-new-capabilities
plan: 02
subsystem: api
tags: [settings, assets, rest-api, file-upload, config-merge, bun]

requires:
  - phase: 06.1
    provides: "Route dispatcher pattern (handleXRequest -> Response|null)"
provides:
  - "Two-tier settings API (global + project config merge)"
  - "Assets API (upload, list, delete with validation)"
  - "Pipeline getPlanningDir() accessor"
affects: [settings-panel-ui, project-assets-gallery]

tech-stack:
  added: []
  patterns: [two-tier-config-merge, multipart-upload, duplicate-filename-suffix]

key-files:
  created:
    - packages/mission-control/src/server/settings-api.ts
    - packages/mission-control/src/server/assets-api.ts
    - packages/mission-control/tests/settings-api.test.ts
    - packages/mission-control/tests/assets-api.test.ts
  modified:
    - packages/mission-control/src/server.ts
    - packages/mission-control/src/server/pipeline.ts

key-decisions:
  - "_setGlobalDir() test injection instead of mocking os.homedir() — simpler, no module mock needed"
  - "Path traversal check before sub-path routing in DELETE handler for security"
  - "Pipeline getPlanningDir() getter added to PipelineHandle interface for route handlers"

patterns-established:
  - "Two-tier config: readJsonSafe with try/catch returning {} on missing files"
  - "Duplicate filename handling: append -N suffix before extension"
  - "CORS updated to include PUT/DELETE methods"

requirements-completed: [SETTINGS-01, SETTINGS-02, ASSETS-01, ASSETS-02]

duration: 5min
completed: 2026-03-11
---

# Phase 06.3 Plan 02: Settings & Assets API Summary

**Two-tier settings API (global + project merge) and assets API (upload/list/delete with type/size validation) wired into server**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-11T08:31:29Z
- **Completed:** 2026-03-11T08:36:18Z
- **Tasks:** 2 (TDD: 4 commits per task pair)
- **Files modified:** 6

## Accomplishments
- Settings API reads/merges global (~/.gsd/defaults.json) and project (.planning/config.json) tiers with project overriding global
- Assets API uploads files to .planning/assets/ with extension validation (png, jpg, jpeg, gif, svg, mp4, webm, pdf, md, txt) and 50MB size limit
- Path traversal protection on asset delete, duplicate filename suffix handling on upload
- Both APIs wired into server.ts with CORS headers supporting PUT/DELETE

## Task Commits

Each task was committed atomically (TDD: RED then GREEN):

1. **Task 1: Settings API** - RED: `1fa9ff4` (test) -> GREEN: `1affe45` (feat)
2. **Task 2: Assets API + wiring** - RED: `ac3ee5a` (test) -> GREEN: `9a131b8` (feat)

_TDD flow: failing tests first, then implementation to pass_

## Files Created/Modified
- `packages/mission-control/src/server/settings-api.ts` - Two-tier config CRUD with getSettings/saveSettings/handleSettingsRequest
- `packages/mission-control/src/server/assets-api.ts` - Upload, list, delete with type/size validation and path traversal protection
- `packages/mission-control/src/server.ts` - Routes wired for /api/settings and /api/assets/*, CORS updated
- `packages/mission-control/src/server/pipeline.ts` - Added getPlanningDir() to PipelineHandle interface
- `packages/mission-control/tests/settings-api.test.ts` - 11 tests for settings merge, save, and HTTP handler
- `packages/mission-control/tests/assets-api.test.ts` - 11 tests for upload, list, delete, validation

## Decisions Made
- Used _setGlobalDir() injection for test isolation instead of mocking os.homedir() — simpler and avoids module-level mocks
- Path traversal check runs before sub-path routing in DELETE handler to catch encoded traversal attempts (..%2F)
- Added getPlanningDir() to PipelineHandle interface so route handlers can access the current planning directory

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Path traversal check ordering in DELETE handler**
- **Found during:** Task 2 (Assets API implementation)
- **Issue:** Encoded path traversal (..%2F) was decoded to contain "/" which triggered the sub-path null return before the traversal check
- **Fix:** Moved ".." check before "/" sub-path check
- **Files modified:** packages/mission-control/src/server/assets-api.ts
- **Verification:** Path traversal test passes, returns 400
- **Committed in:** 9a131b8 (Task 2 GREEN commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Security fix necessary for correctness. No scope creep.

## Issues Encountered
None beyond the path traversal ordering fix documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Settings and assets backend ready for UI integration in settings panel and project assets gallery
- Routes available at /api/settings (GET/PUT) and /api/assets/* (POST upload, GET list, DELETE)

## Self-Check: PASSED

All 4 created files verified on disk. All 4 commit hashes (1fa9ff4, 1affe45, ac3ee5a, 9a131b8) verified in git log.

---
*Phase: 06.3-new-capabilities*
*Completed: 2026-03-11*
