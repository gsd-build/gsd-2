---
phase: 06.3-new-capabilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src/server/session-manager.ts
  - packages/mission-control/src/server/chat-types.ts
  - packages/mission-control/src/server/pipeline.ts
  - packages/mission-control/src/server/ws-server.ts
  - packages/mission-control/tests/session-manager.test.ts
  - packages/mission-control/tests/session-ws.test.ts
autonomous: true
requirements: [MULTI-01, MULTI-02, MULTI-03]

must_haves:
  truths:
    - "Server can create up to 4 concurrent chat sessions per project"
    - "Each session has its own ClaudeProcessManager instance"
    - "WebSocket messages route to the correct session by sessionId"
    - "Closing a session kills its Claude process"
    - "Creating a 5th session is rejected with error"
    - "Session metadata (IDs, names, tab order) persists to disk and restores on startup"
  artifacts:
    - path: "packages/mission-control/src/server/session-manager.ts"
      provides: "SessionManager class with create/get/close/rename/list and metadata persistence"
      exports: ["SessionManager", "SessionState"]
    - path: "packages/mission-control/tests/session-manager.test.ts"
      provides: "Unit tests for session lifecycle"
    - path: "packages/mission-control/tests/session-ws.test.ts"
      provides: "Unit tests for WebSocket session routing"
  key_links:
    - from: "packages/mission-control/src/server/pipeline.ts"
      to: "packages/mission-control/src/server/session-manager.ts"
      via: "SessionManager replaces single processManager"
      pattern: "sessionManager\\."
    - from: "packages/mission-control/src/server/ws-server.ts"
      to: "packages/mission-control/src/server/session-manager.ts"
      via: "sessionId routing on chat messages"
      pattern: "parsed\\.sessionId"
---

<objective>
Create the server-side multi-session infrastructure: SessionManager class, session-scoped WebSocket protocol, and pipeline refactoring.

Purpose: Foundation for multi-session chat. All subsequent session UI work depends on this server-side registry.
Output: SessionManager class, extended chat types, refactored pipeline and WebSocket server, with tests for both.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-new-capabilities-multi-session-chat-git-worktree-per-session-settings-panel-project-assets/06.3-RESEARCH.md

<interfaces>
<!-- Existing types and contracts the executor needs -->

From packages/mission-control/src/server/chat-types.ts:
```typescript
export interface ChatRequest {
  type: "chat";
  prompt: string;
}

export interface ChatResponse {
  type: "chat_event" | "chat_complete" | "chat_error" | "permission_prompt";
  event?: StreamEvent;
  sessionId?: string;
  error?: string;
}

export interface ChatMessage {
  id: string;
  role: "user" | "assistant" | "system";
  content: string;
  timestamp: number;
  streaming: boolean;
  toolName?: string;
  toolDone?: boolean;
}
```

From packages/mission-control/src/server/claude-process.ts:
```typescript
export class ClaudeProcessManager {
  constructor(cwd: string, options?: ClaudeProcessOptions);
  get isActive(): boolean;
  get isProcessing(): boolean;
  get sessionId(): string | null;
  onEvent(handler: (event: StreamEvent) => void): void;
  start(): Promise<void>;
  sendMessage(prompt: string): Promise<void>;
  kill(): Promise<void>;
}
```

From packages/mission-control/src/server/pipeline.ts:
```typescript
export interface PipelineHandle {
  stop(): void;
  processManager: ClaudeProcessManager;
  switchProject(newPlanningDir: string): Promise<void>;
}
```

From packages/mission-control/src/server/ws-server.ts:
```typescript
export interface WsServer {
  broadcast(diff: StateDiff): void;
  sendToClient(ws: ServerWebSocket, data: unknown): void;
  publishChat(data: unknown): void;
  stop(): void;
  getSequence(): number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SessionManager class with lifecycle, persistence, and tests</name>
  <files>packages/mission-control/src/server/session-manager.ts, packages/mission-control/src/server/chat-types.ts, packages/mission-control/tests/session-manager.test.ts</files>
  <behavior>
    - createSession returns SessionState with unique id, default name "Chat N", slug, and ClaudeProcessManager instance
    - createSession with forkFromSessionId copies the source session's Claude --resume session ID
    - getSession returns the session by id, undefined if not found
    - listSessions returns all sessions ordered by createdAt
    - closeSession kills the Claude process and removes from registry
    - closeSession on nonexistent id is a no-op (no throw)
    - createSession rejects with error when 4 sessions already exist
    - renameSession updates name and slug (display-only rename, no worktree rename)
    - Sessions start with sequential names: "Chat 1", "Chat 2", etc., reusing gaps from closed sessions
    - persistMetadata writes session IDs, names, slugs, tab order to .planning/.session-metadata.json
    - restoreMetadata reads .planning/.session-metadata.json and re-creates sessions (without Claude processes — those start on first message)
    - persistMetadata is called after every create/close/rename operation
    - restoreMetadata gracefully handles missing or corrupt file (returns empty)
  </behavior>
  <action>
    1. Extend `chat-types.ts`:
       - Add `sessionId: string` field to `ChatRequest` interface
       - Add `SessionMetadata` interface: `{ id: string; name: string; slug: string; isProcessing: boolean; createdAt: number }`

    2. Create `session-manager.ts`:
       - Export `SessionState` interface: `{ id: string; name: string; slug: string; processManager: ClaudeProcessManager; activeClient: ServerWebSocket | null; worktreePath: string | null; worktreeBranch: string | null; createdAt: number; claudeSessionId: string | null }`
       - Export `SessionManager` class with `private sessions = new Map<string, SessionState>()`
       - Constructor takes `planningDir: string` for persistence path
       - `maxSessions = 4` constant
       - `createSession(cwd: string, opts?: { forkFromSessionId?: string }): SessionState` — generates UUID via `crypto.randomUUID()`, assigns name "Chat N" using next available number, creates new `ClaudeProcessManager(cwd)`. If forking, copies `claudeSessionId` from source session. Calls `persistMetadata()` after.
       - `getSession(id: string): SessionState | undefined`
       - `listSessions(): SessionState[]` — returns sorted by createdAt
       - `async closeSession(id: string): Promise<void>` — sets `activeClient = null` first (stop routing), then `processManager.kill()`, then `sessions.delete(id)`. Calls `persistMetadata()` after.
       - `renameSession(id: string, newName: string): void` — updates name and regenerates slug (kebab-case). Calls `persistMetadata()` after.
       - `getMetadata(): SessionMetadata[]` — returns lightweight session list for client
       - `async persistMetadata(): Promise<void>` — writes `{ sessions: [{ id, name, slug, createdAt, claudeSessionId }], tabOrder: [...ids] }` to `planningDir/.session-metadata.json`
       - `async restoreMetadata(cwd: string): Promise<void>` — reads `.session-metadata.json`, recreates SessionState entries with lazy ClaudeProcessManager (creates PM instance but does NOT start it — PM starts on first sendMessage). Wraps in try/catch, logs warning and returns empty on corrupt/missing file.
       - Helper: `private toSlug(name: string): string` — `name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')`
       - Helper: `private nextName(): string` — finds lowest unused "Chat N" number

    3. Write tests in `tests/session-manager.test.ts`:
       - Test all behaviors listed above including persistence
       - Mock ClaudeProcessManager with minimal stub (constructor, kill returning Promise, isProcessing)
       - Use temp directory as planningDir for persistence tests
       - Test persistMetadata creates file, restoreMetadata reads it back
       - Test corrupt JSON file handled gracefully
       - Use `beforeEach` to create fresh SessionManager
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/session-manager.test.ts</automated>
  </verify>
  <done>SessionManager class handles full create/get/close/rename/list lifecycle. Max 4 sessions enforced. Fork copies Claude session ID. Metadata persists to .session-metadata.json and restores on startup. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor pipeline and WebSocket server for multi-session routing with tests</name>
  <files>packages/mission-control/src/server/pipeline.ts, packages/mission-control/src/server/ws-server.ts, packages/mission-control/tests/session-ws.test.ts</files>
  <behavior>
    - WebSocket message with sessionId routes to correct session's processManager
    - WebSocket message without sessionId routes to first (default) session
    - session_create action creates a new session and broadcasts session_update
    - session_close action closes session and broadcasts session_update
    - session_rename action renames session and broadcasts session_update
    - session_list action responds with current session metadata
    - Chat message to nonexistent sessionId returns chat_error
    - Pipeline startup creates default session and restores persisted metadata
    - switchProject closes all sessions and creates fresh default in new project
  </behavior>
  <action>
    1. Refactor `pipeline.ts`:
       - Import `SessionManager` and `SessionState`
       - Replace single `processManager` variable with `const sessionManager = new SessionManager(planningDir)`
       - On startup: call `sessionManager.restoreMetadata(repoRoot)` to restore prior sessions, then if no sessions exist, `sessionManager.createSession(repoRoot)` to ensure at least one default session
       - Replace `wireProcessManagerEvents(pm)` with a generic `wireSessionEvents(session: SessionState)` that routes events by session.id
       - Update `PipelineHandle`: replace `processManager` with `sessionManager: SessionManager`, add `createSession`, `closeSession`, `renameSession` methods that delegate to sessionManager
       - In `switchProject`: iterate `sessionManager.listSessions()`, close all sessions, create fresh default session in new project
       - Wire `onChatMessage` to accept `sessionId` from parsed message: look up session via `sessionManager.getSession(sessionId)`, send message to that session's processManager. If no sessionId provided, use first session (backward compat). If session not found, send chat_error.
       - Wire new WS message types: `session_create`, `session_close`, `session_rename`, `session_list` — each calls sessionManager and broadcasts updated session metadata to all clients

    2. Refactor `ws-server.ts`:
       - Add `onSessionAction?: (action: SessionAction, ws: ServerWebSocket) => void` to `WsServerOptions`
       - Define `SessionAction = { type: "session_create"; forkFromSessionId?: string } | { type: "session_close"; sessionId: string } | { type: "session_rename"; sessionId: string; name: string } | { type: "session_list" }`
       - In message handler: parse `session_create`, `session_close`, `session_rename`, `session_list` message types and call `onSessionAction`
       - Update chat message parsing to extract `sessionId` from parsed JSON and pass to `onChatMessage(prompt, ws, sessionId)`
       - Update `onChatMessage` callback signature: `(prompt: string, ws: ServerWebSocket, sessionId?: string) => void`
       - Add `publishSessionUpdate(data: unknown): void` to WsServer interface that broadcasts session metadata changes

    3. Backward compatibility: if a chat message arrives without sessionId, route to the first (default) session. This ensures existing client code works until the UI is updated in Plan 04.

    4. Write tests in `tests/session-ws.test.ts`:
       - Test WebSocket message parsing extracts sessionId from chat messages
       - Test session action dispatch (session_create, session_close, session_rename, session_list)
       - Test backward compat: message without sessionId routes to default session
       - Test chat_error response when sessionId not found
       - Mock SessionManager and WebSocket to isolate WS routing logic
       - Test publishSessionUpdate broadcasts to all connected clients
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/session-ws.test.ts && bun build src/server.ts --target=bun --outdir=dist --no-splitting 2>&1 | head -5</automated>
  </verify>
  <done>Pipeline uses SessionManager instead of single processManager. WebSocket routes chat messages by sessionId. Session CRUD actions flow through WS protocol. Session metadata restored on startup. Build compiles without errors. All WS routing tests pass.</done>
</task>

</tasks>

<verification>
- `bun test tests/session-manager.test.ts` passes all session lifecycle and persistence tests
- `bun test tests/session-ws.test.ts` passes all WS routing tests
- `bun build` compiles with no type errors
- Server starts, restores session metadata, and default session is created automatically
</verification>

<success_criteria>
- SessionManager supports create/close/rename/list/fork with max 4 enforcement
- Session metadata persists to .session-metadata.json and restores on startup
- Pipeline routes chat messages to correct session by sessionId
- WebSocket protocol supports session CRUD actions
- Backward compatible: no sessionId = uses default session
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-new-capabilities-multi-session-chat-git-worktree-per-session-settings-panel-project-assets/06.3-01-SUMMARY.md`
</output>
