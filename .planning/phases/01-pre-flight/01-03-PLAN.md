---
phase: 01-pre-flight
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: []
autonomous: true
requirements: [PF-05, PF-06]

must_haves:
  truths:
    - "madge --circular reports zero cycles in pi-coding-agent/src/"
    - "zero .ts import extension specifiers exist in pi-coding-agent/src/"
    - "npm run test:packages passes with no regressions"
  artifacts: []
  key_links: []
---

<objective>
Run the madge final gate (PF-05 second half) and the .ts extension specifier grep audit (PF-06). Confirm zero circular dependencies and zero .ts specifiers. Run the package test suite as a regression gate.

Purpose: This is the Phase 1 exit gate. PF-05 final gate confirms all circular dependency fixes from PF-01 and PF-04 are effective. PF-06 confirms the codebase was already compliant. The test suite run confirms no regressions from the four fix commits.

Output: Two audit commits (PF-05, PF-06) documenting results. Phase 1 complete — all preconditions for file moves in Phases 3-4 are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight/01-CONTEXT.md
@.planning/phases/01-pre-flight/01-RESEARCH.md
@.planning/phases/01-pre-flight/01-01-SUMMARY.md
@.planning/phases/01-pre-flight/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run madge final gate and .ts specifier audit — PF-05 gate + PF-06 (per D-05, D-06)</name>
  <files>N/A — audit only, no file modifications</files>
  <read_first>
    - .planning/phases/01-pre-flight/01-01-SUMMARY.md (madge baseline output for comparison)
  </read_first>
  <action>
**Step 1 — PF-05 final gate: Run madge circular dependency audit:**
```bash
npx madge --circular packages/pi-coding-agent/src/ --extensions ts
```
This MUST report zero circular dependencies. If it reports any cycles, STOP — do not commit. The cycles must be fixed before proceeding (return to the relevant PF fix).

Compare the output against the baseline from Plan 01 Task 1 (captured in the 01-01-SUMMARY.md). The baseline showed ~16 cycles. The final gate must show 0.

If zero cycles confirmed, commit per D-07:
```
refactor: [PF-05] madge audit confirms zero circular dependencies
```
This is a documentation-only commit — add a brief note to the commit message body showing "madge --circular: 0 cycles (baseline: N cycles)".

**Step 2 — PF-06 grep audit: Confirm zero .ts extension specifiers:**
```bash
grep -r "from.*\.ts['\"]" packages/pi-coding-agent/src/ --include="*.ts"
```
This MUST return zero results. Per D-06 and research, the `tsconfig.json` already enforces `allowImportingTsExtensions: false` so this is expected to be a no-op confirmation.

If zero results confirmed, commit per D-07:
```
refactor: [PF-06] audit confirms zero .ts import specifiers in pi-coding-agent
```

**Step 3 — Regression gate: Build and run package tests:**
```bash
cd packages/pi-coding-agent && tsc -p tsconfig.json && npm run copy-assets
```
Then:
```bash
npm run test:packages
```
All tests must pass. If any fail, investigate and fix before completing this plan.
  </action>
  <verify>
    <automated>npx madge --circular /Users/trekkie/projects/gsd-2/packages/pi-coding-agent/src/ --extensions ts 2>&1 | grep -c "No circular" || npx madge --circular /Users/trekkie/projects/gsd-2/packages/pi-coding-agent/src/ --extensions ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx madge --circular packages/pi-coding-agent/src/ --extensions ts` reports zero circular dependencies (output contains "No circular dependency found" or equivalent zero-result output)
    - `grep -r "from.*\.ts['\"]" packages/pi-coding-agent/src/ --include="*.ts" | wc -l` returns `0`
    - `npm run test:packages` exits 0 (all tests pass)
    - `git log --oneline -2` contains both `[PF-05]` and `[PF-06]`
    - `git log --oneline -6 | grep -c "\[PF-0"` returns `6` (all six PF commits present on branch)
  </acceptance_criteria>
  <done>madge confirms zero circular dependencies (down from ~16 baseline). grep confirms zero .ts specifiers. Package tests pass. All 6 PF commits landed on refactor/pi-clean-seam. Phase 1 pre-flight complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Audit-only tasks — no code changes, no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | N/A | N/A | accept | No code changes — audit and test run only |
</threat_model>

<verification>
After all tasks:
1. `npx madge --circular packages/pi-coding-agent/src/ --extensions ts` shows zero cycles
2. `grep -r "from.*\.ts['\"]" packages/pi-coding-agent/src/ --include="*.ts"` returns empty
3. `npm run test:packages` passes
4. `git log --oneline -6` on refactor/pi-clean-seam shows all 6 PF commits
</verification>

<success_criteria>
- madge reports zero circular dependencies in pi-coding-agent/src/
- grep confirms zero .ts extension specifiers in pi-coding-agent/src/
- npm run test:packages passes with no regressions
- All 6 PF commits (PF-01 through PF-06) present on refactor/pi-clean-seam branch
- Phase 1 exit criteria from ROADMAP.md all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-flight/01-03-SUMMARY.md`
</output>
