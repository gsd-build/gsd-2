---
phase: 03-gsd-agent-modes-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pi-coding-agent/src/core/thinking-level.ts
  - packages/pi-coding-agent/src/core/model-resolver.ts
  - packages/pi-coding-agent/src/cli/args.ts
  - packages/pi-coding-agent/src/core/theme/theme.ts
  - packages/pi-coding-agent/src/core/theme/themes.ts
  - packages/pi-coding-agent/src/core/resource-loader.ts
  - packages/pi-coding-agent/src/core/extensions/runner.ts
  - packages/pi-coding-agent/src/core/extensions/types.ts
  - packages/pi-coding-agent/src/core/export-html/index.ts
  - packages/pi-coding-agent/src/core/export-html/tool-renderer.ts
  - packages/pi-coding-agent/src/index.ts
  - packages/pi-coding-agent/src/modes/interactive/theme/theme.ts
  - packages/pi-coding-agent/src/modes/interactive/theme/themes.ts
  - packages/gsd-agent-modes/src/cli/args.ts
  - packages/gsd-agent-modes/src/cli/config-selector.ts
  - packages/gsd-agent-modes/src/cli/file-processor.ts
  - packages/gsd-agent-modes/src/cli/list-models.ts
  - packages/gsd-agent-modes/src/cli/session-picker.ts
  - packages/gsd-agent-modes/src/modes/shared/command-context-actions.ts
  - packages/gsd-agent-modes/src/modes/print-mode.ts
autonomous: true
requirements:
  - MODES-01
  - MODES-02
  - MODES-03

must_haves:
  truths:
    - "isValidThinkingLevel is importable from pi-coding-agent/src/core/thinking-level.ts, not from cli/args.ts"
    - "theme/ directory lives at pi-coding-agent/src/core/theme/, not modes/interactive/theme/"
    - "All 5 cli/ files exist in gsd-agent-modes/src/cli/ and are deleted from pi-coding-agent/src/cli/"
    - "modes/shared/command-context-actions.ts exists in gsd-agent-modes and is deleted from pi-coding-agent"
    - "modes/print-mode.ts (or modes/print/print-mode.ts) exists in gsd-agent-modes and is deleted from pi-coding-agent"
    - "npm run build:pi-coding-agent && npm run build:agent-modes exits 0 after every sub-batch"
  artifacts:
    - path: "packages/pi-coding-agent/src/core/thinking-level.ts"
      provides: "isValidThinkingLevel and VALID_THINKING_LEVELS extracted from args.ts"
    - path: "packages/pi-coding-agent/src/core/theme/theme.ts"
      provides: "Theme infrastructure relocated from modes/interactive/theme/"
    - path: "packages/gsd-agent-modes/src/cli/args.ts"
      provides: "CLI argument parsing extracted to agent-modes"
    - path: "packages/gsd-agent-modes/src/modes/shared/command-context-actions.ts"
      provides: "Shared mode utilities extracted"
  key_links:
    - from: "packages/pi-coding-agent/src/core/model-resolver.ts"
      to: "packages/pi-coding-agent/src/core/thinking-level.ts"
      via: "import { isValidThinkingLevel }"
      pattern: "from.*thinking-level"
    - from: "packages/gsd-agent-modes/src/cli/args.ts"
      to: "@gsd/pi-coding-agent"
      via: "package import for APP_NAME, CONFIG_DIR_NAME, etc."
      pattern: "from.*@gsd/pi-coding-agent"
---

<objective>
Prepare the codebase for extraction (two blockers), then extract cli/, modes/shared/, and modes/print/ from pi-coding-agent into @gsd/agent-modes.

Purpose: Unblock all subsequent extraction batches by resolving the isValidThinkingLevel cross-dependency and the theme/ circular dependency. Then move the three simplest sub-batches (cli/, shared/, print/) to establish the extraction pattern.

Output: 8 files moved to @gsd/agent-modes, 2 prep tasks complete, build green after each sub-batch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gsd-agent-modes-extraction/03-RESEARCH.md
@.planning/phases/03-gsd-agent-modes-extraction/03-CONTEXT.md

@packages/pi-coding-agent/src/index.ts
@packages/pi-coding-agent/src/cli/args.ts
@packages/pi-coding-agent/src/core/model-resolver.ts
@packages/pi-coding-agent/src/modes/interactive/theme/theme.ts
@packages/pi-coding-agent/src/modes/interactive/theme/themes.ts
@packages/pi-coding-agent/src/core/resource-loader.ts
@packages/pi-coding-agent/src/core/extensions/runner.ts
@packages/pi-coding-agent/src/core/extensions/types.ts
@packages/pi-coding-agent/src/core/export-html/index.ts
@packages/pi-coding-agent/src/core/export-html/tool-renderer.ts
@packages/pi-coding-agent/src/modes/shared/command-context-actions.ts
@packages/pi-coding-agent/src/modes/print-mode.ts
@packages/pi-coding-agent/src/cli/config-selector.ts
@packages/pi-coding-agent/src/cli/file-processor.ts
@packages/pi-coding-agent/src/cli/list-models.ts
@packages/pi-coding-agent/src/cli/session-picker.ts
@packages/gsd-agent-modes/tsconfig.json
@packages/gsd-agent-modes/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: PREP-A and PREP-B — Extract isValidThinkingLevel and relocate theme/</name>
  <files>
    packages/pi-coding-agent/src/core/thinking-level.ts
    packages/pi-coding-agent/src/core/model-resolver.ts
    packages/pi-coding-agent/src/cli/args.ts
    packages/pi-coding-agent/src/core/theme/theme.ts
    packages/pi-coding-agent/src/core/theme/themes.ts
    packages/pi-coding-agent/src/modes/interactive/theme/theme.ts
    packages/pi-coding-agent/src/modes/interactive/theme/themes.ts
    packages/pi-coding-agent/src/core/resource-loader.ts
    packages/pi-coding-agent/src/core/extensions/runner.ts
    packages/pi-coding-agent/src/core/extensions/types.ts
    packages/pi-coding-agent/src/core/export-html/index.ts
    packages/pi-coding-agent/src/core/export-html/tool-renderer.ts
    packages/pi-coding-agent/src/index.ts
  </files>
  <action>
Two preparation tasks that MUST complete before any file moves. Do them in order.

**PREP-A: Extract isValidThinkingLevel from cli/args.ts to core/thinking-level.ts**

1. Read `packages/pi-coding-agent/src/cli/args.ts`. Find the `isValidThinkingLevel` function and `VALID_THINKING_LEVELS` constant.
2. Create `packages/pi-coding-agent/src/core/thinking-level.ts` containing:
   - The `VALID_THINKING_LEVELS` constant (copy the exact array/type)
   - The `isValidThinkingLevel` function (copy exactly)
   - Export both as named exports
3. Update `packages/pi-coding-agent/src/core/model-resolver.ts`:
   - Change `import { isValidThinkingLevel } from "../cli/args.js"` to `import { isValidThinkingLevel } from "./thinking-level.js"`
4. Update `packages/pi-coding-agent/src/cli/args.ts`:
   - Remove the `isValidThinkingLevel` function body and `VALID_THINKING_LEVELS` constant
   - Add `import { isValidThinkingLevel, VALID_THINKING_LEVELS } from "../core/thinking-level.js"` and re-export them so existing consumers are not broken: `export { isValidThinkingLevel, VALID_THINKING_LEVELS };`
5. Run: `npm run build:pi-coding-agent` — must exit 0.

**PREP-B: Relocate modes/interactive/theme/ to core/theme/**

1. Create directory `packages/pi-coding-agent/src/core/theme/`
2. Move `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts` to `packages/pi-coding-agent/src/core/theme/theme.ts`
3. Move `packages/pi-coding-agent/src/modes/interactive/theme/themes.ts` to `packages/pi-coding-agent/src/core/theme/themes.ts`
4. In `core/theme/theme.ts`: update any relative import of `themes.ts` (should be `./themes.js` — unchanged since both files move together)
5. Update all 5 core files that imported from `../modes/interactive/theme/theme.js` or `../../modes/interactive/theme/theme.js`:
   - `core/resource-loader.ts` line ~6: change `"../modes/interactive/theme/theme.js"` to `"./theme/theme.js"`
   - `core/extensions/runner.ts` line ~8: change `"../../modes/interactive/theme/theme.js"` to `"../theme/theme.js"`
   - `core/extensions/types.ts` line ~41: change `"../../modes/interactive/theme/theme.js"` to `"../theme/theme.js"`
   - `core/export-html/index.ts` line ~5: change `"../../modes/interactive/theme/theme.js"` to `"../theme/theme.js"`
   - `core/export-html/tool-renderer.ts` line ~9: change `"../../modes/interactive/theme/theme.js"` to `"../theme/theme.js"`
6. Update `packages/pi-coding-agent/src/index.ts`: find the existing theme re-export line (currently pointing to `./modes/interactive/theme/theme.js`) and change it to `./core/theme/theme.js`
7. Check if any files in `modes/interactive/` import from `./theme/theme.js` or `../theme/theme.js` — these stay as-is for now (they will be updated to `@gsd/pi-coding-agent` when those files move in Plan 03-02)
8. Delete the now-empty `packages/pi-coding-agent/src/modes/interactive/theme/` directory
9. Run: `npm run build:pi-coding-agent` — must exit 0.

Commit both PREP tasks together: `refactor(03-01): extract isValidThinkingLevel to core/ and relocate theme/ to core/theme/`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && npm run build:pi-coding-agent</automated>
  </verify>
  <done>
    - packages/pi-coding-agent/src/core/thinking-level.ts exists with isValidThinkingLevel and VALID_THINKING_LEVELS
    - packages/pi-coding-agent/src/core/model-resolver.ts imports from ./thinking-level.js (not ../cli/args.js)
    - packages/pi-coding-agent/src/core/theme/theme.ts and themes.ts exist
    - packages/pi-coding-agent/src/modes/interactive/theme/ directory no longer exists
    - All 5 core/ files import theme from core/theme/ path (not modes/interactive/theme/)
    - npm run build:pi-coding-agent exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract cli/ directory to @gsd/agent-modes (MODES-01)</name>
  <files>
    packages/pi-coding-agent/src/index.ts
    packages/gsd-agent-modes/src/cli/args.ts
    packages/gsd-agent-modes/src/cli/config-selector.ts
    packages/gsd-agent-modes/src/cli/file-processor.ts
    packages/gsd-agent-modes/src/cli/list-models.ts
    packages/gsd-agent-modes/src/cli/session-picker.ts
  </files>
  <action>
**Step 1: Expand pi-coding-agent public API (BEFORE moving files)**

Add these missing exports to `packages/pi-coding-agent/src/index.ts` (per D-03 cross-package import strategy — moved files import from @gsd/pi-coding-agent):

```typescript
// config.js exports needed by cli/ files
export { APP_NAME, CONFIG_DIR_NAME, ENV_AGENT_DIR } from "./config.js";

// core/tools/ exports needed by cli/args.ts
export { allTools } from "./core/tools/index.js";
export type { ToolName } from "./core/tools/index.js";

// utils/mime.js needed by cli/file-processor.ts
export { detectSupportedImageMimeTypeFromFile } from "./utils/mime.js";
```

Verify these don't conflict with existing exports. Place them near related existing exports (e.g., near `getAgentDir`, `VERSION` for the config ones).

Run: `npm run build:pi-coding-agent` — must exit 0 after API expansion.

**Step 2: Copy cli/ files to @gsd/agent-modes**

```bash
mkdir -p packages/gsd-agent-modes/src/cli
cp packages/pi-coding-agent/src/cli/args.ts packages/gsd-agent-modes/src/cli/
cp packages/pi-coding-agent/src/cli/config-selector.ts packages/gsd-agent-modes/src/cli/
cp packages/pi-coding-agent/src/cli/file-processor.ts packages/gsd-agent-modes/src/cli/
cp packages/pi-coding-agent/src/cli/list-models.ts packages/gsd-agent-modes/src/cli/
cp packages/pi-coding-agent/src/cli/session-picker.ts packages/gsd-agent-modes/src/cli/
```

**Step 3: Update cross-package imports in each copied file**

For every file in `packages/gsd-agent-modes/src/cli/`:

- Replace `../config.js` imports with `@gsd/pi-coding-agent` (e.g., `APP_NAME`, `CONFIG_DIR_NAME`, `ENV_AGENT_DIR`)
- Replace `../core/*` imports with `@gsd/pi-coding-agent` (e.g., `KeybindingsManager`, `ModelRegistry`, `SessionManager`, `SettingsManager`, `ResolvedPaths`, etc.)
- Replace `../utils/mime.js` imports with `@gsd/pi-coding-agent`
- Keep `@gsd/pi-ai` imports unchanged (already a package import)
- Keep `@gsd/pi-tui` imports unchanged
- `args.ts`: The `isValidThinkingLevel`/`VALID_THINKING_LEVELS` re-export from `../core/thinking-level.js` becomes an import from `@gsd/pi-coding-agent` — add `isValidThinkingLevel` and `VALID_THINKING_LEVELS` to pi-coding-agent's index.ts exports if not already there (export from `./core/thinking-level.js`)
- `config-selector.ts`: imports `../modes/interactive/components/config-selector.js` — this file will also be in @gsd/agent-modes after Plan 03-02, but it has NOT moved yet. For now, use a relative path: `../modes/interactive/components/config-selector.js` (this path will resolve once interactive/ components move in Plan 03-02, so temporarily this import may cause a build error — check if config-selector.ts can be deferred. If it causes errors, keep it in pi-coding-agent for now and move it during Plan 03-02 interactive/ batch instead). IMPORTANT: Also check if config-selector.ts imports theme — after PREP-B, theme is at `@gsd/pi-coding-agent` via the package export.
- `session-picker.ts`: imports `../modes/interactive/components/session-selector.js` — same situation as config-selector.ts above.

CRITICAL: If `config-selector.ts` or `session-picker.ts` reference files still in pi-coding-agent's `modes/interactive/`, those relative paths will NOT resolve from `@gsd/agent-modes`. Two options:
  (a) Move them now and use `../modes/interactive/components/config-selector.js` as a relative path within agent-modes (this will fail until Plan 03-02 moves components/) — NOT viable.
  (b) Move them now and temporarily import from `@gsd/pi-coding-agent` — but those components are NOT exported from pi-coding-agent's index.ts and should not be (they are internal GSD components, not pi upstream).
  (c) **Best option:** Move `config-selector.ts` and `session-picker.ts` to agent-modes now, but add temporary re-exports of `ConfigSelectorComponent` and `SessionSelectorComponent` to `pi-coding-agent/src/index.ts`. Remove these temporary re-exports in Plan 03-02 when the components move. Mark them with a `// TEMPORARY — remove after interactive/ extraction (Plan 03-02)` comment.

**Step 4: Delete originals from pi-coding-agent**

```bash
rm packages/pi-coding-agent/src/cli/args.ts
rm packages/pi-coding-agent/src/cli/config-selector.ts
rm packages/pi-coding-agent/src/cli/file-processor.ts
rm packages/pi-coding-agent/src/cli/list-models.ts
rm packages/pi-coding-agent/src/cli/session-picker.ts
rmdir packages/pi-coding-agent/src/cli/
```

**Step 5: Build gate**

```bash
npm run build:pi-coding-agent && npm run build:agent-modes
```

Both must exit 0. If either fails, fix import paths before proceeding.

Commit: `refactor(03-01): extract cli/ directory to @gsd/agent-modes [MODES-01]`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && npm run build:pi-coding-agent && npm run build:agent-modes</automated>
  </verify>
  <done>
    - packages/gsd-agent-modes/src/cli/ contains args.ts, config-selector.ts, file-processor.ts, list-models.ts, session-picker.ts
    - packages/pi-coding-agent/src/cli/ directory no longer exists
    - All cross-package imports in moved files use @gsd/pi-coding-agent (not relative paths to core/)
    - npm run build:pi-coding-agent && npm run build:agent-modes both exit 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract modes/shared/ and modes/print/ to @gsd/agent-modes (MODES-02, MODES-03)</name>
  <files>
    packages/gsd-agent-modes/src/modes/shared/command-context-actions.ts
    packages/gsd-agent-modes/src/modes/print-mode.ts
    packages/pi-coding-agent/src/modes/shared/command-context-actions.ts
    packages/pi-coding-agent/src/modes/print-mode.ts
  </files>
  <action>
**Sub-batch A: Extract modes/shared/ (MODES-02)**

1. Create directory: `packages/gsd-agent-modes/src/modes/shared/`
2. Copy: `packages/pi-coding-agent/src/modes/shared/command-context-actions.ts` to `packages/gsd-agent-modes/src/modes/shared/command-context-actions.ts`
3. Update imports in the copied file:
   - `../../core/agent-session.js` import (type AgentSession) becomes `import type { AgentSession } from "@gsd/pi-coding-agent"`
   - `../../core/extensions/index.js` import (type ExtensionCommandContextActions) becomes `import type { ExtensionCommandContextActions } from "@gsd/pi-coding-agent"`
   - Both symbols are ALREADY in pi-coding-agent/src/index.ts — no API expansion needed.
4. Delete original: `rm packages/pi-coding-agent/src/modes/shared/command-context-actions.ts && rmdir packages/pi-coding-agent/src/modes/shared/`
5. Build gate: `npm run build:pi-coding-agent && npm run build:agent-modes` — both exit 0.

**Sub-batch B: Extract modes/print/ (MODES-03)**

Note: `print-mode.ts` lives directly in `packages/pi-coding-agent/src/modes/print-mode.ts` (not in a print/ subdirectory). Per the locked decision to mirror directory structure, place it at `packages/gsd-agent-modes/src/modes/print-mode.ts` (same relative location).

1. Copy: `packages/pi-coding-agent/src/modes/print-mode.ts` to `packages/gsd-agent-modes/src/modes/print-mode.ts`
2. Update imports in the copied file:
   - `../core/agent-session.js` becomes `import type { AgentSession } from "@gsd/pi-coding-agent"`
   - `../core/extensions/index.js` becomes `import type { ExtensionCommandContextActions } from "@gsd/pi-coding-agent"` (or similar — read the actual imports)
   - `./shared/command-context-actions.js` — this file is now in the SAME package (@gsd/agent-modes) at `./shared/command-context-actions.js`. Keep as relative import.
   - `@gsd/pi-ai` imports stay unchanged.
3. Delete original: `rm packages/pi-coding-agent/src/modes/print-mode.ts`
4. Build gate: `npm run build:pi-coding-agent && npm run build:agent-modes` — both exit 0.

Commit: `refactor(03-01): extract modes/shared/ and modes/print/ to @gsd/agent-modes [MODES-02, MODES-03]`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && npm run build:pi-coding-agent && npm run build:agent-modes</automated>
  </verify>
  <done>
    - packages/gsd-agent-modes/src/modes/shared/command-context-actions.ts exists
    - packages/gsd-agent-modes/src/modes/print-mode.ts exists
    - packages/pi-coding-agent/src/modes/shared/ directory no longer exists
    - packages/pi-coding-agent/src/modes/print-mode.ts no longer exists
    - Cross-package imports use @gsd/pi-coding-agent, intra-package imports use relative paths
    - npm run build:pi-coding-agent && npm run build:agent-modes both exit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan moves existing TypeScript source files between packages within the same workspace. No new network endpoints, auth flows, user input handling, or data storage.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | pi-coding-agent/src/index.ts expanded API | accept | New re-exports are utility/config symbols already used across the codebase. No secrets or internal-only logic exposed. |
</threat_model>

<verification>
After all 3 tasks complete:
```bash
# Build gate
npm run build:pi-coding-agent && npm run build:agent-modes

# Verify cli/ files moved
test -d packages/gsd-agent-modes/src/cli && echo "cli/ exists in agent-modes"
test ! -d packages/pi-coding-agent/src/cli && echo "cli/ removed from pi-coding-agent"

# Verify shared/ and print moved
test -f packages/gsd-agent-modes/src/modes/shared/command-context-actions.ts && echo "shared/ exists"
test -f packages/gsd-agent-modes/src/modes/print-mode.ts && echo "print-mode exists"

# Verify prep tasks
test -f packages/pi-coding-agent/src/core/thinking-level.ts && echo "PREP-A done"
test -f packages/pi-coding-agent/src/core/theme/theme.ts && echo "PREP-B done"
test ! -d packages/pi-coding-agent/src/modes/interactive/theme && echo "old theme/ gone"
```
</verification>

<success_criteria>
- PREP-A complete: isValidThinkingLevel in core/thinking-level.ts, model-resolver.ts updated
- PREP-B complete: theme/ at core/theme/, all 5 core importers updated, old theme/ deleted
- MODES-01 satisfied: all 5 cli/ files in @gsd/agent-modes, none in pi-coding-agent
- MODES-02 satisfied: modes/shared/ in @gsd/agent-modes
- MODES-03 satisfied: print-mode.ts in @gsd/agent-modes
- Build green after every sub-batch: npm run build:pi-coding-agent && npm run build:agent-modes
</success_criteria>

<output>
After completion, create `.planning/phases/03-gsd-agent-modes-extraction/03-01-SUMMARY.md`
</output>
