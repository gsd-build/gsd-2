---
phase: 02-package-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gsd-agent-core/package.json
  - packages/gsd-agent-core/tsconfig.json
  - packages/gsd-agent-core/src/index.ts
  - packages/gsd-agent-modes/package.json
  - packages/gsd-agent-modes/tsconfig.json
  - packages/gsd-agent-modes/src/index.ts
autonomous: true
requirements: [SCAF-01, SCAF-02]

must_haves:
  truths:
    - "packages/gsd-agent-core/ exists with valid package.json, tsconfig.json, and src/index.ts"
    - "packages/gsd-agent-modes/ exists with valid package.json, tsconfig.json, and src/index.ts"
    - "tsc -p tsconfig.json compiles successfully in each new package"
    - "npm workspace resolves both new packages"
  artifacts:
    - path: "packages/gsd-agent-core/package.json"
      provides: "@gsd/agent-core package manifest"
      contains: '"name": "@gsd/agent-core"'
    - path: "packages/gsd-agent-core/tsconfig.json"
      provides: "TypeScript compilation config for agent-core"
      contains: '"module": "Node16"'
    - path: "packages/gsd-agent-core/src/index.ts"
      provides: "Empty module placeholder"
      contains: "export {}"
    - path: "packages/gsd-agent-modes/package.json"
      provides: "@gsd/agent-modes package manifest"
      contains: '"name": "@gsd/agent-modes"'
    - path: "packages/gsd-agent-modes/tsconfig.json"
      provides: "TypeScript compilation config for agent-modes"
      contains: '"module": "Node16"'
    - path: "packages/gsd-agent-modes/src/index.ts"
      provides: "Empty module placeholder"
      contains: "export {}"
  key_links:
    - from: "packages/gsd-agent-core/package.json"
      to: "@gsd/pi-coding-agent, @gsd/pi-agent-core, @gsd/pi-ai"
      via: "dependencies field"
      pattern: '"@gsd/pi-coding-agent"'
    - from: "packages/gsd-agent-modes/package.json"
      to: "@gsd/agent-core, @gsd/pi-coding-agent, @gsd/pi-tui"
      via: "dependencies field"
      pattern: '"@gsd/agent-core"'
---

<objective>
Create both new workspace packages (`@gsd/agent-core` and `@gsd/agent-modes`) with correct manifests, TypeScript configs, and empty entry points.

Purpose: Establish the two GSD-owned packages that will receive extracted files in Phases 3 and 4. Both must compile cleanly before any build chain wiring occurs.
Output: Two new package directories under `packages/` that are valid npm workspace members and compile with `tsc`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-scaffolding/02-CONTEXT.md
@.planning/phases/02-package-scaffolding/02-RESEARCH.md
@.planning/phases/02-package-scaffolding/02-PATTERNS.md

<interfaces>
<!-- Template package.json from pi-agent-core (the analog for both new packages) -->
From packages/pi-agent-core/package.json:
```json
{
  "name": "@gsd/pi-agent-core",
  "version": "2.74.0",
  "description": "General-purpose agent core (vendored from pi-mono)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {}
}
```

From packages/pi-agent-core/tsconfig.json:
```json
{
  "compilerOptions": {
    "target": "ES2024",
    "module": "Node16",
    "lib": ["ES2024"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "incremental": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "inlineSources": true,
    "inlineSourceMap": false,
    "moduleResolution": "Node16",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": false,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "useDefineForClassFields": false,
    "types": ["node"],
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "**/*.d.ts", "src/**/*.d.ts", "src/**/*.test.ts"]
}
```

Root package.json workspace config (line 15-18):
```json
"workspaces": [
  "packages/*",
  "studio"
]
```
(New directories under packages/ are automatically workspace members -- no root edit needed.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @gsd/agent-core package (SCAF-01)</name>
  <files>packages/gsd-agent-core/package.json, packages/gsd-agent-core/tsconfig.json, packages/gsd-agent-core/src/index.ts</files>
  <read_first>
    packages/pi-agent-core/package.json
    packages/pi-agent-core/tsconfig.json
  </read_first>
  <action>
Create directory `packages/gsd-agent-core/src/`.

Create `packages/gsd-agent-core/package.json` with this exact content (per D-08, D-09, D-10):
```json
{
  "name": "@gsd/agent-core",
  "version": "1.0.0",
  "description": "GSD session orchestration layer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@gsd/pi-coding-agent": "*",
    "@gsd/pi-agent-core": "*",
    "@gsd/pi-ai": "*"
  }
}
```

Create `packages/gsd-agent-core/tsconfig.json` as a verbatim copy of `packages/pi-agent-core/tsconfig.json` (per D-01). The exact content is:
```json
{
  "compilerOptions": {
    "target": "ES2024",
    "module": "Node16",
    "lib": ["ES2024"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "incremental": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "inlineSources": true,
    "inlineSourceMap": false,
    "moduleResolution": "Node16",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": false,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "useDefineForClassFields": false,
    "types": ["node"],
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "**/*.d.ts", "src/**/*.d.ts", "src/**/*.test.ts"]
}
```

NOTE: D-01 prose says "no legacy decorator options" but the actual template file DOES include `experimentalDecorators`, `emitDecoratorMetadata`, `useDefineForClassFields`. The file is authoritative over the prose. Copy verbatim.

Create `packages/gsd-agent-core/src/index.ts` with this exact content (per D-08):
```typescript
export {}
```

After creating all three files, compile the package:
```bash
cd packages/gsd-agent-core && npx tsc -p tsconfig.json
```

Verify `dist/index.js` and `dist/index.d.ts` exist after compilation.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2/packages/gsd-agent-core && npx tsc -p tsconfig.json && test -f dist/index.js && test -f dist/index.d.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/gsd-agent-core/package.json contains `"name": "@gsd/agent-core"`
    - packages/gsd-agent-core/package.json contains `"version": "1.0.0"`
    - packages/gsd-agent-core/package.json contains `"type": "module"`
    - packages/gsd-agent-core/package.json contains `"@gsd/pi-coding-agent": "*"`
    - packages/gsd-agent-core/package.json contains `"@gsd/pi-agent-core": "*"`
    - packages/gsd-agent-core/package.json contains `"@gsd/pi-ai": "*"`
    - packages/gsd-agent-core/package.json does NOT contain `piConfig`
    - packages/gsd-agent-core/tsconfig.json contains `"module": "Node16"`
    - packages/gsd-agent-core/tsconfig.json contains `"experimentalDecorators": true`
    - packages/gsd-agent-core/tsconfig.json contains `"allowImportingTsExtensions": false`
    - packages/gsd-agent-core/tsconfig.json contains `"src/**/*.test.ts"` in exclude array
    - packages/gsd-agent-core/src/index.ts contains `export {}`
    - `tsc -p tsconfig.json` in packages/gsd-agent-core exits 0
    - packages/gsd-agent-core/dist/index.js exists
    - packages/gsd-agent-core/dist/index.d.ts exists
  </acceptance_criteria>
  <done>@gsd/agent-core package exists with correct manifest, tsconfig, empty entry point, and compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create @gsd/agent-modes package (SCAF-02)</name>
  <files>packages/gsd-agent-modes/package.json, packages/gsd-agent-modes/tsconfig.json, packages/gsd-agent-modes/src/index.ts</files>
  <read_first>
    packages/pi-agent-core/package.json
    packages/pi-agent-core/tsconfig.json
  </read_first>
  <action>
Create directory `packages/gsd-agent-modes/src/`.

Create `packages/gsd-agent-modes/package.json` with this exact content (per D-08, D-09, D-10):
```json
{
  "name": "@gsd/agent-modes",
  "version": "1.0.0",
  "description": "GSD run-mode and CLI layer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@gsd/agent-core": "*",
    "@gsd/pi-coding-agent": "*",
    "@gsd/pi-tui": "*"
  }
}
```

Create `packages/gsd-agent-modes/tsconfig.json` as a verbatim copy of `packages/pi-agent-core/tsconfig.json` (per D-01). Identical to gsd-agent-core/tsconfig.json:
```json
{
  "compilerOptions": {
    "target": "ES2024",
    "module": "Node16",
    "lib": ["ES2024"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "incremental": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "inlineSources": true,
    "inlineSourceMap": false,
    "moduleResolution": "Node16",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": false,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "useDefineForClassFields": false,
    "types": ["node"],
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "**/*.d.ts", "src/**/*.d.ts", "src/**/*.test.ts"]
}
```

Create `packages/gsd-agent-modes/src/index.ts` with this exact content (per D-08):
```typescript
export {}
```

After creating all three files, compile the package:
```bash
cd packages/gsd-agent-modes && npx tsc -p tsconfig.json
```

Verify `dist/index.js` and `dist/index.d.ts` exist after compilation.

After both packages are created and compiled, run `tsc --noEmit` from workspace root to confirm no workspace-level type errors:
```bash
cd /Users/trekkie/projects/gsd-2 && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2/packages/gsd-agent-modes && npx tsc -p tsconfig.json && test -f dist/index.js && test -f dist/index.d.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/gsd-agent-modes/package.json contains `"name": "@gsd/agent-modes"`
    - packages/gsd-agent-modes/package.json contains `"version": "1.0.0"`
    - packages/gsd-agent-modes/package.json contains `"type": "module"`
    - packages/gsd-agent-modes/package.json contains `"@gsd/agent-core": "*"`
    - packages/gsd-agent-modes/package.json contains `"@gsd/pi-coding-agent": "*"`
    - packages/gsd-agent-modes/package.json contains `"@gsd/pi-tui": "*"`
    - packages/gsd-agent-modes/package.json does NOT contain `piConfig`
    - packages/gsd-agent-modes/tsconfig.json contains `"module": "Node16"`
    - packages/gsd-agent-modes/tsconfig.json contains `"experimentalDecorators": true`
    - packages/gsd-agent-modes/tsconfig.json contains `"allowImportingTsExtensions": false`
    - packages/gsd-agent-modes/tsconfig.json contains `"src/**/*.test.ts"` in exclude array
    - packages/gsd-agent-modes/src/index.ts contains `export {}`
    - `tsc -p tsconfig.json` in packages/gsd-agent-modes exits 0
    - packages/gsd-agent-modes/dist/index.js exists
    - packages/gsd-agent-modes/dist/index.d.ts exists
    - `tsc --noEmit` from workspace root exits 0
  </acceptance_criteria>
  <done>@gsd/agent-modes package exists with correct manifest, tsconfig, empty entry point, compiles cleanly, and workspace-level tsc --noEmit passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant attack surface -- scaffolding only. New packages contain `export {}` and compile-time configuration only. No runtime code, no API surfaces, no auth handling, no user input, no data storage.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | package.json dependencies | accept | Dependencies use `"*"` which resolves within npm workspace only; no registry fetch risk in local dev |
</threat_model>

<verification>
1. `ls packages/gsd-agent-core/package.json packages/gsd-agent-core/tsconfig.json packages/gsd-agent-core/src/index.ts` -- all exist
2. `ls packages/gsd-agent-modes/package.json packages/gsd-agent-modes/tsconfig.json packages/gsd-agent-modes/src/index.ts` -- all exist
3. `cd packages/gsd-agent-core && tsc -p tsconfig.json` -- exits 0
4. `cd packages/gsd-agent-modes && tsc -p tsconfig.json` -- exits 0
5. `tsc --noEmit` from workspace root -- exits 0
</verification>

<success_criteria>
Both `packages/gsd-agent-core/` and `packages/gsd-agent-modes/` exist with valid package.json, tsconfig.json, and compiled dist/ output. Workspace-level `tsc --noEmit` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-scaffolding/02-01-SUMMARY.md`
</output>
