---
phase: 17-permission-model
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified: []
autonomous: false
requirements:
  - PERM-01
  - PERM-02
  - PERM-03
  - PERM-04

must_haves:
  truths:
    - "Settings panel has no Skip permissions toggle — shows Manage build permissions link"
    - "TrustDialog shows plain-language content and CTA"
    - "Confirming TrustDialog writes .gsd/.mission-control-trust and hides the dialog"
    - "BOUNDARY_VIOLATION event from pipeline renders a red banner in the UI"
    - "AdvancedPermissionsPanel shows 6 permission rows including locked file-operations row and amber debug warning"
  artifacts:
    - path: "packages/mission-control/src/components/permissions/TrustDialog.tsx"
      provides: "Trust dialog modal"
    - path: "packages/mission-control/src/components/permissions/AdvancedPermissionsPanel.tsx"
      provides: "Advanced permission toggles"
    - path: "packages/mission-control/src/server/boundary-enforcer.ts"
      provides: "Boundary detection pure function"
    - path: "packages/mission-control/src/server/trust-api.ts"
      provides: "Trust flag read/write"
  key_links:
    - from: "App.tsx"
      to: "TrustDialog"
      via: "trustStatus === needs_trust conditional render"
      pattern: "needs_trust"
    - from: "pipeline.ts"
      to: "boundary-enforcer.ts"
      via: "detectBoundaryViolation call in wireSessionEvents"
      pattern: "detectBoundaryViolation"
---

<objective>
Human verification of all Phase 17 permission model work.

Purpose: Confirm the four PERM requirements are visually and functionally correct before closing the phase.

Output: User confirms SC-1 through SC-4 pass; phase marked complete.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-permission-model/17-01-SUMMARY.md
@.planning/phases/17-permission-model/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build verification</name>
  <files></files>
  <action>
    Run all tests to confirm no regressions:
    ```
    cd packages/mission-control && bun test --timeout 30000 2>&1 | tail -20
    ```

    Also run the Phase 17 specific tests:
    ```
    bun test tests/trust-api.test.ts tests/trust-dialog.test.tsx tests/boundary-enforcer.test.ts --timeout 10000
    ```

    Run frontend build:
    ```
    bun build src/frontend.tsx --outdir /tmp/phase17-final-build --target browser
    ```

    Report: total tests, pass/fail count, any new failures vs Phase 16 baseline (698 tests).
    If any Phase 17 tests fail, fix before proceeding to human verification.
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test --timeout 30000 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass (including new Phase 17 tests); no regressions from Phase 16 baseline</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 17-01: TrustDialog, AdvancedPermissionsPanel, trust-api.ts, SettingsView updated
    Plan 17-02: boundary-enforcer.ts, pipeline wired, App.tsx trust check, BOUNDARY_VIOLATION banner
  </what-built>
  <how-to-verify>
    Start the dev server: cd packages/mission-control && bun dev

    SC-1 — Settings has no Skip permissions toggle:
    1. Open http://localhost:4000
    2. Click Settings tab
    3. Confirm NO toggle labeled "Skip permissions" or "--dangerously-skip-permissions" appears
    4. Confirm a "Build Permissions" section exists with a "Manage build permissions →" link
    EXPECTED: Link visible; no raw skip-permissions toggle anywhere in Settings

    SC-2 — Trust dialog shown once per new project:
    1. If .gsd/.mission-control-trust exists in the current project, DELETE it: rm .gsd/.mission-control-trust
    2. Reload http://localhost:4000
    3. Confirm TrustDialog appears BEFORE the main UI — overlay with "Before we start building" heading
    4. Confirm ✓ and ✗ lists are visible in plain language
    5. Click "I understand, start building"
    6. Confirm dialog disappears and AppShell loads
    7. Confirm .gsd/.mission-control-trust file now exists: ls .gsd/.mission-control-trust
    8. Reload the page again — confirm dialog does NOT appear a second time
    EXPECTED: Dialog shown once; trust flag written; never re-shown after

    SC-3 — BOUNDARY_VIOLATION banner (simulated):
    Since we cannot trigger a real gsd out-of-project write during verification, simulate via browser DevTools:
    1. Open http://localhost:4000 (main UI visible)
    2. Open browser DevTools Console
    3. Run: (new WebSocket("ws://localhost:4001")) — then inspect network, OR
       Trigger via a direct WebSocket message (DevTools → Network → WS tab, find the connection,
       send message: { "type": "chat", "prompt": "test" })
    Alternative simpler test:
    1. Open DevTools → Console
    2. Inject a fake boundary_violation into the app's existing WebSocket:
       The app's ws is accessible if you can find the reference. Since this is complex, instead:
       Verify the AppShell source contains the boundary_violation handler (grep/view source)
       AND verify the red banner JSX is present in AppShell.tsx source.
    3. OR: grep the built bundle: grep -l "boundary_violation" /tmp/phase17-final-build/*.js
    EXPECTED: "boundary_violation" string present in compiled bundle; banner code is present

    SC-4 — Advanced permission settings panel:
    1. Open Settings → "Build Permissions" section
    2. Click "Manage build permissions →"
    3. Confirm AdvancedPermissionsPanel appears inline with:
       - "File operations inside project" (locked row, cannot be toggled, shows "Always on" in green)
       - "Install packages automatically" toggle (default ON)
       - "Run shell and build commands" toggle (default ON)
       - "Create git checkpoints automatically" toggle (default ON)
       - "Push to remote git" toggle (default OFF)
       - "Ask before each operation" toggle (default OFF)
    4. Toggle "Ask before each operation" ON — confirm amber warning appears:
       "This will pause GSD after every file operation..."
    EXPECTED: All 6 rows present; amber warning shown when debug mode enabled
  </how-to-verify>
  <resume-signal>Type "approved" if SC-1 through SC-4 pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four PERM requirements verified by human:
- PERM-01: Settings has "Manage build permissions →" link, no raw skip-permissions toggle
- PERM-02: TrustDialog shown once; .gsd/.mission-control-trust written on confirm
- PERM-03: BOUNDARY_VIOLATION event wired in pipeline; banner in AppShell
- PERM-04: AdvancedPermissionsPanel with 6 rows and amber debug warning
</verification>

<success_criteria>
Human approves SC-1 through SC-4:
1. SC-1: No skip-permissions toggle in Settings; "Manage build permissions →" present
2. SC-2: TrustDialog shown once per project; .gsd/.mission-control-trust written; never re-shown
3. SC-3: BOUNDARY_VIOLATION event is wired (verified via source or bundle grep)
4. SC-4: AdvancedPermissionsPanel shows all 6 rows; debug mode shows amber warning
</success_criteria>

<output>
After completion, create `.planning/phases/17-permission-model/17-03-SUMMARY.md`
</output>
