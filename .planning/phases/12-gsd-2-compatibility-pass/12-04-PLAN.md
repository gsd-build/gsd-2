---
phase: 12-gsd-2-compatibility-pass
plan: "04"
type: execute
wave: 3
depends_on:
  - "12-03"
files_modified:
  - packages/mission-control/src/lib/slash-commands.ts
  - packages/mission-control/src/components/views/ChatView.tsx
  - packages/mission-control/src/components/MigrationBanner.tsx
autonomous: true
requirements:
  - COMPAT-04
  - COMPAT-06

must_haves:
  truths:
    - "Autocomplete shows exactly 9 GSD 2 /gsd commands — no /gsd: v1 entries visible in UI"
    - "Command palette GSD entries use /gsd subcommand syntax"
    - "A v1 project (has .planning/, no .gsd/) shows the migration banner in ChatView"
    - "Migration banner Run migration button sends /gsd migrate to the active session"
    - "slash-commands.test.ts GSD 2 registry cases are GREEN"
    - "migration-banner.test.ts UI cases are GREEN"
  artifacts:
    - path: "packages/mission-control/src/lib/slash-commands.ts"
      provides: "GSD_COMMANDS array with 9 GSD 2 entries only"
      contains: "/gsd auto"
    - path: "packages/mission-control/src/components/MigrationBanner.tsx"
      provides: "Inline banner component for v1 projects"
      exports:
        - "MigrationBanner"
      contains: "needsMigration"
    - path: "packages/mission-control/src/components/views/ChatView.tsx"
      provides: "MigrationBanner wired at top of chat area when state.needsMigration"
      contains: "MigrationBanner"
  key_links:
    - from: "src/components/views/ChatView.tsx"
      to: "src/components/MigrationBanner.tsx"
      via: "import MigrationBanner"
      pattern: "MigrationBanner"
    - from: "src/components/MigrationBanner.tsx"
      to: "sendMessage('/gsd migrate')"
      via: "Run migration button onClick"
      pattern: "gsd migrate"
    - from: "src/lib/slash-commands.ts"
      to: "ChatInput autocomplete"
      via: "GSD_COMMANDS exported and consumed by ChatInput"
      pattern: "GSD_COMMANDS"
---

<objective>
Wave 3a: Replace the v1 slash command registry with GSD 2 syntax and add the migration banner component.

Purpose: Users typing `/gsd` must see GSD 2 subcommands, not the old `/gsd:discuss-phase` v1 syntax. V1 project owners must see a clear migration prompt when they open their project.
Output: `slash-commands.ts` with 9 GSD 2 entries, `MigrationBanner.tsx` new component, `ChatView.tsx` wired.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-gsd-2-compatibility-pass/12-CONTEXT.md
@.planning/phases/12-gsd-2-compatibility-pass/12-RESEARCH.md
@.planning/phases/12-gsd-2-compatibility-pass/12-03-SUMMARY.md

<interfaces>
<!-- Exact GSD 2 command registry from CONTEXT.md (locked decision) -->
GSD_COMMANDS must be EXACTLY these 9 entries:
  /gsd              Guided mode — reads project state, shows what's next
  /gsd auto         Autonomous mode — research, plan, execute, commit, repeat
  /gsd stop         Stop auto mode gracefully
  /gsd discuss      Discuss architecture and decisions
  /gsd status       Progress dashboard
  /gsd queue        Queue future milestones
  /gsd prefs        Model selection, timeouts, budget ceiling
  /gsd migrate      Migrate v1 .planning/ directory to .gsd/ format
  /gsd doctor       Validate .gsd/ integrity, find and fix issues

CLAUDE_CODE_COMMANDS — DO NOT CHANGE. These are native /help, /clear, /model etc. Not GSD v1 commands.

Design tokens (from CONTEXT.md — do not deviate):
  Background: #0F1419   Surface: #131C2B   Elevated: #1A2332   Border: #1E2D3D
  Amber: #F59E0B (banner warning color)   Accent cyan: #5BC8F0   Green: #22C55E

Migration banner placement: top of ChatView chat area (above message list), not in header or sidebar.
Banner is dismissible by the user (close button) even without running migration.
Banner sends /gsd migrate as a chat message via the same sendMessage() function ChatInput uses.

GSD2State.needsMigration: boolean — set to true when .planning/ exists without .gsd/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace GSD_COMMANDS with 9 GSD 2 entries in slash-commands.ts</name>
  <files>
    packages/mission-control/src/lib/slash-commands.ts
  </files>
  <behavior>
    - GSD_COMMANDS.length === 9
    - Every entry has command starting with "/gsd" (no colon)
    - No entry has command starting with "/gsd:" (v1 colon syntax)
    - CLAUDE_CODE_COMMANDS array is untouched
    - filterCommands(input) works correctly for space-separated subcommands:
      - filterCommands("/gsd") returns all 9 entries (prefix match)
      - filterCommands("/gsd a") returns /gsd auto only
      - filterCommands("/gsd s") returns /gsd stop and /gsd status
    - slash-commands.test.ts GSD 2 registry assertions are GREEN
  </behavior>
  <action>
    Read packages/mission-control/src/lib/slash-commands.ts in full first.

    REWRITE just the GSD_COMMANDS array. Replace all 21 v1 entries with the 9 GSD 2 entries listed in the interfaces section above. Use source: "gsd" for all 9, args: "" for all.

    Update the module-level JSDoc comment. Change:
    - "1. GSD workflow commands (/gsd:*)" → "1. GSD 2 workflow commands (/gsd subcommands)"
    - Remove "Three sources: 1. GSD ... 2. Native Claude Code ... 3. User custom" if still accurate, or update to reflect new naming

    KEEP: CLAUDE_CODE_COMMANDS array unchanged. Keep the SlashCommand interface, filterCommands function, and any other exports.

    VERIFY the filterCommands function works with space-separated subcommands. The existing prefix-match `c.command.startsWith(input)` will correctly match "/gsd auto" when input is "/gsd " — this is correct behavior. If filterCommands currently trims or lowercases, check that "/gsd auto" still matches input "/gsd a". If not, a simple includes() or startsWith() fix may be needed.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "slash-commands" 2>&1 | tail -10</automated>
  </verify>
  <done>slash-commands.test.ts GSD 2 registry tests are GREEN. GSD_COMMANDS has 9 entries. No v1 /gsd: entries remain. filterCommands works for /gsd prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MigrationBanner.tsx and wire into ChatView</name>
  <files>
    packages/mission-control/src/components/MigrationBanner.tsx
    packages/mission-control/src/components/views/ChatView.tsx
  </files>
  <behavior>
    - MigrationBanner renders when shown=true: displays text "This project uses GSD v1. Run /gsd migrate to upgrade it."
    - MigrationBanner has a "Run migration" button — onClick calls onRunMigration prop
    - MigrationBanner has a dismiss button (×) — onClick calls onDismiss prop
    - MigrationBanner uses design tokens: amber #F59E0B border, surface #131C2B background
    - ChatView renders MigrationBanner at top of chat area when state?.needsMigration === true
    - ChatView passes onRunMigration={sendMessage.bind(null, '/gsd migrate')} to banner
    - ChatView tracks dismissed state locally (useState) — banner hidden after dismiss
    - migration-banner.test.ts UI rendering assertions turn GREEN
  </behavior>
  <action>
    CREATE packages/mission-control/src/components/MigrationBanner.tsx:

    ```tsx
    interface MigrationBannerProps {
      onRunMigration: () => void;
      onDismiss: () => void;
    }

    export function MigrationBanner({ onRunMigration, onDismiss }: MigrationBannerProps) {
      return (
        <div style={{ borderLeft: "3px solid #F59E0B", background: "#131C2B", padding: "12px 16px", ... }}>
          <span>This project uses GSD v1. Run /gsd migrate to upgrade it.</span>
          <button onClick={onRunMigration}>Run migration</button>
          <button onClick={onDismiss} aria-label="Dismiss">×</button>
        </div>
      );
    }
    ```

    Use Tailwind classes consistent with the existing design system rather than inline styles if the rest of ChatView uses Tailwind. Match the visual style of existing banners/alerts in the codebase.

    EDIT packages/mission-control/src/components/views/ChatView.tsx:
    1. Read ChatView.tsx in full first to understand its structure and how it receives state
    2. Add import: `import { MigrationBanner } from "../MigrationBanner"`
    3. Add local dismissed state: `const [migrationDismissed, setMigrationDismissed] = useState(false)`
    4. Find the sendMessage function or the prop that handles sending chat messages
    5. In the JSX, before the message list (or at the top of the chat content area), add:
       ```tsx
       {state?.needsMigration && !migrationDismissed && (
         <MigrationBanner
           onRunMigration={() => { sendMessage('/gsd migrate'); setMigrationDismissed(true); }}
           onDismiss={() => setMigrationDismissed(true)}
         />
       )}
       ```
    6. The `state` object here is the GSD2State received via WebSocket / usePlanningState hook.

    NOTE: If ChatView does not directly have access to `state.needsMigration` (e.g., it only receives session-level props), check how the state flows into ChatView — trace from usePlanningState to the component tree. Add needsMigration to the props or context as needed.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "migration-banner" 2>&1 | tail -10</automated>
  </verify>
  <done>migration-banner.test.ts UI cases GREEN. MigrationBanner.tsx exists and is importable. ChatView.tsx imports and conditionally renders MigrationBanner when needsMigration is true.</done>
</task>

</tasks>

<verification>
Slash command tests:
`cd packages/mission-control && bun test --testPathPattern "slash-commands" 2>&1 | tail -10`

Migration banner tests:
`cd packages/mission-control && bun test --testPathPattern "migration-banner" 2>&1 | tail -10`

Confirm no /gsd: entries remain in slash-commands.ts:
`grep -n "gsd:" packages/mission-control/src/lib/slash-commands.ts`
(should return 0 lines)
</verification>

<success_criteria>
- GSD_COMMANDS has exactly 9 entries with /gsd space-separated subcommand syntax
- No /gsd: colon-prefixed entries anywhere in slash-commands.ts
- CLAUDE_CODE_COMMANDS unchanged
- MigrationBanner.tsx created and exported
- ChatView.tsx renders MigrationBanner when state.needsMigration is true
- Both test files GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/12-gsd-2-compatibility-pass/12-04-SUMMARY.md`
</output>
