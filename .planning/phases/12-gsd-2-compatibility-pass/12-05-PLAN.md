---
phase: 12-gsd-2-compatibility-pass
plan: "05"
type: execute
wave: 3
depends_on:
  - "12-03"
files_modified:
  - packages/mission-control/src/components/views/SettingsView.tsx
  - packages/mission-control/src/server/settings-api.ts
autonomous: true
requirements:
  - COMPAT-07

must_haves:
  truths:
    - "Settings panel shows per-phase model selects (research, planning, execution, completion)"
    - "Settings panel shows budget ceiling numeric input"
    - "Settings panel shows skill discovery select (auto / suggest / off)"
    - "Settings panel has no Skip permissions toggle"
    - "Settings panel has no Allowed tools field"
    - "settings-view-gsd2.test.ts assertions are GREEN"
  artifacts:
    - path: "packages/mission-control/src/components/views/SettingsView.tsx"
      provides: "GSD 2 settings panel with per-phase models, budget ceiling, skill_discovery"
      contains: "budget_ceiling"
    - path: "packages/mission-control/src/server/settings-api.ts"
      provides: "Settings API reads/writes preferences.md — not config.json"
      contains: "preferences.md"
  key_links:
    - from: "src/components/views/SettingsView.tsx"
      to: "/api/settings"
      via: "fetch GET/POST to settings endpoint"
      pattern: "api/settings"
    - from: "src/server/settings-api.ts"
      to: ".gsd/preferences.md"
      via: "readFileText + gray-matter parse"
      pattern: "preferences.md"
---

<objective>
Wave 3b: Update SettingsView to show GSD 2 fields (per-phase models, budget ceiling, skill_discovery) and remove v1 Claude Code fields. Update settings-api.ts to read/write preferences.md instead of config.json.

Purpose: The Settings panel is the primary configuration surface. Users expect to see GSD 2 model selection and budget control — not v1 Claude Code toggles that no longer apply.
Output: SettingsView with GSD 2 fields, settings-api.ts reading preferences.md.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-gsd-2-compatibility-pass/12-CONTEXT.md
@.planning/phases/12-gsd-2-compatibility-pass/12-RESEARCH.md
@.planning/phases/12-gsd-2-compatibility-pass/12-03-SUMMARY.md

<interfaces>
<!-- GSD 2 settings fields from CONTEXT.md (locked decision) -->

Settings API reads from: ~/.gsd/preferences.md (global) and {projectRoot}/.gsd/preferences.md (per-project)
File format: markdown with YAML frontmatter — parse with gray-matter, NOT JSON.parse

Required GSD 2 fields (from CONTEXT.md decisions):
  research_model:   string — select from model options
  planning_model:   string — select from model options
  execution_model:  string — select from model options
  completion_model: string — select from model options
  budget_ceiling:   number — numeric input, maps to budget_ceiling in preferences.md
  skill_discovery:  "auto" | "suggest" | "off" — select

Model options list (from RESEARCH.md):
  ["claude-sonnet-4-6", "claude-opus-4-6", "claude-haiku-4-5-20251001"]

REMOVED from settings (v1 fields — must not appear):
  skip_permissions  (replaced by Phase 17 trust dialog)
  allowed_tools     (v1 Claude Code specific)
  model (single global model) → replaced by four per-phase fields

Design system: same dark theme tokens. Settings section label changes from
"Claude Code Options" to "GSD 2 Options" or just "AI Model Settings".

GSD2Preferences interface (already added in types.ts by Plan 02):
```typescript
export interface GSD2Preferences {
  research_model?: string;
  planning_model?: string;
  execution_model?: string;
  completion_model?: string;
  budget_ceiling?: number;
  skill_discovery?: "auto" | "suggest" | "off";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update SettingsView.tsx — GSD 2 fields, remove v1 fields</name>
  <files>
    packages/mission-control/src/components/views/SettingsView.tsx
  </files>
  <behavior>
    - Renders four model select dropdowns: "Research model", "Planning model", "Execution model", "Completion model"
    - Each select has options: claude-sonnet-4-6, claude-opus-4-6, claude-haiku-4-5-20251001
    - Renders "Budget ceiling" numeric input (dollar amount or credit limit)
    - Renders "Skill discovery" select with options: auto, suggest, off
    - Does NOT render "Skip permissions" toggle or any permissions-related toggle
    - Does NOT render "Allowed tools" text field
    - Does NOT render a single "Model" global select (replaced by four per-phase selects)
    - Section heading changed from "Claude Code Options" to "AI Model Settings" (or equivalent GSD 2 label)
    - settings-view-gsd2.test.ts assertions GREEN
  </behavior>
  <action>
    Read packages/mission-control/src/components/views/SettingsView.tsx in full first.

    Identify the section that currently renders:
    - single "Model" select field
    - "Skip permissions" toggle
    - "Allowed tools" text input or checkbox list

    REPLACE that section with the GSD 2 fields:
    1. Section heading: "AI Model Settings"
    2. Four model selects, each with label and GSD2_MODEL_OPTIONS:
       ```
       Research model: [select]
       Planning model: [select]
       Execution model: [select]
       Completion model: [select]
       ```
    3. Budget ceiling: numeric input, label "Budget ceiling ($)", placeholder "e.g. 50"
    4. Skill discovery: select with options auto / suggest / off, label "Skill discovery"

    Keep all other sections (general settings, project path, theme, etc.) unchanged.

    State management: SettingsView likely reads from a local API (settings-api.ts via fetch). The form state should bind to `preferences.research_model`, `preferences.planning_model`, etc. If SettingsView uses a flat state object, map the four model fields into it. On save, POST the GSD2Preferences shape to /api/settings.

    Keep the existing save/cancel button logic unchanged — only the fields in the "Claude Code Options" section need replacing.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "settings-view-gsd2" 2>&1 | tail -10</automated>
  </verify>
  <done>settings-view-gsd2.test.ts assertions GREEN. SettingsView renders budget_ceiling input, skill_discovery select, and four per-phase model selects. No skip_permissions or allowed_tools fields present.</done>
</task>

<task type="auto">
  <name>Task 2: Update settings-api.ts to read/write preferences.md</name>
  <files>
    packages/mission-control/src/server/settings-api.ts
  </files>
  <action>
    Read packages/mission-control/src/server/settings-api.ts in full first. Also read packages/mission-control/tests/settings-api.test.ts to understand existing test coverage before editing.

    Current behavior: settings-api.ts likely reads from config.json (v1 GSD format) or possibly already reads from another source. Check what file it currently reads/writes.

    IF settings-api.ts reads config.json:
    CHANGE to read preferences.md:
    1. Read file with readFileText()
    2. Parse with gray-matter → extract GSD2Preferences fields
    3. Return GSD2Preferences object to client
    4. On write (POST): serialize back to preferences.md frontmatter format:
       ```markdown
       ---
       research_model: claude-sonnet-4-6
       planning_model: claude-sonnet-4-6
       execution_model: claude-sonnet-4-6
       completion_model: claude-sonnet-4-6
       budget_ceiling: 50
       skill_discovery: auto
       ---
       ```
    5. Write file with Bun.write()

    The preferences.md path is: join(projectRoot, ".gsd", "preferences.md")
    where projectRoot comes from the pipeline's current project path.

    IF settings-api.ts already reads a markdown file — check if it reads .gsd/preferences.md or .planning/config.json and update accordingly.

    Return type: the GET handler returns GSD2Preferences JSON. The POST handler accepts GSD2Preferences JSON and writes back to preferences.md.

    Do NOT break the existing settings-api.test.ts — check what the tests expect and update fixture paths from config.json to preferences.md if needed.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "settings-api" 2>&1 | tail -10</automated>
  </verify>
  <done>settings-api.test.ts remains GREEN (or is updated to match new preferences.md schema). settings-api.ts reads and writes preferences.md. GET returns GSD2Preferences. POST writes YAML frontmatter to .gsd/preferences.md.</done>
</task>

</tasks>

<verification>
Settings view and API tests:
`cd packages/mission-control && bun test --testPathPattern "settings" 2>&1 | tail -20`

Confirm no v1 settings fields in SettingsView:
`grep -n "skip_permissions\|allowed_tools\|skipPermissions\|allowedTools" packages/mission-control/src/components/views/SettingsView.tsx`
(should return 0 lines)

Confirm preferences.md referenced in settings-api.ts:
`grep -n "preferences.md" packages/mission-control/src/server/settings-api.ts`
(should return hits)
</verification>

<success_criteria>
- SettingsView renders four per-phase model selects
- SettingsView renders budget ceiling input
- SettingsView renders skill_discovery select
- No skip_permissions or allowed_tools fields in SettingsView
- settings-api.ts reads/writes .gsd/preferences.md using gray-matter
- settings-view-gsd2.test.ts GREEN
- settings-api.test.ts remains GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/12-gsd-2-compatibility-pass/12-05-SUMMARY.md`
</output>
