---
phase: 12-gsd-2-compatibility-pass
plan: "06"
type: execute
wave: 4
depends_on:
  - "12-04"
  - "12-05"
files_modified: []
autonomous: false
requirements:
  - COMPAT-01
  - COMPAT-02
  - COMPAT-03
  - COMPAT-04
  - COMPAT-05
  - COMPAT-06
  - COMPAT-07

must_haves:
  truths:
    - "Full bun test suite is GREEN"
    - "GSD 2 project opens and shows milestone/slice data from .gsd/"
    - "Autocomplete shows /gsd subcommands only — no /gsd: entries"
    - "gsd process is visible in Task Manager when /gsd auto is sent"
    - "V1 project shows migration banner with Run migration button"
    - "Settings shows GSD 2 fields only"
  artifacts: []
  key_links: []
---

<objective>
Wave 4: Full-suite verification and human regression test — confirm all 7 COMPAT requirements are met end-to-end in a running app before closing Phase 12.

Purpose: CONTEXT.md mandates a regression test: open GSD 2 project → STATE.md read correctly → autocomplete shows GSD 2 commands → /gsd auto spawns and streams. This plan runs the automated suite and gates on human confirmation for the 5 visual/functional checks that require a live app.
Output: All test files GREEN, human confirms live regression behaviors.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-gsd-2-compatibility-pass/12-CONTEXT.md
@.planning/phases/12-gsd-2-compatibility-pass/12-VALIDATION.md
@.planning/phases/12-gsd-2-compatibility-pass/12-04-SUMMARY.md
@.planning/phases/12-gsd-2-compatibility-pass/12-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — confirm all Phase 12 tests GREEN</name>
  <files>packages/mission-control/tests/</files>
  <action>
    Run the full bun test suite. Report:
    1. Total tests passing / failing
    2. Which Phase 12 test files pass (slash-commands, claude-process-gsd, migration-banner, settings-view-gsd2, state-deriver)
    3. Any regressions in pre-existing test files

    If any Phase 12 test is still RED or any existing test regressed, diagnose and fix before proceeding to the checkpoint task.

    Quick command: `cd packages/mission-control && bun test 2>&1 | tail -30`
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test 2>&1 | grep -E "pass|fail|error" | tail -10</automated>
  </verify>
  <done>All tests pass. Zero failures. Zero regressions. Exit code 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human regression test — verify all 7 COMPAT requirements in live app</name>
  <files>packages/mission-control/</files>
  <action>
    Start the dev server (`cd packages/mission-control && bun dev`) then run each verification check manually as described below. This is a visual/functional regression test — automated tests cannot cover live process spawning and visual rendering.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun dev --help 2>&1 | head -3</automated>
  </verify>
  <done>
    Human has confirmed all 5 checks below pass. Type "approved" to proceed.

    **Check 1 — GSD 2 project reads STATE.md (COMPAT-01, COMPAT-02, COMPAT-03):**
    Open a project that has a .gsd/ directory. In the sidebar, confirm:
    - Milestone name / slice name appears (read from .gsd/STATE.md + M{NNN}-ROADMAP.md)
    - It is NOT showing a 404 or "no state" error
    - The data matches what is in the .gsd/STATE.md file directly

    **Check 2 — Autocomplete shows GSD 2 commands (COMPAT-04):**
    Click into the chat input. Type "/gsd". Confirm:
    - Dropdown shows entries like "/gsd auto", "/gsd discuss", "/gsd stop"
    - No entry shows "/gsd:" colon syntax
    - Type "/gsd a" — confirm only "/gsd auto" remains

    **Check 3 — gsd binary spawned (COMPAT-05):**
    Send "/gsd auto" in the chat input. Then:
    - Open Task Manager (Windows) — check for a "gsd" process
    - The process should be visible; it should NOT be "claude" or "claude-code"
    - (Stream output may differ — Phase 13 handles Pi SDK parsing)

    **Check 4 — Migration banner for v1 project (COMPAT-06):**
    Open a project that has a .planning/ directory but NO .gsd/ directory. Confirm:
    - A banner appears at the top of the chat area
    - Banner text: "This project uses GSD v1. Run /gsd migrate to upgrade it."
    - "Run migration" button is visible
    - Clicking "Run migration" sends /gsd migrate to chat

    **Check 5 — Settings panel GSD 2 fields (COMPAT-07):**
    Open Settings panel. Confirm:
    - "Research model" select is present
    - "Planning model" select is present
    - "Execution model" select is present
    - "Completion model" select is present
    - "Budget ceiling" input is present
    - "Skill discovery" select is present (options: auto, suggest, off)
    - NO "Skip permissions" toggle visible
    - NO "Allowed tools" field visible
  </done>
</task>

</tasks>

<verification>
Automated suite:
`cd packages/mission-control && bun test 2>&1 | tail -5`
Expected: 0 failures, exit code 0.
</verification>

<success_criteria>
- bun test passes with 0 failures
- Human confirms all 5 regression checks pass
- Phase 12 GSD 2 Compatibility Pass is complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-gsd-2-compatibility-pass/12-06-SUMMARY.md`
</output>
