---
phase: 12-gsd-2-compatibility-pass
plan: "03"
type: execute
wave: 2
depends_on:
  - "12-02"
files_modified:
  - packages/mission-control/src/server/watcher.ts
  - packages/mission-control/src/server.ts
  - packages/mission-control/src/server/fs-api.ts
  - packages/mission-control/src/server/fs-types.ts
  - packages/mission-control/src/server/pipeline.ts
  - packages/mission-control/src/server/claude-process.ts
autonomous: true
requirements:
  - COMPAT-01
  - COMPAT-05

must_haves:
  truths:
    - "Bun.watch() receives a .gsd/ path, never a .planning/ path"
    - "server.ts startup resolves .gsd/ not .planning/"
    - "switchProject handler resolves .gsd/ not .planning/"
    - "fs-api.ts project detection checks .gsd/ — GSD 2 projects show the GSD icon in file browser"
    - "ClaudeProcessManager spawns gsd binary, not claude"
    - "claude-process-gsd.test.ts binary name cases turn GREEN"
    - "watcher.test.ts and pipeline-switch.test.ts remain GREEN"
  artifacts:
    - path: "packages/mission-control/src/server/watcher.ts"
      provides: "Dotfile filter allows .gsd/ events through"
      contains: ".gsd"
    - path: "packages/mission-control/src/server.ts"
      provides: "planningDir resolved to .gsd/ at startup and switchProject"
      contains: ".gsd"
    - path: "packages/mission-control/src/server/fs-api.ts"
      provides: "GSD project detection checks .gsd/ directory"
      contains: ".gsd"
    - path: "packages/mission-control/src/server/claude-process.ts"
      provides: "Spawn binary is gsd"
      contains: "\"gsd\""
  key_links:
    - from: "src/server.ts"
      to: "src/server/pipeline.ts"
      via: "new Pipeline({ planningDir: resolve(repoRoot, '.gsd') })"
      pattern: "\\.gsd"
    - from: "src/server/watcher.ts"
      to: ".gsd/ directory"
      via: "Bun.watch dotfile filter exception"
      pattern: "\\.gsd"
    - from: "src/server/claude-process.ts"
      to: "gsd binary"
      via: "Bun.spawn first argument"
      pattern: "\"gsd\""
---

<objective>
Wave 2: Migrate all file path strings from `.planning` to `.gsd` and change the child process binary from `claude` to `gsd`.

Purpose: Without these changes the watcher watches the wrong directory (no state updates) and the process spawns the wrong binary (chat doesn't work). These are string-level surgical edits — no logic changes.
Output: Watcher, server, fs-api, pipeline, and claude-process all using GSD 2 conventions.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-gsd-2-compatibility-pass/12-CONTEXT.md
@.planning/phases/12-gsd-2-compatibility-pass/12-RESEARCH.md
@.planning/phases/12-gsd-2-compatibility-pass/12-02-SUMMARY.md

<interfaces>
<!-- Precise touch points from RESEARCH.md Pattern 3 and Pattern 4 -->

watcher.ts line 44 (approximate):
  BEFORE: `if (firstSegment.startsWith(".") && !firstSegment.startsWith(".planning")) return`
  AFTER:  `if (firstSegment.startsWith(".") && !firstSegment.startsWith(".gsd")) return`

server.ts:
  Line 19 (startup):   `planningDir: resolve(repoRoot, ".planning")`  → `.gsd`
  Line 111 (switch):   `const planningDir = resolve(projectPath, ".planning")`  → `.gsd`
  Line 114 (access):   `await access(planningDir)` — logic unchanged, now checks .gsd/
  Comment line 109:    "Always pass .planning/ path" → "Always pass .gsd/ path"

fs-api.ts (3 occurrences):
  `access(join(..., ".planning"))` → `access(join(..., ".gsd"))`
  `access(join(resolved, ".planning"))` → `access(join(resolved, ".gsd"))`
  Any string ".planning" in isGsdProject checks → ".gsd"

fs-types.ts:
  Comment on isGsdProject field: update "contains .planning/" → "contains .gsd/"

pipeline.ts:
  Comment update only: "one level up from .planning" → "one level up from .gsd"
  No logic change — path comes from options.planningDir passed by caller

claude-process.ts (RESEARCH.md Pattern 4):
  Spawn binary: "claude" → "gsd"
  Remove --resume flag from args (Claude Code specific; gsd binary may not support it)
  Keep all other args for now — Phase 13 handles full Pi SDK arg mapping
  Remove or update any comment referencing "claude-code" or CLAUDECODE env stripping

Variable name `planningDir` throughout — DO NOT rename. Only the string value ".planning" changes to ".gsd".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate .planning → .gsd path strings in watcher, server, fs-api, pipeline</name>
  <files>
    packages/mission-control/src/server/watcher.ts
    packages/mission-control/src/server.ts
    packages/mission-control/src/server/fs-api.ts
    packages/mission-control/src/server/fs-types.ts
    packages/mission-control/src/server/pipeline.ts
  </files>
  <action>
    Read each file before editing. Make only the changes listed in the interfaces section above — nothing else.

    **watcher.ts:**
    Find the dotfile filter line containing `.startsWith(".planning")`. Change to `.startsWith(".gsd")`. This is a single character/string change. Do not touch anything else in this file.

    **server.ts:**
    Two string changes:
    1. Startup path: `resolve(repoRoot, ".planning")` → `resolve(repoRoot, ".gsd")`
    2. switchProject path: `resolve(projectPath, ".planning")` → `resolve(projectPath, ".gsd")`
    Update the comment on the line above switchProject that says ".planning/ path" to ".gsd/ path".
    The `hasPlanningDir` variable (if present) — check if it is still used for migration detection. If it guards "no .planning/ yet → new project" logic, update the comment to reference .gsd/ and keep the logic since it now correctly checks .gsd/ presence.

    **fs-api.ts:**
    Find all occurrences of `".planning"` in this file. Change each to `".gsd"`. This affects `listDirectory()` and `detectProject()` — both check for the GSD directory to set `isGsdProject`. After the change, a folder is identified as a GSD project if it contains `.gsd/`. Also check if there is dual detection (checking both .planning AND .gsd) — if so, keep the .planning check ONLY for migration banner purposes (needsMigration flow in state-deriver). If the check is purely "is this a GSD project?", change to .gsd only.

    **fs-types.ts:**
    Find the comment on `isGsdProject` field. Update it from "contains .planning/" to "contains .gsd/".

    **pipeline.ts:**
    Find any comment referencing ".planning" (e.g., "one level up from .planning"). Update comment text only. Do not change any logic — the path value comes from options.planningDir which is now resolved to .gsd/ by server.ts.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "watcher|pipeline-switch" 2>&1 | tail -20</automated>
  </verify>
  <done>watcher.test.ts and pipeline-switch.test.ts remain GREEN. No .planning string remains in watcher.ts, server.ts, fs-api.ts, or pipeline.ts (except where explicitly kept for migration detection in fs-api.ts). grep confirms: `grep -r '\.planning' packages/mission-control/src/server/ --include="*.ts" | grep -v "migration\|v1\|comment"` returns no hits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Change claude-process.ts spawn binary from claude to gsd</name>
  <files>
    packages/mission-control/src/server/claude-process.ts
  </files>
  <behavior>
    - Spawn call uses "gsd" as first argument (binary name), not "claude"
    - "--resume" flag removed from args (Claude Code specific; Phase 13 adds gsd session continuity)
    - No other arg changes — Phase 13 handles full Pi SDK arg mapping
    - claude-process-gsd.test.ts binary name assertion turns GREEN
  </behavior>
  <action>
    Read packages/mission-control/src/server/claude-process.ts in full first.

    Find the Bun.spawn call (or equivalent process spawn). Change:
    - The binary name argument: `"claude"` → `"gsd"`

    Find where `--resume` is pushed to the args array. Remove that block:
    - `if (this._sessionId) args.push("--resume", this._sessionId);`
    Delete this conditional entirely. The _sessionId field can stay for now (Phase 13 will implement proper gsd session continuity).

    Leave all other args as-is:
    - `-p`, `--output-format stream-json`, `--verbose`, `--include-partial-messages` — keep for now
    - Phase 13 will map these to gsd's actual arg interface

    Update any comment that references "claude", "claude-code", "Claude Code CLI", or the CLAUDECODE env var stripping rationale. Change to reference "gsd" and "Pi SDK" or add "TODO(Phase-13): update args for Pi SDK interface".

    Do NOT change the class name `ClaudeProcessManager` — renaming causes ripple effects across the codebase. The implementation changes; the name can be updated in a later cleanup.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "claude-process-gsd" 2>&1 | tail -10</automated>
  </verify>
  <done>claude-process-gsd.test.ts binary name assertion is GREEN. Binary is "gsd". "--resume" flag is absent. All other existing claude-process tests pass (no regression).</done>
</task>

</tasks>

<verification>
Confirm no residual .planning string in server-side source:
`grep -rn '\.planning' packages/mission-control/src/ --include="*.ts" --include="*.tsx" | grep -v "//.*migration\|migration\|MIGRATION\|v1\|\.planning.*comment"`

Full watcher + pipeline test suite:
`cd packages/mission-control && bun test --testPathPattern "watcher|pipeline" 2>&1 | tail -20`

Binary check in process test:
`cd packages/mission-control && bun test --testPathPattern "claude-process" 2>&1 | tail -10`
</verification>

<success_criteria>
- All .planning path strings in server source changed to .gsd (except migration detection code)
- watcher.ts dotfile filter allows .gsd/ events through
- server.ts startup and switchProject use .gsd/
- fs-api.ts detects GSD 2 projects by .gsd/ presence
- claude-process.ts spawns "gsd" binary
- --resume flag removed from spawn args
- claude-process-gsd.test.ts GREEN
- watcher.test.ts and pipeline-switch.test.ts remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/12-gsd-2-compatibility-pass/12-03-SUMMARY.md`
</output>
