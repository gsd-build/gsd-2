---
phase: 12-gsd-2-compatibility-pass
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/mission-control/tests/slash-commands.test.ts
  - packages/mission-control/tests/claude-process-gsd.test.ts
  - packages/mission-control/tests/migration-banner.test.ts
  - packages/mission-control/tests/settings-view-gsd2.test.ts
  - packages/mission-control/tests/state-deriver.test.ts
autonomous: true
requirements:
  - COMPAT-01
  - COMPAT-02
  - COMPAT-03
  - COMPAT-04
  - COMPAT-05
  - COMPAT-06
  - COMPAT-07

must_haves:
  truths:
    - "Four new test files exist and are RED (failing) before any production code changes"
    - "state-deriver.test.ts has GSD 2 fixture cases for dynamic ID resolution"
    - "All five test files run without import errors — only assertion failures"
  artifacts:
    - path: "packages/mission-control/tests/slash-commands.test.ts"
      provides: "RED tests for GSD 2 command registry (COMPAT-04)"
      contains: "GSD_COMMANDS"
    - path: "packages/mission-control/tests/claude-process-gsd.test.ts"
      provides: "RED tests for gsd binary spawn (COMPAT-05)"
      contains: "gsd"
    - path: "packages/mission-control/tests/migration-banner.test.ts"
      provides: "RED tests for needsMigration flag (COMPAT-06)"
      contains: "needsMigration"
    - path: "packages/mission-control/tests/settings-view-gsd2.test.ts"
      provides: "RED tests for GSD 2 settings fields (COMPAT-07)"
      contains: "budget_ceiling"
    - path: "packages/mission-control/tests/state-deriver.test.ts"
      provides: "GSD 2 fixture cases augmented (COMPAT-01, -02, -03)"
      contains: ".gsd"
  key_links:
    - from: "tests/slash-commands.test.ts"
      to: "src/lib/slash-commands.ts"
      via: "import { GSD_COMMANDS }"
      pattern: "GSD_COMMANDS"
    - from: "tests/claude-process-gsd.test.ts"
      to: "src/server/claude-process.ts"
      via: "import { ClaudeProcessManager }"
      pattern: "ClaudeProcessManager"
    - from: "tests/migration-banner.test.ts"
      to: "src/server/state-deriver.ts"
      via: "checkMigrationNeeded or buildFullState"
      pattern: "needsMigration"
    - from: "tests/settings-view-gsd2.test.ts"
      to: "src/components/views/SettingsView.tsx"
      via: "import SettingsView"
      pattern: "SettingsView"
---

<objective>
Wave 0: Create failing test stubs that RED-gate all GSD 2 compatibility requirements before production code changes begin.

Purpose: Nyquist rule enforcement — COMPAT-04, COMPAT-05, COMPAT-06, COMPAT-07 have no existing test files. These stubs must fail first so subsequent plan waves turn them green. Also augment state-deriver.test.ts with GSD 2 fixture cases covering COMPAT-01 (watcher path), COMPAT-02 (GSD 2 schema), and COMPAT-03 (dynamic IDs).
Output: Five test files — four new RED stubs, one augmented — all runnable with `bun test` without import errors.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-gsd-2-compatibility-pass/12-CONTEXT.md
@.planning/phases/12-gsd-2-compatibility-pass/12-RESEARCH.md
@.planning/phases/12-gsd-2-compatibility-pass/12-VALIDATION.md

<interfaces>
<!-- Existing types from production code — do NOT change in this plan -->

From packages/mission-control/src/lib/slash-commands.ts:
```typescript
export interface SlashCommand {
  command: string;
  description: string;
  args: string;
  source: "gsd" | "claude" | "custom";
}
export const GSD_COMMANDS: SlashCommand[];      // currently 21 v1 entries
export const CLAUDE_CODE_COMMANDS: SlashCommand[];
```

From packages/mission-control/src/server/claude-process.ts:
```typescript
export class ClaudeProcessManager {
  spawn(prompt: string): void;
  // internally calls Bun.spawn with binary "claude" — must be "gsd" after COMPAT-05
}
```

From packages/mission-control/tests/state-deriver.test.ts:
<!-- Existing file tests buildFullState() with .planning/ fixtures. Tests use tmpDir with
     .planning/STATE.md, ROADMAP.md, config.json, phases/ subdirectory.
     GSD 2 fixtures will use .gsd/ directory with GSD 2 flat schema instead. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED test stubs for COMPAT-04, COMPAT-05, COMPAT-06, COMPAT-07</name>
  <files>
    packages/mission-control/tests/slash-commands.test.ts
    packages/mission-control/tests/claude-process-gsd.test.ts
    packages/mission-control/tests/migration-banner.test.ts
    packages/mission-control/tests/settings-view-gsd2.test.ts
  </files>
  <behavior>
    slash-commands.test.ts RED behaviors:
    - GSD_COMMANDS contains all 9 GSD 2 commands (/gsd, /gsd auto, /gsd stop, /gsd discuss, /gsd status, /gsd queue, /gsd prefs, /gsd migrate, /gsd doctor)
    - GSD_COMMANDS contains NO entry whose command starts with "/gsd:" (v1 colon syntax)
    - GSD_COMMANDS has exactly 9 entries (not 21)
    - Each entry has source: "gsd"

    claude-process-gsd.test.ts RED behaviors:
    - ClaudeProcessManager spawned binary is "gsd" not "claude"
    - ClaudeProcessManager spawn args do NOT contain "--resume" flag
    - Test uses a process factory mock to capture spawn args (same pattern as pipeline-config-bridge.test.ts)

    migration-banner.test.ts RED behaviors:
    - buildFullState() returns needsMigration: true when tmpDir has .planning/ but no .gsd/
    - buildFullState() returns needsMigration: false when tmpDir has .gsd/ (regardless of .planning/)
    - buildFullState() returns needsMigration: false when neither directory exists

    settings-view-gsd2.test.ts RED behaviors:
    - SettingsView renders a field labeled "Budget ceiling" (budget_ceiling)
    - SettingsView renders a "Skill discovery" select with options auto/suggest/off
    - SettingsView renders per-phase model fields: research, planning, execution, completion
    - SettingsView does NOT render "Skip permissions" toggle
    - SettingsView does NOT render "Allowed tools" field
  </behavior>
  <action>
    Create four test files. All four MUST fail (RED) because production code has not been updated yet.

    **slash-commands.test.ts:**
    Import GSD_COMMANDS from "../src/lib/slash-commands". Write describe block "GSD 2 command registry". Tests must fail because GSD_COMMANDS currently has 21 v1 "/gsd:" entries. Use `expect(GSD_COMMANDS).toHaveLength(9)` etc.

    **claude-process-gsd.test.ts:**
    Import ClaudeProcessManager from "../src/server/claude-process". Use a process factory mock that captures the binary name passed to Bun.spawn (same injection pattern used in pipeline-config-bridge.test.ts — pass a mock processFactory via PipelineOptions). Assert `spawnedBinary === "gsd"`. Test must fail because binary is currently "claude".

    NOTE: Read packages/mission-control/tests/pipeline-config-bridge.test.ts first to understand the mock pattern before writing this test.

    **migration-banner.test.ts:**
    Import buildFullState from "../src/server/state-deriver". Use mkdtemp to create a real tmpDir. Test 1: mkdir tmpDir/.planning, do NOT create .gsd/ → assert `(await buildFullState(path.join(tmpDir, ".gsd"))).needsMigration === true`. Test 2: mkdir tmpDir/.gsd → assert false. Test must fail because GSD2State type does not exist yet and needsMigration field is absent.

    **settings-view-gsd2.test.ts:**
    Use React Testing Library render (check if RTL is installed — if not, use same renderToString approach from error-boundary.test.tsx). Render SettingsView with minimal props stub. Assert the rendered output contains "Budget ceiling" and "Skill discovery". Assert it does NOT contain "skip_permissions" or "allowed_tools" text. Test must fail because SettingsView has v1 fields today.

    NOTE: Check packages/mission-control/tests/error-boundary.test.tsx for the renderToString pattern if RTL is not installed.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern "slash-commands|claude-process-gsd|migration-banner|settings-view-gsd2" 2>&1 | tail -20</automated>
  </verify>
  <done>All four files exist. Running `bun test` on them reports failures (not import errors). Exit code non-zero confirms RED state before any implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Augment state-deriver.test.ts with GSD 2 fixture cases</name>
  <files>
    packages/mission-control/tests/state-deriver.test.ts
  </files>
  <behavior>
    GSD 2 fixture cases (RED — fail until Wave 1 implementation):
    - buildFullState() with .gsd/ fixture dir returns a GSD2State (not PlanningState)
    - GSD2State.projectState.active_milestone equals "M001" when STATE.md frontmatter has active_milestone: M001
    - GSD2State.roadmap is derived from "M001-ROADMAP.md" file in .gsd/
    - GSD2State.activePlan is derived from "S01-PLAN.md" when STATE.md has active_slice: S01
    - GSD2State.needsMigration is false for a clean .gsd/ fixture (no .planning/ alongside)
    - File watcher path: the gsdDir passed to buildFullState() is the .gsd/ path (COMPAT-01 regression)
  </behavior>
  <action>
    Read the current state-deriver.test.ts in full. Do NOT delete existing v1 tests — add a new describe block "GSD 2 fixtures" below existing tests.

    Create a GSD 2 fixture helper function `createGsd2Fixture(tmpDir)` that creates:
    - tmpDir/.gsd/STATE.md with YAML frontmatter: active_milestone: M001, active_slice: S01, active_task: T01, status: in_progress, gsd_state_version: "1.0"
    - tmpDir/.gsd/M001-ROADMAP.md with minimal milestone heading content
    - tmpDir/.gsd/S01-PLAN.md with minimal slice plan content
    - tmpDir/.gsd/T01-SUMMARY.md with minimal task summary content
    - tmpDir/.gsd/preferences.md with YAML frontmatter: budget_ceiling: 50, skill_discovery: auto, research_model: claude-sonnet-4-6

    Add test cases calling `buildFullState(path.join(tmpDir, ".gsd"))` against these fixtures. Assertions will fail because buildFullState() currently reads v1 .planning/ schema, but the test structure itself must be importable and runnable.

    After adding, verify the file parses cleanly (no TypeScript errors that prevent the test from running).
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --testPathPattern state-deriver 2>&1 | tail -20</automated>
  </verify>
  <done>state-deriver.test.ts runs. Original v1 tests still pass. New GSD 2 fixture block is present and failing (RED) — confirmed by test output showing new test names in failure list.</done>
</task>

</tasks>

<verification>
Run the full new-stub suite:
`cd packages/mission-control && bun test --testPathPattern "slash-commands|claude-process-gsd|migration-banner|settings-view-gsd2|state-deriver" 2>&1 | tail -30`

Expected: 4 new test files failing + state-deriver existing tests passing + new GSD 2 fixture tests failing. No import errors.
</verification>

<success_criteria>
- Five test files exist with GSD 2 assertion cases
- All four new files are RED (failing assertions, not import errors)
- New state-deriver GSD 2 fixture block is RED
- Existing state-deriver v1 tests remain GREEN (no regression)
- `bun test` exit code is non-zero (failures present)
</success_criteria>

<output>
After completion, create `.planning/phases/12-gsd-2-compatibility-pass/12-01-SUMMARY.md`
</output>
