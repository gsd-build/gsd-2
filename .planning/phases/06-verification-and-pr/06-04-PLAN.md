---
phase: 06-verification-and-pr
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - package.json
autonomous: true
requirements: [VER-01]
gap_closure: true
gap_source: 06-UAT.md
gaps_addressed:
  - "rm -rf packages/*/dist && npm run build:pi exits 0 (VER-01 — clean-state build passes)"

must_haves:
  truths:
    - "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo && npm run build:pi exits 0"
    - "The clean script removes tsbuildinfo files so incremental state cannot survive a clean"
  artifacts:
    - path: "package.json"
      provides: "clean script that removes both dist/ and tsconfig.tsbuildinfo"
      contains: "tsconfig.tsbuildinfo"
  key_links:
    - from: "package.json clean script"
      to: "packages/native/tsconfig.tsbuildinfo"
      via: "rm -rf glob"
      pattern: "packages/\\*/tsconfig\\.tsbuildinfo"
---

<objective>
Fix VER-01 gap: stale `packages/native/tsconfig.tsbuildinfo` survives `rm -rf packages/*/dist`, causing TypeScript's incremental build to skip emission entirely, which leaves `dist/` absent and causes `@gsd/pi-tui` to fail with TS2307 on `@gsd/native` subpath exports.

Purpose: `npm run clean` (and the VER-01 test command) must remove tsbuildinfo files alongside dist/ directories so TypeScript is forced to emit from scratch on every clean build.

Output: `package.json` gains a `clean` script; `build:pi` (and the referenced clean command) reliably removes incremental state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-verification-and-pr/06-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clean script and verify VER-01</name>
  <files>package.json</files>
  <action>
Add a `"clean"` script to the `"scripts"` section of root `package.json`. Insert it immediately before the `"build:pi-tui"` line (or any logical grouping position near the build scripts):

```json
"clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
```

Do NOT modify `build:pi` itself — the VER-01 test explicitly runs the clean command separately before calling `npm run build:pi`, and changing `build:pi` would couple clean semantics to every incremental build invocation.

After adding the script, run the full VER-01 verification sequence to confirm the gap is closed:

```bash
rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo && npm run build:pi
```

The command must exit 0 with no TS2307 errors from `@gsd/pi-tui` regarding `@gsd/native` subpath exports (`/fd`, `/image`, `/text`).
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo && npm run build:pi 2>&1 | tail -20; echo "EXIT: $?"</automated>
  </verify>
  <done>
- `package.json` contains a `"clean"` script with `rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo`
- `rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo && npm run build:pi` exits 0
- No TS2307 errors appear for `@gsd/native` subpath exports
- `npm run clean` runs without error (smoke-check the script itself is valid JSON and callable)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → tsc | tsbuildinfo files are read by tsc as trusted incremental state; stale state can cause silent mis-builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | tsconfig.tsbuildinfo | mitigate | clean script deletes all tsbuildinfo files before build, eliminating stale incremental state |
</threat_model>

<verification>
Run after task completion:

```bash
# Confirm clean script exists
node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.scripts.clean)"

# VER-01 full sequence
rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo && npm run build:pi
echo "VER-01 exit: $?"
```

Expected: clean script prints the rm command; `npm run build:pi` exits 0 with no TS2307 errors.
</verification>

<success_criteria>
- `package.json` has `"clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo"`
- `rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo && npm run build:pi` exits 0
- VER-01 gap is closed — UAT can be re-run to confirm
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-and-pr/06-04-SUMMARY.md` documenting:
- The script added and exact line in package.json
- VER-01 result (exit code, any relevant build output)
- Confirmation that UAT gap is resolved
</output>
