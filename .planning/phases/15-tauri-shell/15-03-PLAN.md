---
phase: 15-tauri-shell
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - src-tauri/src/dep_check.rs
  - src-tauri/dep_screen.html
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - TAURI-03

must_haves:
  truths:
    - "On startup, the app runs which/where checks for bun and gsd before the main window opens"
    - "If both bun and gsd are found, startup proceeds normally with no visible change"
    - "If bun or gsd is missing, a dependency screen is shown as an injected HTML page rather than the main React app"
    - "The dependency screen names which tools are missing and provides plain-language install instructions with one-click links"
    - "The dependency screen has a Retry button that re-runs the checks without restarting the whole app"
    - "cargo check passes after implementing dep_check.rs"
  artifacts:
    - path: "src-tauri/src/dep_check.rs"
      provides: "Platform-aware dependency checker + dep screen injection"
      exports:
        - "run_startup_checks"
        - "check_dependency"
    - path: "src-tauri/dep_screen.html"
      provides: "Pre-dashboard dependency screen HTML"
      contains: "Install Bun"
  key_links:
    - from: "src-tauri/src/dep_check.rs"
      to: "src-tauri/dep_screen.html"
      via: "read file content at runtime and inject via window.eval or navigate"
      pattern: "dep_screen.html"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/dep_check.rs"
      via: "run_startup_checks called in setup()"
      pattern: "dep_check::run_startup_checks"
---

<objective>
Implement dependency checking (bun + gsd presence) on startup and show a pre-dashboard dependency screen when either is missing.

Purpose: Mission Control requires both bun (to run the server) and gsd (to run AI sessions). If a user installs the app without these, they must see clear install instructions rather than a broken UI.

Output:
- src-tauri/src/dep_check.rs — check_dependency() (platform-aware which/where), run_startup_checks() that emits dep-check-result event
- src-tauri/dep_screen.html — standalone HTML page shown by Tauri window when deps are missing
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tauri-shell/15-01-SUMMARY.md

Design system colors (use in dep_screen.html):
- Background: #0F1419, Surface: #131C2B, Border: #1E2D3D
- Accent cyan: #5BC8F0, Green: #22C55E, Amber: #F59E0B, Red: #EF4444
- Fonts: JetBrains Mono (body), Share Tech Mono (display)

The dep_screen.html is NOT served by the Bun server (which may not be running if bun is missing).
Tauri can display a local HTML file directly via window navigation or by using the asset protocol.
Strategy: When deps are missing, emit a `dep-check-failed` event from Rust. The React app listens for this event and shows the dep screen UI. BUT if bun itself is missing, the React app won't load either. So the dep screen must be a raw HTML file that Tauri loads directly.

Approach: In dep_check.rs, if any dependency is missing:
1. Prevent the main window from loading the React app URL
2. Instead, navigate the window to the dep_screen.html using `window.navigate("tauri://localhost/dep_screen.html")` (asset protocol) or embed HTML inline via `window.eval()`

Simpler approach that avoids protocol complexity: inject the dep screen as a data URL or use `window.navigate()` with the Tauri asset protocol. The cleanest M2 approach:
- Include dep_screen.html as a Tauri resource in the bundle (add to tauri.conf.json `bundle.resources`)
- Navigate window to `asset://localhost/dep_screen.html` when deps are missing

This plan implements that approach.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dep_screen.html with install instructions</name>
  <files>src-tauri/dep_screen.html</files>
  <action>
Create src-tauri/dep_screen.html — a self-contained HTML page using inline styles (no external CSS). The page:
- Uses the GSD design system colors
- Detects which tools are missing via URL query params (?missing=bun,gsd or ?missing=bun or ?missing=gsd)
- Shows plain-language install instructions for each missing tool
- Provides one-click links to install pages
- Has a "Retry" button that calls Tauri's invoke("retry_dep_check") command
- Uses Share Tech Mono for the heading, JetBrains Mono for body text (loaded from Google Fonts CDN as fallback — acceptable since this is a setup screen not production UI)

For the Retry button, use `window.__TAURI__.invoke` directly — Tauri injects this global into the webview automatically without any import needed. Do NOT use a CDN import for `@tauri-apps/api`.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GSD Mission Control — Setup Required</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: #0F1419;
      color: #CBD5E1;
      font-family: 'JetBrains Mono', 'Courier New', monospace;
      font-size: 14px;
      line-height: 1.6;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 32px;
    }

    .container {
      max-width: 600px;
      width: 100%;
    }

    .logo {
      font-family: 'Share Tech Mono', 'Courier New', monospace;
      font-size: 24px;
      color: #5BC8F0;
      margin-bottom: 8px;
      letter-spacing: 2px;
    }

    .subtitle {
      color: #64748B;
      margin-bottom: 40px;
      font-size: 13px;
    }

    h1 {
      font-family: 'Share Tech Mono', 'Courier New', monospace;
      font-size: 20px;
      color: #F8FAFC;
      margin-bottom: 8px;
    }

    .intro {
      color: #94A3B8;
      margin-bottom: 32px;
    }

    .dep-card {
      background: #131C2B;
      border: 1px solid #1E2D3D;
      border-radius: 8px;
      padding: 24px;
      margin-bottom: 16px;
    }

    .dep-card.missing {
      border-color: #F59E0B;
    }

    .dep-card.ok {
      border-color: #22C55E;
      opacity: 0.6;
    }

    .dep-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 12px;
    }

    .dep-status {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      flex-shrink: 0;
    }

    .dep-status.missing { background: #F59E0B; }
    .dep-status.ok { background: #22C55E; }

    .dep-name {
      font-family: 'Share Tech Mono', 'Courier New', monospace;
      font-size: 16px;
      color: #F8FAFC;
    }

    .dep-description {
      color: #94A3B8;
      font-size: 13px;
      margin-bottom: 16px;
    }

    .install-link {
      display: inline-block;
      background: #5BC8F0;
      color: #0F1419;
      text-decoration: none;
      padding: 8px 16px;
      border-radius: 4px;
      font-size: 13px;
      font-weight: 600;
      cursor: pointer;
      border: none;
    }

    .install-link:hover { background: #7DD5F5; }

    .retry-section {
      margin-top: 32px;
      text-align: center;
    }

    .retry-btn {
      background: transparent;
      border: 1px solid #1E2D3D;
      color: #5BC8F0;
      padding: 10px 24px;
      border-radius: 4px;
      font-family: 'JetBrains Mono', monospace;
      font-size: 13px;
      cursor: pointer;
    }

    .retry-btn:hover { border-color: #5BC8F0; }

    .footer {
      margin-top: 24px;
      text-align: center;
      color: #475569;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">GSD</div>
    <div class="subtitle">Mission Control</div>

    <h1>Setup Required</h1>
    <p class="intro">Before launching Mission Control, a few tools need to be installed on your system.</p>

    <div id="deps"></div>

    <div class="retry-section">
      <button class="retry-btn" onclick="retryChecks()">Check Again</button>
    </div>

    <div class="footer">
      Once installed, click "Check Again" — Mission Control will open automatically.
    </div>
  </div>

  <script>
    const params = new URLSearchParams(window.location.search);
    const missing = (params.get('missing') || '').split(',').filter(Boolean);

    const ALL_DEPS = [
      {
        id: 'bun',
        name: 'Bun',
        description: 'Bun is the JavaScript runtime that powers Mission Control\'s server. It runs the local web server on your machine.',
        installUrl: 'https://bun.sh',
        installLabel: 'Install Bun at bun.sh →',
      },
      {
        id: 'gsd',
        name: 'GSD CLI',
        description: 'GSD (Get Shit Done) is the AI coding agent that Mission Control orchestrates. It handles all the AI-powered build sessions.',
        installUrl: 'https://github.com/glittercowboy/gsd-pi',
        installLabel: 'Install GSD CLI →',
      },
    ];

    const container = document.getElementById('deps');
    ALL_DEPS.forEach(dep => {
      const isMissing = missing.includes(dep.id);
      const card = document.createElement('div');
      card.className = `dep-card ${isMissing ? 'missing' : 'ok'}`;
      card.innerHTML = `
        <div class="dep-header">
          <div class="dep-status ${isMissing ? 'missing' : 'ok'}"></div>
          <div class="dep-name">${dep.name}</div>
          <span style="margin-left:auto;font-size:12px;color:${isMissing ? '#F59E0B' : '#22C55E'}">${isMissing ? 'Not installed' : 'Installed'}</span>
        </div>
        ${isMissing ? `
          <p class="dep-description">${dep.description}</p>
          <a class="install-link" href="${dep.installUrl}" target="_blank">${dep.installLabel}</a>
        ` : ''}
      `;
      container.appendChild(card);
    });

    async function retryChecks() {
      try {
        // Tauri injects window.__TAURI__ into the webview automatically — no import needed.
        const invoke = window.__TAURI__.invoke;
        await invoke('retry_dep_check');
      } catch {
        // Fallback: reload the page (Tauri will re-run checks on navigation)
        window.location.reload();
      }
    }
  </script>
</body>
</html>
```

Note: The `missing` query param is set by Rust when it navigates the window to this file, e.g. `asset://localhost/dep_screen.html?missing=bun,gsd`.
  </action>
  <verify>
    <automated>ls /c/Users/Bantu/mzansi-agentive/gsd-2/src-tauri/dep_screen.html && echo "EXISTS"</automated>
  </verify>
  <done>dep_screen.html exists; contains Install Bun link, Install GSD CLI link, Retry button using window.__TAURI__.invoke (no CDN imports), query-param-driven dep display, GSD design system colors</done>
</task>

<task type="auto">
  <name>Task 2: Implement dep_check.rs with platform-aware checks</name>
  <files>
    src-tauri/src/dep_check.rs
    src-tauri/tauri.conf.json
    src-tauri/src/commands.rs
    src-tauri/src/lib.rs
  </files>
  <behavior>
    - check_dependency("bun") on Windows runs `where bun` → returns true if exit code 0
    - check_dependency("bun") on macOS/Linux runs `which bun` → returns true if exit code 0
    - check_dependency("gsd") follows same platform pattern
    - run_startup_checks with both present → emits "dep-check-passed" event, window stays at devUrl
    - run_startup_checks with bun missing → navigates window to dep_screen.html?missing=bun
    - run_startup_checks with both missing → navigates to dep_screen.html?missing=bun,gsd
  </behavior>
  <action>
Replace the dep_check.rs stub with a full implementation:

```rust
use std::process::Command;
use tauri::{AppHandle, Manager};

/// Check if a CLI tool is available on PATH.
/// Uses `where` on Windows, `which` on macOS/Linux.
pub fn check_dependency(name: &str) -> bool {
    #[cfg(target_os = "windows")]
    let checker = "where";
    #[cfg(not(target_os = "windows"))]
    let checker = "which";

    Command::new(checker)
        .arg(name)
        .output()
        .map(|out| out.status.success())
        .unwrap_or(false)
}

/// Run startup dependency checks. Called from setup() before any UI interaction.
/// If all deps present: emits `dep-check-passed`.
/// If any dep missing: navigates the main window to dep_screen.html with ?missing= param.
pub async fn run_startup_checks(app: AppHandle) {
    let bun_ok = check_dependency("bun");
    let gsd_ok = check_dependency("gsd");

    if bun_ok && gsd_ok {
        let _ = app.emit("dep-check-passed", ());
        return;
    }

    // Build missing list
    let mut missing: Vec<&str> = Vec::new();
    if !bun_ok { missing.push("bun"); }
    if !gsd_ok { missing.push("gsd"); }
    let missing_str = missing.join(",");

    eprintln!("[dep_check] Missing dependencies: {missing_str}");

    // Navigate main window to dep screen
    // Use the asset protocol to serve the bundled dep_screen.html
    let dep_url = format!("asset://localhost/dep_screen.html?missing={missing_str}");

    // Get main window and navigate
    if let Some(window) = app.get_webview_window("main") {
        let _ = window.navigate(dep_url.parse().expect("valid dep screen URL"));
    }

    // Also emit event in case React app is listening
    let _ = app.emit("dep-check-failed", missing_str);
}
```

Also update src-tauri/tauri.conf.json to add dep_screen.html as a bundle resource and allow the asset protocol to serve it. Add to the `"bundle"` section:
```json
"resources": ["dep_screen.html"]
```

Add a `retry_dep_check` IPC command stub to src-tauri/src/commands.rs:
```rust
#[tauri::command]
pub async fn retry_dep_check(app: tauri::AppHandle) -> bool {
    crate::dep_check::run_startup_checks(app).await;
    true
}
```

Register it in lib.rs invoke_handler:
```rust
commands::retry_dep_check,
```

Note on window.navigate() in Tauri 2: The method is `WebviewWindow::navigate(url: Url)`. Import `tauri::WebviewWindowBuilder` is not needed; `app.get_webview_window("main")` returns `Option<WebviewWindow>`. The window label "main" must match tauri.conf.json — check that the window has no explicit label (Tauri defaults to "main" for the first window defined). If there is no label field in tauri.conf.json windows array, the default is "main".
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>dep_check.rs compiles; check_dependency() uses which/where per platform; run_startup_checks navigates to dep_screen.html when deps missing; cargo check passes; retry_dep_check registered as IPC command</done>
</task>

</tasks>

<verification>
1. `cd src-tauri && cargo check` exits 0
2. `grep "which\|where" src-tauri/src/dep_check.rs` confirms platform detection
3. `grep "dep_screen.html" src-tauri/src/dep_check.rs` confirms navigation
4. `grep "dep_screen.html" src-tauri/tauri.conf.json` confirms resource bundling
5. `grep "Install Bun" src-tauri/dep_screen.html` confirms install instructions present
6. `grep "__TAURI__" src-tauri/dep_screen.html` confirms no CDN import — uses injected global
</verification>

<success_criteria>
- dep_check.rs: check_dependency() platform-aware; run_startup_checks navigates to dep_screen.html with ?missing= when any tool absent
- dep_screen.html: shows missing/present status per tool, install links, Retry button using window.__TAURI__.invoke (no CDN imports), GSD design system colors
- tauri.conf.json: dep_screen.html listed in bundle.resources
- retry_dep_check IPC command registered in commands.rs and lib.rs invoke_handler
- cargo check passes
</success_criteria>

<output>
After completion, create .planning/phases/15-tauri-shell/15-03-SUMMARY.md
</output>
