---
id: T02
parent: S01
milestone: M010
provides:
  - Both builds (npm run build and npm run build:web-host) passing clean after the v2.28.0 merge
key_files:
  - packages/*/dist/ (cleaned)
key_decisions:
  - No build fixes were needed — the merge resolution in T01 was clean enough that no import paths or type signatures required adjustment
patterns_established:
  - Always rm -rf packages/*/dist/ before rebuilding after a large merge to avoid TS5055 stale declaration errors
observability_surfaces:
  - "npm run build" exit code — must be 0 for healthy main build
  - "npm run build:web-host" exit code — must be 0 for healthy web build
  - "@gsd/native" module-not-found warning in web build is expected and benign (Node-only module, gracefully handled at runtime)
duration: 3m
verification_result: passed
completed_at: 2026-03-18
blocker_discovered: false
---

# T02: Clean stale artifacts and fix both builds

**Cleaned packages/*/dist/, rebuilt both targets — zero errors, no fixes needed.**

## What Happened

Removed all `packages/*/dist/` directories to eliminate stale `.d.ts` files that could cause TS5055 errors after the large merge. Ran `npm run build` — all five workspace packages (native, pi-tui, pi-ai, pi-agent-core, pi-coding-agent) compiled cleanly, followed by the root `tsc` and resource/theme/export-html copy steps. Ran `npm run build:web-host` — Next.js 16.1.6 Turbopack compiled successfully with only the expected `@gsd/native` module-not-found warning (Node-only dependency, handled gracefully at runtime). The standalone web host was staged without issues. No TypeScript errors, no import path fixes, no type signature adjustments required — T01's merge resolutions were fully compatible.

## Verification

- `npm run build` — exited 0, all 5 workspace packages + root compiled clean
- `npm run build:web-host` — exited 0, Next.js production build + standalone staging succeeded
- Conflict marker scan — `rg "^<<<<<<<|^>>>>>>>|^=======$" src/ web/ packages/ .github/` returned empty
- Merge history audit — `git log --oneline v2.22.0..HEAD | wc -l` shows 491 (223 upstream + fork commits)
- Fork preservation — `--web` flag and `deriveState` logic confirmed present

## Verification Evidence

| # | Command | Exit Code | Verdict | Duration |
|---|---------|-----------|---------|----------|
| 1 | `rm -rf packages/*/dist/` | 0 | ✅ pass | <1s |
| 2 | `npm run build` | 0 | ✅ pass | 15.5s |
| 3 | `npm run build:web-host` | 0 | ✅ pass | 13.1s |
| 4 | `rg "^<<<<<<<\|^>>>>>>>\|^=======$" src/ web/ packages/ .github/` | 1 (no matches) | ✅ pass | <1s |
| 5 | `git log --oneline v2.22.0..HEAD \| wc -l` | 0 (491 commits) | ✅ pass | <1s |

## Diagnostics

- **Build health:** `npm run build` and `npm run build:web-host` exit codes are the primary signals. Non-zero means regression.
- **Stale artifacts:** If future merges produce TS5055 errors, `rm -rf packages/*/dist/` before rebuilding.
- **Web build warning:** The `@gsd/native` module-not-found warning in `native-git-bridge.ts:127` is expected in the web context — it's a Node-only native module wrapped in a try/catch.

## Deviations

None. No build fixes were needed — all steps completed as planned without iteration.

## Known Issues

None.

## Files Created/Modified

- `packages/*/dist/` — removed stale build artifacts (all regenerated by build)
