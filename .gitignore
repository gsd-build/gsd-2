
# ── GSD (user project artifacts — never commit) ──
.gsd/
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.next/
/dist/
!/pkg/dist/
build/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/
.planning

# ── GSD baseline (auto-generated) ──
dist/
.bg_shell
.gsd*.tgz
.artifacts/
AGENTS.md
.bg-shell/
TODOS.md

# ── GSD baseline (auto-generated) ──
.gsd/

# ── Mission Control ──
packages/*/node_modules/
packages/*/bun.lock

# ── Tauri build output (never commit) ──
packages/mission-control/src-tauri/target/

# ── Playwright E2E (output artifacts — never commit) ──
packages/mission-control/e2e/reports/
packages/mission-control/test-results/
packages/mission-control/playwright-report/
# Root-level leftovers (removed — kept here for safety)
/e2e/reports/
/test-results/
/playwright-report/
